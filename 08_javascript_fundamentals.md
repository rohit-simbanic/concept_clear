# React Developer-দের জন্য JavaScript Deep Dive Guide

স্বাগতম! একজন প্রফেশনাল React ডেভেলপার হওয়ার জন্য JavaScript-এর অন্তর্নিহিত মেকানিজম (Core Internals) বোঝা অত্যন্ত জরুরি। এই গাইডে ২৫টি গুরুত্বপূর্ণ JavaScript কনসেপ্টকে গভীরভাবে আলোচনা করা হয়েছে, যা আপনার React অ্যাপ্লিকেশন ডেভেলপমেন্ট এবং ইন্টারভিউয়ের প্রস্তুতিকে এক নতুন উচ্চতায় নিয়ে যাবে।

---

## ১. Execution Context, How JS is executed & Call Stack

### ১. Simple definition (বাংলায়)
JavaScript-এ যেকোনো কোড রান করার সময় ব্যাকগ্রাউন্ডে যে পরিবেশ বা এনভায়রনমেন্ট তৈরি হয়, তাকে **Execution Context** বলে। এটি মূলত একটি বক্স বা কন্টেইনার যার ভেতরে JavaScript কোড মূল্যায়িত (Evaluate) এবং এক্সিকিউট হয়। আর এই এক্সিকিউশন কনটেক্সটগুলোকে ম্যানেজ করার জন্য যে স্ট্যাক (Stack) ডাটা স্ট্রাকচার ব্যবহৃত হয়, তাকে **Call Stack** বলে।

### ২. Why this concept exists
JavaScript একটি Single-threaded ভাষা, যার মানে এটি একবারে কেবল একটি কাজই করতে পারে। কোড রান করার সময় কোন ফাংশনটি এখন চলছে, তার ভ্যারিয়েবলগুলো কোথায় সংরক্ষিত আছে এবং এরপরে কোন কোডটি রান হবে—এই সবকিছু ট্র্যাক করার জন্যই Execution Context এবং Call Stack-এর অস্তিত্ব রয়েছে।

### ৩. What problem it solves
যদি Execution Context না থাকত, তবে জাভাস্ক্রিপ্ট ইঞ্জিন ট্র্যাক রাখতে পারত না যে কোন ফাংশনের ভেতরে কোন ভ্যারিয়েবল ডিক্লেয়ার করা হয়েছে এবং ফাংশনটি শেষ হওয়ার পর এক্সিকিউশন কন্ট্রোল কোথায় ফেরত যাবে। এটি কোড এক্সিকিউশনের ফ্লো এবং মেমোরি ম্যানেজমেন্টের সমস্যার সমাধান করে।

### ৪. Real-life analogy
চিন্তা করুন আপনি একটি বই পড়ছেন। পড়তে পড়তে আপনি একটি লাইনে রেফারেন্স পেলেন অন্য একটি চ্যাপ্টারের। আপনি ওই নতুন চ্যাপ্টারে চলে গেলেন এবং সেখানে পড়ার সময় আরেকটি ছোট নোট দেখার প্রয়োজন হলো। আপনি নোটে গেলেন, নোট পড়া শেষ করে আবার চ্যাপ্টারে ফিরলেন, এবং চ্যাপ্টার শেষ করে মূল পাতায় ফিরে এলেন। এখানে বইয়ের পাতা এবং নোটগুলো হলো এক একটি Execution Context, আর আপনার মাথায় রাখা "কোথায় ফিরতে হবে" সেই লিস্টটি হলো Call Stack।

### ৫. How React works internally regarding this concept
React-এর রেন্ডার ফেজ (Render Phase) পুরোটাই JavaScript-এর Call Stack-এর ওপর নির্ভর করে। যখন React কোনো কম্পোনেন্ট রেন্ডার করে (যেমন: `App()`), তখন Call Stack-এ `App` এর জন্য একটি Execution Context তৈরি হয়। যদি `App`-এর ভেতরে `Navbar()` কম্পোনেন্ট থাকে, তবে Call Stack-এ `Navbar` এর context পুশ হয়। React 16-এর আগে (Stack Reconciler-এ) এই প্রসেসটি সিঙ্ক্রোনাস ছিল, যা বড় অ্যাপে ব্রাউজার ফ্রেম ড্রপ করাত। React 16+ এ Fiber আর্কিটেকচারের মাধ্যমে এই Call Stack ব্লকিং সমস্যা দূর করে রেন্ডারিং প্রসেসকে ছোট ছোট চাঙ্কে ভাগ করে এক্সিকিউট করা হয়।

### ৬. Basic example
```javascript
const globalVar = "I am global";

defOne();

function defOne() {
  const a = "Hello";
  defTwo();
  console.log(a + " from defOne");
}

function defTwo() {
  const b = "World";
  console.log(b + " from defTwo");
}
```

### ৭. Step-by-step explanation of the code
১. প্রথমে **Global Execution Context (GEC)** তৈরি হয় এবং Call Stack-এ পুশ হয়।
২. প্রথম ধাপে মেমোরি ক্রিয়েশন ফেজে `globalVar`, `defOne`, এবং `defTwo` এর জন্য মেমোরি স্পেস বরাদ্দ হয়।
৩. দ্বিতীয় ধাপে কোড রান হওয়া শুরু করে। `defOne()` কল হওয়ার সাথে সাথে Call Stack-এ `defOne()`-এর জন্য একটি নতুন **Function Execution Context (FEC)** তৈরি হয়।
৪. `defOne` এর ভেতর থেকে `defTwo()` কল হলে Call Stack-এ `defTwo()` এর FEC পুশ হয়।
৫. `defTwo` এক্সিকিউশন শেষ করে কনসোলে প্রিন্ট করে এবং স্ট্যাক থেকে পপ (Pop) হয়ে যায়।
৬. এরপর কন্ট্রোল আবার `defOne` এ ফিরে আসে, তার বাকি অংশ রান করে এবং সেও স্ট্যাক থেকে বের হয়ে যায়।

### ৮. Another real-world example
```javascript
function calculateCartTotal(price, tax) {
  const total = price + getTaxAmount(price, tax);
  return total;
}

function getTaxAmount(price, tax) {
  return price * tax;
}

const finalAmount = calculateCartTotal(100, 0.15);
console.log(finalAmount);
```
এখানে `calculateCartTotal` কল হওয়ার পর Call Stack-এ যায়, সেখান থেকে `getTaxAmount` কল হয়ে স্ট্যাকে ঢোকে। ট্যাক্স হিসাব করে রিটার্ন করার পর `getTaxAmount` স্ট্যাক থেকে পপ হয়ে যায় এবং সবশেষে `calculateCartTotal` পপ হয়।

### ৯. Common mistakes beginners make
* **Maximum call stack size exceeded (Stack Overflow):** রিকার্সিভ ফাংশনে বেস কেস (Base Case) না দিলে ফাংশনটি নিজেকে কল করতেই থাকে, ফলে Call Stack ওভারফ্লো হয়ে ব্রাউজার ক্র্যাশ করে।

### ১০. Interview questions related to this topic
* **Q:** JavaScript-এ Execution Context-এর দুটি ফেজ কী কী?
  * **Ans:** Memory Creation Phase (যেখানে ভ্যারিয়েবল ও ফাংশন ডিক্লেয়ারেশনের জন্য মেমোরি রাখা হয়) এবং Code Execution Phase (যেখানে লাইন বাই লাইন কোড রান হয়)।
* **Q:** Call Stack কী?
  * **Ans:** এটি একটি LIFO (Last In, First Out) ডাটা স্ট্রাকচার যা জাভাস্ক্রিপ্ট ইঞ্জিনের এক্সিকিউশন ট্র্যাক করে।

### ১১. Best practices
* রিকার্সিভ ফাংশন বা লুপ ব্যবহারের সময় স্ট্যাক ট্র্যাকিং ঠিক রাখতে সবসময় ডিফেন্সিভ কোডিং করুন এবং বেস কন্ডিশন নিশ্চিত করুন।

### ১২. Performance considerations
* Call Stack-এ খুব বেশি ডিপ বা নেস্টেড ফাংশন কল এড়িয়ে চলুন, কারণ এটি মেমোরি কনজাম্পশন বাড়ায়।

### ১৩. When NOT to use it
* এটি জাভাস্ক্রিপ্টের বিল্ট-ইন মেকানিজম, তাই এটি ব্যবহার না করার কোনো সুযোগ নেই। তবে পারফরম্যান্স ক্রিটিক্যাল কাজে অতিরিক্ত রিকার্সন এড়িয়ে চলাই শ্রেয়।

### ১৪. Comparison with similar concepts
| বৈশিষ্ট্য | Execution Context | Call Stack |
| :--- | :--- | :--- |
| **ভূমিকা** | কোড রান করার পরিবেশ বা কন্টেইনার | পরিবেশগুলোর এক্সিকিউশন অর্ডার ট্র্যাকিং |
| **গঠন** | Memory Space + Execution Thread | LIFO ডাটা স্ট্রাকচার |

### ১৫. Summary in simple Bangla
Execution Context হলো জাভাস্ক্রিপ্টের কোড রান করার মাঠ, আর Call Stack হলো সেই রেফারি যে ট্র্যাক করে কখন কোন খেলোয়াড় (ফাংশন) মাঠে নামবে আর কখন উঠবে।

### ১৬. 5 MCQ questions
১. জাভাস্ক্রিপ্ট ইঞ্জিন রান করার সাথে সাথে কোন Execution Context তৈরি হয়?
   (ক) Function EC (খ) Global EC (গ) Local EC (ঘ) Dynamic EC
   **উত্তরঃ খ**
২. Call Stack কোন নীতি অনুসরণ করে?
   (ক) FIFO (খ) LIFO (গ) LILO (ঘ) Random
   **উত্তরঃ খ**
৩. Memory Phase-এ ভ্যারিয়েবল ডিক্লেয়ারেশনের ডিফল্ট ভ্যালু কী থাকে?
   (ক) null (খ) undefined (গ) 0 (ঘ) empty string
   **উত্তরঃ খ**
৪. Call Stack পূর্ণ হয়ে গেলে কোন এররটি দেখায়?
   (ক) ReferenceError (খ) TypeError (গ) Stack Overflow (ঘ) SyntaxError
   **উত্তরঃ গ**
৫. নিচের কোনটি Execution Context-এর অংশ নয়?
   (ক) Variable Object (খ) Scope Chain (গ) Event Loop (ঘ) "this" value
   **উত্তরঃ গ**

### ১৭. 5 Coding exercises
১. একটি রিকার্সিভ ফাংশন লিখুন যা ১ থেকে N পর্যন্ত যোগ করবে এবং Call Stack ট্রেস করুন।
২. এমন একটি কোড লিখুন যা ইচ্ছাকৃতভাবে `RangeError: Maximum call stack size exceeded` তৈরি করবে।
৩. ৩টি নেস্টেড ফাংশন কল করে তাদের কল অর্ডার কনসোলে প্রিন্ট করুন।
৪. `console.trace()` ব্যবহার করে কল স্ট্যাকের বর্তমান অবস্থা প্রিন্ট করার একটি প্রোগ্রাম লিখুন।
৫. নিচের কোডটির আউটপুট কী হবে তা ব্যাখ্যা করুন:
```javascript
function a() { b(); }
function b() { c(); }
function c() { console.log("Done"); }
a();
```

---

## ২. Hoisting in JavaScript (variables & functions)

### ১. Simple definition (বাংলায়)
**Hoisting** হলো JavaScript-এর এমন একটি ডিফল্ট আচরণ, যার ফলে কোড রান করার আগেই ভ্যারিয়েবল এবং ফাংশন ডিক্লেয়ারেশনগুলো তাদের নিজস্ব স্কোপের সবার ওপরে চলে যায় বলে মনে হয়। এর ফলে কোনো ভ্যারিয়েবল বা ফাংশন ডিক্লেয়ার করার আগেই কোডে সেটিকে অ্যাক্সেস করা সম্ভব হয় (যদিও ভ্যালু অ্যাসাইনমেন্ট হোইস্ট হয় না)।

### ২. Why this concept exists
JavaScript ইঞ্জিন কোড রান করার আগে পুরো ফাইলটি একবার স্ক্যান করে মেমোরি অ্যালোকেশন ফেজ (Memory Allocation Phase) সম্পন্ন করে। এই স্ক্যানিংয়ের ফলেই হোইস্টিংয়ের সৃষ্টি হয়।

### ৩. What problem it solves
এটি ডেভেলপারদের ফাংশন ডিক্লেয়ার করার আগেই তা কল করার সুবিধা দেয়। এর ফলে কোড স্ট্রাকচার সাজানো সহজ হয়; যেমন ফাইল এর শুরুতে মূল লজিক রেখে নিচে সহায়ক ফাংশনগুলো ডিক্লেয়ার করা যায়।

### ৪. Real-life analogy
মনে করুন ক্লাসের শিক্ষক ক্লাসে ঢোকার আগেই সব ছাত্রের নাম রোল শিটে লিখে রেখেছেন (মেমোরি ফেজ)। শিক্ষক ক্লাসে ঢুকে সরাসরি কোনো ছাত্রকে ডাকলে (কোড রান) ছাত্রটি উত্তর দিতে পারে, কারণ শিক্ষক তার নাম আগেই জানেন, যদিও ছাত্রটি এখনো নিজের পরিচয় বা অ্যাসাইনমেন্ট ক্লাসে জমা দেয়নি (undefined)।

### ৫. How React works internally regarding this concept
React-এ আমরা যখন ফাংশনাল কম্পোনেন্ট লিখি, তখন যদি সাধারণ `function ComponentName() {}` সিনট্যাক্স ব্যবহার করি, তবে হোইস্টিংয়ের কারণে আমরা ফাইলটির যেকোনো স্থানে তা ব্যবহার করতে পারি। কিন্তু যদি আমরা অ্যারো ফাংশন (`const Component = () => {}`) ব্যবহার করি, তবে হোইস্টিং কাজ করে না (Temporal Dead Zone-এর কারণে)। তাই React-এ কম্পোনেন্ট ডিক্লেয়ারেশন অর্ডারিংয়ে হোইস্টিং একটি গুরুত্বপূর্ণ ভূমিকা পালন করে।

### ৬. Basic example
```javascript
console.log(myVar); // undefined
greet(); // "Hello Developers!"

var myVar = 10;
function greet() {
  console.log("Hello Developers!");
}
```

### ৭. Step-by-step explanation of the code
১. মেমোরি ক্রিয়েশন ফেজে, `myVar`-কে মেমোরিতে জায়গা দেওয়া হয় এবং `undefined` দিয়ে ইনিশিয়ালাইজ করা হয়।
২. `greet` ফাংশনের পুরো বডিটি মেমোরিতে সংরক্ষিত হয়।
৩. কোড এক্সিকিউশন ফেজে যখন `console.log(myVar)` রান হয়, তখন স্ক্রিনে `undefined` দেখায় কারণ এখনো `myVar = 10` অ্যাসাইনমেন্ট লাইনে কোড পৌঁছায়নি।
৪. `greet()` কল করার সময় এটি সফলভাবে রান করে কারণ মেমোরিতে পুরো ফাংশনটি আগেই ছিল।

### ৮. Another real-world example
```javascript
// React Utility Function Helper
const total = calculateTotal(100); 
console.log(total); // Error or success? Let's check hoisting.

function calculateTotal(price) {
  return price + (price * 0.1);
}
```
যেহেতু এটি একটি ট্র্যাডিশনাল ফাংশন ডিক্লেয়ারেশন, তাই এটি সফলভাবে রান করবে। কিন্তু এটি যদি `const calculateTotal = (price) => ...` হতো, তবে `ReferenceError` দিত।

### ৯. Common mistakes beginners make
* অ্যারো ফাংশন বা ফাংশন এক্সপ্রেশনকে সাধারণ ফাংশনের মতো হোইস্ট হবে ভেবে ডিক্লেয়ার করার আগে কল করা।
* `var` হোইস্ট হয়ে `undefined` দেয় কিন্তু `let` ও `const` দেয় `ReferenceError`—এটি গুলিয়ে ফেলা।

### ১০. Interview questions related to this topic
* **Q:** ফাংশন ডিক্লেয়ারেশন এবং ফাংশন এক্সপ্রেশনের হোইস্টিংয়ের মধ্যে পার্থক্য কী?
  * **Ans:** ফাংশন ডিক্লেয়ারেশন সম্পূর্ণ হোইস্ট হয় (বডিসহ), কিন্তু ফাংশন এক্সপ্রেশন (যেমন `var foo = function() {}`) এর ক্ষেত্রে শুধু ভ্যারিয়েবলটি হোইস্ট হয়, ফলে কল করলে `TypeError: foo is not a function` আসে।
* **Q:** `let` এবং `const` কি হোইস্ট হয়?
  * **Ans:** হ্যাঁ, তারাও হোইস্ট হয় কিন্তু তারা Temporal Dead Zone (TDZ)-এ থাকে বিধায় ইনিশিয়ালাইজেশনের আগে অ্যাক্সেস করতে গেলে `ReferenceError` দেয়।

### ১১. Best practices
* সবসময় ভ্যারিয়েবল এবং ফাংশন ব্যবহারের আগে ডিক্লেয়ার করুন।
* `var`-এর পরিবর্তে `let` এবং `const` ব্যবহার করুন।

### ১২. Performance considerations
* হোইস্টিং জাভাস্ক্রিপ্ট ইঞ্জিনের ডিফল্ট বিহেভিয়ার হওয়ায় এতে বাড়তি কোনো পারফরম্যান্স ওভারহেড নেই।

### ১৩. When NOT to use it
* ভ্যারিয়েবল হোইস্টিংয়ের ওপর নির্ভর করে কোড লেখা একটি খারাপ অভ্যাস। হোইস্টিং এড়াতে সবসময় `const` এবং `let` ব্যবহার করা উচিত।

### ১৪. Comparison with similar concepts
| ডিক্লেয়ারেশন | হোইস্টিং আচরণ | ইনিশিয়াল ভ্যালু |
| :--- | :--- | :--- |
| `var` | হোইস্ট হয় | `undefined` |
| `let` / `const` | হোইস্ট হয় কিন্তু অ্যাক্সেস ব্লকড (TDZ) | কোনো ভ্যালু থাকে না (Error) |
| `function` | সম্পূর্ণ হোইস্ট হয় | ফাংশন বডি |

### ১৫. Summary in simple Bangla
হোইস্টিং হলো জাভাস্ক্রিপ্টের কোড রান করার আগে সবকিছু গুছিয়ে রাখার কাজ, যেখানে শিক্ষক আগেই রোল কল বুক রেডি করে রাখেন, ফলে নাম সবার উপরে চলে যায়।

### ১৬. 5 MCQ questions
১. নিচের কোডটির আউটপুট কী হবে?
```javascript
console.log(x);
var x = 5;
```
   (ক) 5 (খ) ReferenceError (গ) undefined (ঘ) null
   **উত্তরঃ গ**
২. ফাংশন এক্সপ্রেশনের ক্ষেত্রে হোইস্টিং-এ কী ঘটে?
   (ক) সম্পূর্ণ ফাংশন হোইস্ট হয় (খ) শুধুমাত্র ভ্যারিয়েবল ডিক্লেয়ারেশন হোইস্ট হয় (গ) কোনো হোইস্টিং হয় না (ঘ) এরর দেয়
   **উত্তরঃ খ**
৩. `let` এবং `const` হোইস্ট হওয়ার পর কোন জোনে থাকে?
   (ক) Global Zone (খ) Temporal Dead Zone (গ) Shadow Zone (ঘ) Lexical Zone
   **উত্তরঃ খ**
৪. নিচের কোডটির রেজাল্ট কী?
```javascript
foo();
var foo = () => console.log("Hi");
```
   (ক) "Hi" (খ) TypeError: foo is not a function (গ) ReferenceError (ঘ) undefined
   **উত্তরঃ খ**
৫. জাভাস্ক্রিপ্টের কোন ফেজের কারণে হোইস্টিং ঘটে?
   (ক) Execution phase (খ) Compilation/Memory phase (গ) Garbage collection phase (ঘ) Parsing phase
   **উত্তরঃ খ**

### ১৭. 5 Coding exercises
১. `var`, `let`, এবং `const` ব্যবহার করে একই নামের তিনটি ভ্যারিয়েবলকে ডিক্লেয়ার করার আগে অ্যাক্সেস করার চেষ্টা করুন এবং কনসোলের এররগুলো পর্যবেক্ষণ করুন।
২. একটি সাধারণ ফাংশন এবং একটি অ্যারো ফাংশন ডিক্লেয়ার করার আগে কল করে তাদের হোইস্টিং আচরণ দেখান।
৩. নিচের কোডটির আউটপুট কী হবে এবং কেন?
```javascript
var a = 1;
function b() {
  a = 10;
  return;
  function a() {}
}
b();
console.log(a);
```
৪. হোইস্টিংয়ের কারণে ঘটা একটি বাগ তৈরি করুন এবং তা `const` ব্যবহার করে ঠিক করুন।
৫. ফাংশন হোইস্টিং ব্যবহার করে এমন একটি মডিউল লিখুন যেখানে সহায়ক ফাংশনগুলো ফাইলের একদম শেষে থাকবে।

---

## ৩. Functions and Variable Environments

### ১. Simple definition (বাংলায়)
জাভাস্ক্রিপ্ট ইঞ্জিনের প্রতিটি Execution Context-এর ভেতরে যেখানে ভ্যারিয়েবল এবং ফাংশনগুলো জমা থাকে, তাকে **Variable Environment** বলে। প্রতিটি ফাংশন কল হওয়ার পর তার নিজস্ব স্কোপের জন্য যে আলাদা মেমোরি বা পরিবেশ তৈরি করে, তা-ই হলো সেই ফাংশনের Variable Environment।

### ২. Why this concept exists
একই নামের ভ্যারিয়েবল বিভিন্ন ফাংশনের ভেতরে আলাদা আলাদা ভ্যালু ধারণ করতে পারে। জাভাস্ক্রিপ্ট যাতে তাদের একে অপরের সাথে গুলিয়ে না ফেলে এবং কোন ফাংশন তার নিজস্ব কোন ভ্যালু অ্যাক্সেস করবে তা ট্র্যাক করতে পারে, সেজন্যই এটি প্রয়োজন।

### ৩. What problem it solves
এটি ভ্যারিয়েবলের ওভারল্যাপিং বা সংঘর্ষ (Collision) রোধ করে। এটি নিশ্চিত করে যে ফাংশন `A` এর ভেতর ডিক্লেয়ার করা ভ্যারিয়েবল `x` যেন ফাংশন `B` এর `x` কে ওভাররাইট না করে।

### ৪. Real-life analogy
মনে করুন একটি অফিসের আলাদা আলাদা কেবিন রয়েছে। প্রতিটি কেবিনে একজন করে ম্যানেজার আছেন এবং সবার ডেস্কেই একটি করে কলমদানি আছে। কেবিন ২ এর ম্যানেজার তার কলমদানি থেকে কলম নিচ্ছেন, যা কেবিন ১ এর ম্যানেজারের কলমদানির সাথে কোনোভাবেই সম্পর্কিত নয়। এখানে প্রতিটি কেবিন হলো একটি ফাংশন এবং তার টেবিল বা কলমদানি হলো Variable Environment।

### ৫. How React works internally regarding this concept
React-এর রেন্ডারিং সিস্টেমে প্রতিটি ফাংশনাল কম্পোনেন্ট রান করার সময় তার নিজস্ব Variable Environment তৈরি হয়। আপনি যখন `useState` ব্যবহার করেন, তখন প্রতি রেন্ডারে সেই স্টেটের একটি লোকাল স্ন্যাপশট (Local Snapshot) তৈরি হয়। এই কারণেই কোনো ইভেন্ট হ্যান্ডলার বা `setTimeout`-এ স্টেটের ভ্যালু ওই নির্দিষ্ট রেন্ডারের ভ্যালুকেই নির্দেশ করে, পরের কোনো রেন্ডারের ভ্যালুকে নয়।

### ৬. Basic example
```javascript
var x = 1;
a();
b();
console.log(x);

function a() {
  var x = 10;
  console.log(x);
}

function b() {
  var x = 100;
  console.log(x);
}
```

### ७. Step-by-step explanation of the code
১. Global Execution Context-এর Variable Environment-এ `x` এর ভ্যালু সেট হয় `1`।
২. `a()` ফাংশন কল হলে তার নিজস্ব Execution Context তৈরি হয় এবং তার Variable Environment-এ `x` সেট হয় `10`। এটি কনসোলে `10` প্রিন্ট করে পপ হয়ে যায়।
৩. `b()` ফাংশন কল হলে তার Variable Environment-এ `x` সেট হয় `100`। এটি কনসোলে `100` প্রিন্ট করে পপ হয়ে যায়।
৪. সর্বশেষে গ্লোবাল স্কোপের `console.log(x)` রান হয়ে `1` প্রিন্ট করে, কারণ গ্লোবাল Variable Environment অপরিবর্তিত ছিল।

### ৮. Another real-world example
```javascript
// React state simulation
function renderComponent(props) {
  var localState = props.value;
  console.log("Rendered with state: " + localState);
}

renderComponent({ value: "A" });
renderComponent({ value: "B" });
```
প্রতিটি `renderComponent` কল আলাদা Variable Environment তৈরি করে, যার ফলে একটি রেন্ডারের স্টেট অন্য রেন্ডারকে প্রভাবিত করে না।

### ৯. Common mistakes beginners make
* মনে করা যে গ্লোবাল ভ্যারিয়েবলকে লোকাল ফাংশনের ভেতরে রি-ডিক্লেয়ার করলে গ্লোবাল ভ্যালুটি বদলে যাবে (যখন `var` বা `let` ব্যবহার করা হয়)।

### ১০. Interview questions related to this topic
* **Q:** Variable Environment এবং Lexical Environment-এর মধ্যে মূল পার্থক্য কী?
  * **Ans:** Variable Environment মূলত বর্তমান স্কোপের ভ্যারিয়েবলগুলো ধারণ করে, আর Lexical Environment-এ বর্তমান ভ্যারিয়েবলগুলোর পাশাপাশি প্যারেন্ট স্কোপের রেফারেন্সও থাকে।

### ১১. Best practices
* লোকাল স্কোপের ভ্যারিয়েবলগুলোর নাম এমনভাবে দিন যেন তা গ্লোবাল বা আউটার স্কোপের নামের সাথে কনফ্লিক্ট না করে (Shadowing এড়াতে)।

### ১২. Performance considerations
* ফাংশন এক্সিকিউশন শেষ হওয়ার সাথে সাথে তার Variable Environment গার্বেজ কালেক্টর (Garbage Collector) দ্বারা মেমোরি থেকে মুছে যায়, যদি না কোনো Closure তৈরি হয়।

### ১৩. When NOT to use it
* এটি ইঞ্জিনের অটোমেটিক বিষয়, তবে বড় ভ্যারিয়েবল ডিক্লেয়ার করার পর ফাংশন খুব বেশি সময় ধরে রাখলে মেমোরি ধরে রাখে, যা এড়ানো উচিত।

### ১৪. Comparison with similar concepts
| এনভায়রনমেন্ট | কাজ | স্কোপ |
| :--- | :--- | :--- |
| **Variable Environment** | শুধুমাত্র লোকাল ভ্যারিয়েবল ট্র্যাক করে | লোকাল/ফাংশন স্কোপ |
| **Lexical Environment** | লোকাল ভ্যারিয়েবল + প্যারেন্ট রেফারেন্স | চেইন স্কোপ |

### ১৫. Summary in simple Bangla
Variable Environment হলো প্রতিটি ফাংশনের নিজস্ব প্রাইভেট ড্রয়ার, যেখানে সে তার নিজের প্রয়োজনীয় ফাইলপত্র (Variables) রাখে, যা অন্য কারো ড্রয়ারের ফাইলের সাথে মেশে না।

### ১৬. 5 MCQ questions
১. Variable Environment কার অংশ হিসেবে তৈরি হয়?
   (ক) Global Object (খ) Execution Context (গ) Event Loop (ঘ) Prototype Chain
   **উত্তরঃ খ**
২. ফাংশন এক্সিকিউশন শেষ হওয়ার পর তার Variable Environment-এর কী ঘটে?
   (ক) এটি চিরকাল থেকে যায় (খ) এটি গার্বেজ কালেক্টেড হয়ে যায় (গ) এটি গ্লোবাল অবজেক্টে যোগ হয় (ঘ) রি-লোকেট হয়
   **উত্তরঃ খ**
৩. যদি কোনো ভ্যারিয়েবল লোকাল Variable Environment-এ না পাওয়া যায়, তবে জাভাস্ক্রিপ্ট কোথায় খোঁজে?
   (ক) আউটার Lexical Environment-এ (খ) কোথাও খোঁজে না, এরর দেয় (গ) Call Stack-এ (ঘ) Heap Memory-তে
   **উত্তরঃ ক**
৪. নিচের কোডের আউটপুট কী?
```javascript
var y = 5;
function check() {
  var y = 10;
}
check();
console.log(y);
```
   (ক) 10 (খ) undefined (গ) 5 (ঘ) ReferenceError
   **উত্তরঃ গ**
৫. Variable Environment-এ ভ্যারিয়েবল স্টোরেজ মেকানিজম কী নামে পরিচিত?
   (ক) Context Store (খ) Variable Object (গ) Lexical Stack (ঘ) Call Registry
   **উত্তরঃ খ**

### ১৭. 5 Coding exercises
১. দুটি ফাংশন তৈরি করুন যেখানে একই নামের লোকাল ভ্যারিয়েবল থাকবে এবং দেখান কীভাবে তারা একে অপরকে প্রভাবিত করে না।
২. একটি ফাংশনের ভেতর অন্য ফাংশন নেস্ট করুন এবং দুটিতে একই নামের ভ্যারিয়েবল ডিক্লেয়ার করে মান পরিবর্তন করে দেখান।
৩. `console.dir()` ব্যবহার করে একটি ফাংশনের ইন্টারনাল স্কোপ চেক করার চেষ্টা করুন (ব্রাউজার কনসোলে)।
৪. গ্লোবাল এবং লোকাল এনভায়রনমেন্টের ভ্যারিয়েবল ক্লাশিং এড়াতে একটি IIFE (Immediately Invoked Function Expression) তৈরি করুন।
৫. এমন একটি কোড লিখুন যা দেখাবে কীভাবে আউটার এনভায়রনমেন্টের ভ্যালু ইনার এনভায়রনমেন্ট থেকে পরিবর্তন করা যায় (যদি `var`/`let` রি-ডিক্লেয়ার না করা হয়)।

---

## ৪. Shortest JS Program

### ১. Simple definition (বাংলায়)
জাভাস্ক্রিপ্টের সবচেয়ে ছোট প্রোগ্রাম হলো একটি **খালি ফাইল (Empty File)**। জাভাস্ক্রিপ্ট ফাইলে কোনো কোড না লিখলেও ব্যাকগ্রাউন্ডে জাভাস্ক্রিপ্ট ইঞ্জিন কিছু গ্লোবাল জিনিসপত্র তৈরি করে প্রোগ্রামটি রান করে।

### ২. Why this concept exists
জাভাস্ক্রিপ্ট ইঞ্জিনকে যেকোনো কোড রান করার জন্য ন্যূনতম একটি পরিবেশ বা ফ্রেমওয়ার্ক তৈরি করতে হয়। ফাইল খালি হলেও ইঞ্জিনকে কাজ শুরু করার জন্য একটি মূল অ্যাক্সেস পয়েন্ট তৈরি করতে হয়।

### ৩. What problem it solves
এটি নিশ্চিত করে যে জাভাস্ক্রিপ্ট রানটাইম সবসময়ই একটি স্ট্যান্ডার্ড গ্লোবাল স্পেস পাবে, যেখানে ডেভেলপাররা যেকোনো সময় যেকোনো স্থান থেকে গ্লোবাল ফিচারসমূহ ব্যবহার করতে পারেন।

### ৪. Real-life analogy
চিন্তা করুন আপনি একটি নতুন কেনা ফ্ল্যাটে প্রবেশ করলেন যেখানে কোনো আসবাবপত্র নেই। ফ্ল্যাটটি খালি হলেও তার একটি মেঝে, ছাদ এবং দেয়াল রয়েছে। এই খালি ফ্ল্যাটটিই হলো Shortest JS Program, যেখানে মেঝে-ছাদ হলো গ্লোবাল অবজেক্ট।

### ৫. How React works internally regarding this concept
React অ্যাপ্লিকেশন যখন ব্রাউজারে লোড হয়, তখন স্ক্রিপ্ট ট্যাগগুলোর মাধ্যমে বান্ডেলড কোড রান করার আগে ব্রাউজার ইঞ্জিন তার গ্লোবাল উইন্ডো অবজেক্ট এবং গ্লোবাল এক্সিকিউশন কন্টেক্সট সেটআপ করে নেয়। React-এর নিজস্ব রুট অবজেক্ট (যেমন `window.__REACT_DEVTOOLS_GLOBAL_HOOK__`) এই গ্লোবাল এনভায়রনমেন্টের ওপরেই মাউন্ট হয়।

### ৬. Basic example
একটি সম্পূর্ণ খালি ফাইল তৈরি করুন (যেমন: `app.js` যাতে কোনো লাইন নেই) এবং ব্রাউজারের কনসোলে গিয়ে টাইপ করুন:
```javascript
window;
this;
window === this; // true
```

### ৭. Step-by-step explanation of the code
১. খালি ফাইলটি যখন ব্রাউজারে রান হয়, জাভাস্ক্রিপ্ট ইঞ্জিন একটি **Global Execution Context** তৈরি করে।
২. এটি একটি **Global Object** তৈরি করে, যা ব্রাউজারের ক্ষেত্রে `window` অবজেক্ট।
৩. এটি একটি `this` ভ্যারিয়েবল তৈরি করে এবং তা গ্লোবাল অবজেক্টের দিকে পয়েন্ট করায়।
৪. তাই কোনো কোড না থাকলেও `window === this` এর মান `true` আসে।

### ৮. Another real-world example
```javascript
// browser console.js
console.log(this); // Window { ... }
```
এখানেও কোনো ইউজার ডিক্লেয়ারড কোড ছাড়াই `this` অ্যাক্সেস করা যাচ্ছে।

### ৯. Common mistakes beginners make
* মনে করা যে খালি ফাইলে জাভাস্ক্রিপ্ট ইঞ্জিন কিছুই করে না এবং কোনো মেমোরি খরচ হয় না।

### ১০. Interview questions related to this topic
* **Q:** জাভাস্ক্রিপ্টের সবচেয়ে ছোট ফাইল রান করলে ইঞ্জিন ব্যাকগ্রাউন্ডে কী তৈরি করে?
  * **Ans:** Global Execution Context, Global Object (window), and `this` variables.
* **Q:** ব্রাউজার ছাড়া অন্য রানটাইমে (যেমন Node.js) গ্লোবাল অবজেক্টের নাম কী?
  * **Ans:** Node.js-এ গ্লোবাল অবজেক্টের নাম হলো `global` (আধুনিক জেএস-এ দুটোর জন্যই `globalThis` ব্যবহার করা যায়)।

### ১১. Best practices
* গ্লোবাল স্কোপকে সবসময় পরিষ্কার রাখুন (Avoid polluting the global scope)। গ্লোবাল উইন্ডো অবজেক্টে সরাসরি কোনো ভ্যারিয়েবল অ্যাড করা এড়িয়ে চলুন।

### ১২. Performance considerations
* গ্লোবাল অবজেক্টে অতিরিক্ত ডেটা স্টোর করলে তা মেমোরিতে থেকে যায় এবং গার্বেজ কালেকশন হয় না, যা মেমোরি লিকের কারণ হতে পারে।

### ১৩. When NOT to use it
* এটি একটি থিওরেটিক্যাল কনসেপ্ট, বাস্তব প্রোডাকশনে আমরা কখনো খালি ফাইল রাখি না।

### ১৪. Comparison with similar concepts
| রানটাইম পরিবেশ | গ্লোবাল অবজেক্টের নাম |
| :--- | :--- |
| **Browser** | `window` |
| **Node.js** | `global` |
| **Universal (ES2020)** | `globalThis` |

### ১৫. Summary in simple Bangla
জাভাস্ক্রিপ্ট ফাইল সম্পূর্ণ খালি থাকলেও জাভাস্ক্রিপ্ট ইঞ্জিন নিজে থেকে একটি ঘর (Global Context) এবং ঘরের মালিককে (window/this) তৈরি করে রাখে।

### ১৬. 5 MCQ questions
১. ব্রাউজারে গ্লোবাল স্কোপে `this` নিচের কোনটিকে নির্দেশ করে?
   (ক) document (খ) window (গ) global (ঘ) null
   **উত্তরঃ খ**
২. জাভাস্ক্রিপ্টের খালি ফাইলে নিচের কোনটি থাকে না?
   (ক) Global EC (খ) Global Object (গ) Local EC (ঘ) "this" keyword
   **উত্তরঃ গ**
৩. Node.js-এ গ্লোবাল অবজেক্টের নাম কী?
   (ক) window (খ) local (গ) global (ঘ) process
   **উত্তরঃ গ**
৪. `window === this` ব্রাউজারের গ্লোবাল স্কোপে কী রিটার্ন করবে?
   (ক) false (খ) true (গ) undefined (ঘ) TypeError
   **উত্তরঃ খ**
৫. কোন ES সংস্করণে সব প্লাটফর্মের জন্য `globalThis` আনা হয়েছে?
   (ক) ES5 (খ) ES6 (গ) ES2020 (ঘ) ES2022
   **উত্তরঃ গ**

### ১৭. 5 Coding exercises
১. একটি খালি ফাইল রান করে ব্রাউজার কনসোলে `globalThis` প্রিন্ট করে দেখুন।
২. Node.js এবং ব্রাউজার—উভয় জায়গায় কাজ করবে এমন একটি স্ক্রিপ্ট লিখুন যা গ্লোবাল অবজেক্ট প্রিন্ট করবে।
৩. উইন্ডো অবজেক্টে নিজের নাম অ্যাড করুন এবং অন্য একটি ফাইলে তা অ্যাক্সেস করে দেখান।
৪. `this` কিওয়ার্ডটি গ্লোবাল স্কোপে কী কী প্রপার্টি হোল্ড করে তা লুপ চালিয়ে কনসোলে প্রিন্ট করুন।
৫. গ্লোবাল স্কোপ পল্যুশন রোধ করতে কীভাবে কোড লিখবেন তার একটি ডেমো তৈরি করুন (যেমন: মডিউল বা IIFE ব্যবহার)।

---

## ৫. window & this keyword

### ১. Simple definition (বাংলায়)
**window** হলো ব্রাউজার এনভায়রনমেন্টে জাভাস্ক্রিপ্টের গ্লোবাল অবজেক্ট (Global Object)। আর **this** হলো একটি বিশেষ কিওয়ার্ড যা নির্দেশ করে বর্তমান কোডটি কোন অবজেক্টের আন্ডারে বা কোন কনটেক্সটে এক্সিকিউট হচ্ছে। গ্লোবাল লেভেলে `this` এবং `window` একই অবজেক্টকে নির্দেশ করে।

### ২. Why this concept exists
জাভাস্ক্রিপ্ট কোড যেখানেই রান হোক না কেন (ব্রাউজার, সার্ভার), ব্রাউজারের উইন্ডো কন্ট্রোল করা, লোকাল স্টোরেজ ব্যবহার করা, রিডাইরেক্ট করা ইত্যাদি কাজের জন্য একটি গ্লোবাল গেটওয়ে প্রয়োজন। আর অবজেক্ট ওরিয়েন্টেড প্রোগ্রামিং ও কনটেক্সট ট্র্যাকিংয়ের জন্য `this` এর প্রয়োজন।

### ৩. What problem it solves
এটি ব্রাউজারের বিল্ট-ইন এপিআই (যেমন: `window.location`, `window.localStorage`) অ্যাক্সেস করার একীভূত সমাধান দেয়। এছাড়াও রানটাইমে ডাইনামিক কনটেক্সট ট্র্যাকিং সহজ করে।

### ৪. Real-life analogy
উইন্ডো অবজেক্টটি হলো একটি বড় শপিং মল (গ্লোবাল স্পেস), আর `this` হলো আপনার হাতের তর্জনী আঙুল। আপনি যখন মলের ভেতরে দাঁড়িয়ে কোনো জিনিসকে নির্দেশ করছেন, তখন `this` বলতে মলকে বোঝায়। কিন্তু আপনি যখন কোনো নির্দিষ্ট দোকানে গিয়ে কোনো প্রোডাক্ট দেখাচ্ছেন, তখন `this` বলতে ওই দোকান বা প্রোডাক্টকে বোঝায়।

### ৫. How React works internally regarding this concept
React অ্যাপ্লিকেশনে (বিশেষ করে Server-Side Rendering বা SSR যেমন Next.js-এ) `window` অবজেক্ট সরাসরি অ্যাক্সেস করা যায় না, কারণ সার্ভার সাইডে উইন্ডো অবজেক্ট থাকে না। তাই React ডেভেলপারদের `useEffect` এর ভেতরে উইন্ডো অবজেক্ট অ্যাক্সেস করতে হয় (যা শুধুমাত্র ক্লায়েন্ট সাইডে রান হয়)। আর React-এর পুরোনো Class Component-এ ইভেন্ট হ্যান্ডলারগুলোর সাথে `this` বাইন্ডিং করা অত্যন্ত গুরুত্বপূর্ণ ছিল, নতুবা রানটাইমে `this` এর মান `undefined` হয়ে যেত।

### ৬. Basic example
```javascript
console.log(window.a); // undefined
var a = 10;
console.log(a); // 10
console.log(this.a); // 10
console.log(window.a); // 10
```

### ৭. Step-by-step explanation of the code
১. `var a = 10` লিখলে জাভাস্ক্রিপ্ট এটিকে উইন্ডো অবজেক্টের প্রপার্টি হিসেবে যুক্ত করে।
২. তাই গ্লোবাল স্কোপে থাকা অবস্থায় `a`, `this.a` এবং `window.a` একই মেমোরি লোকেশন ও ভ্যালু (১০) নির্দেশ করে।
৩. তবে `let` বা `const` দিয়ে ভ্যারিয়েবল ডিক্লেয়ার করলে তা উইন্ডো অবজেক্টে যুক্ত হয় না (যদিও তা গ্লোবাল স্কোপেই থাকে)।

### ৮. Another real-world example (SSR check in React)
```javascript
// Safe window usage in React
function getClientWidth() {
  if (typeof window !== 'undefined') {
    return window.innerWidth;
  }
  return 0; // fallback for server rendering
}
```
এটি Next.js বা React SSR-এ সার্ভার সাইড এরর এড়াতে বহুল ব্যবহৃত একটি কোড প্যাটার্ন।

### ৯. Common mistakes beginners make
* Next.js বা SSR-এ কম্পোনেন্টের বডির ভেতরে সরাসরি `window.something` লিখে ফেলা, যা বিল্ড এরর বা হাইড্রেশন এরর ঘটায়।
* `let` ও `const` দিয়ে ডিক্লেয়ার করা গ্লোবাল ভ্যারিয়েবলকে `window.varName` দিয়ে খোঁজা।

### ১০. Interview questions related to this topic
* **Q:** গ্লোবাল স্কোপে `var x = 5` এবং `let y = 10` লিখলে তাদের `window` অবজেক্টে পাওয়ার ক্ষেত্রে কী পার্থক্য হবে?
  * **Ans:** `window.x` এর মান `5` হবে, কিন্তু `window.y` এর মান `undefined` হবে কারণ `let`/`const` গ্লোবাল অবজেক্টে অ্যাটাচ হয় না।
* **Q:** React Class Component-এ কেন মেথডগুলোকে `bind(this)` করতে হতো?
  * **Ans:** কারণ JavaScript-এ মেথড যখন কোনো ইভেন্ট লিসেনার হিসেবে পাস করা হয়, তখন তার মূল কনটেক্সট হারিয়ে যায়। `bind` না করলে `this` এর মান `undefined` হয়ে যায় (strict mode-এর কারণে)।

### ১১. Best practices
* ক্লায়েন্ট-অনলি এপিআই ব্যবহারের সময় সবসময় `typeof window !== 'undefined'` চেক করে নিন।
* মডার্ন React-এ ফাংশনাল কম্পোনেন্ট ব্যবহার করুন, যা `this` নিয়ে মাথা ঘামাতে হয় না।

### ১২. Performance considerations
* উইন্ডো অবজেক্টে অতিরিক্ত মেমোরি ডেটা বা গ্লোবাল স্টেট স্টোর করবেন না, কারণ এটি অ্যাপ বন্ধ না হওয়া পর্যন্ত মেমোরি রিলিজ করে না।

### ১৩. When NOT to use it
* সার্ভার সাইড রেন্ডারিং (SSR) কোড বা নোড এনভায়রনমেন্টে উইন্ডো অবজেক্ট সরাসরি ব্যবহার করবেন না।

### ১৪. Comparison with similar concepts
| অবজেক্ট / কিওয়ার্ড | প্রকৃতি | আচরণ (গ্লোবাল) |
| :--- | :--- | :--- |
| `window` | গ্লোবাল অবজেক্ট | ব্রাউজারের উইন্ডো নির্দেশ করে |
| `this` | কনটেক্সট কিওয়ার্ড | ডিফল্টভাবে উইন্ডো অবজেক্টকে নির্দেশ করে |

### ১৫. Summary in simple Bangla
উইন্ডো হলো পুরো ব্রাউজারের ভার্চুয়াল রাজত্ব, আর `this` হলো একটি তীর যা নির্দেশ করে আমরা বর্তমানে কার প্রাসাদে (কনটেক্সটে) দাঁড়িয়ে আছি।

### ১৬. 5 MCQ questions
১. নিচের কোন ভ্যারিয়েবল উইন্ডো অবজেক্টের প্রপার্টি হিসেবে যুক্ত হবে না?
   (ক) `var a = 5;` (খ) `let b = 10;` (গ) `function greet() {}` (ঘ) কোনোটিই নয়
   **উত্তরঃ খ**
২. strict mode অন থাকলে সাধারণ ফাংশনের ভেতরে `this` এর মান কী হয়?
   (ক) window (খ) undefined (গ) null (ঘ) global
   **উত্তরঃ খ**
৩. Next.js-এ সার্ভার রেন্ডারিংয়ের সময় `typeof window` এর আউটপুট কী হবে?
   (ক) "object" (খ) "undefined" (গ) "window" (ঘ) Error
   **উত্তরঃ খ**
৪. অ্যারো ফাংশনের নিজস্ব `this` কেমন হয়?
   (ক) এর নিজস্ব কোনো `this` নেই, এটি প্যারেন্ট স্কোপ থেকে নেয় (খ) সবসময় window (গ) সবসময় undefined (ঘ) runtime এ ডিসাইড হয়
   **উত্তরঃ ক**
৫. গ্লোবাল উইন্ডো ডিক্লেয়ারেশন ছাড়াও জাভাস্ক্রিপ্টের ইউনিভার্সাল গ্লোবাল রেফারেন্স কোনটি?
   (ক) globalThis (খ) globalWindow (গ) windowObject (ঘ) universe
   **উত্তরঃ ক**

### ১৭. 5 Coding exercises
১. একটি কোড লিখুন যা `let`, `const`, এবং `var` ডিক্লেয়ার করবে এবং দেখাবে কোনটি উইন্ডোতে যুক্ত হচ্ছে।
２. React-এর মতো কন্ডিশনাল কোড লিখুন যা সার্ভার এবং ক্লায়েন্ট সনাক্ত করে যথাক্রমে বার্তা প্রিন্ট করবে।
৩. একটি অবজেক্ট তৈরি করুন যার একটি মেথড নিজের অবজেক্টের ডেটা প্রিন্ট করতে `this` ব্যবহার করবে।
৪. এমন একটি উদাহরণ তৈরি করুন যেখানে `this` হারানো বা `undefined` হওয়ার সমস্যা হবে এবং তা সমাধান করুন।
৫. `window.addEventListener` ব্যবহার করে স্ক্রল পজিশন ট্র্যাকিংয়ের একটি স্ক্রিপ্ট তৈরি করুন।

---

## ৬. undefined vs not defined in JS

### ১. Simple definition (বাংলায়)
**undefined** হলো জাভাস্ক্রিপ্টের একটি বিশেষ ডাটা টাইপ ও মান, যা নির্দেশ করে একটি ভ্যারিয়েবল ডিক্লেয়ার করা হয়েছে কিন্তু এখনো কোনো মান অ্যাসাইন করা হয়নি। আর **not defined** হলো একটি `ReferenceError` যা নির্দেশ করে ওই নামের কোনো ভ্যারিয়েবল জাভাস্ক্রিপ্ট মেমোরিতে ডিক্লেয়ার বা তৈরিই করা হয়নি।

### ২. Why this concept exists
জাভাস্ক্রিপ্ট একটি ডাইনামিকালি টাইপড ভাষা। কোড রান করার সময় কম্পাইলার যাতে বুঝতে পারে যে কোনো ভ্যারিয়েবল সিস্টেমে রেজিস্টার্ড আছে কি না এবং ডেভেলপার যাতে সহজে ডিবাগ করতে পারেন, সেজন্যই এই পার্থক্য করা হয়েছে।

### ৩. What problem it solves
এটি কোড ডিবাগিংকে অনেক সহজ করে তোলে। আপনি যদি কনসোলে `undefined` পান, আপনি বুঝবেন ভ্যারিয়েবল ডিক্লেয়ারেশন ঠিক আছে কিন্তু ভ্যালু দেওয়া হয়নি। আর `not defined` পেলে বুঝবেন ডিক্লেয়ারেশনেই ভুল আছে বা স্পেলিং মিসটেক হয়েছে।

### ৪. Real-life analogy
* **undefined:** আপনার নামে ব্যাংকে একটি খালি অ্যাকাউন্ট খোলা হয়েছে কিন্তু তাতে কোনো টাকা জমা দেওয়া হয়নি।
* **not defined:** ওই ব্যাংকে আপনার নামে কোনো অ্যাকাউন্টই খোলা হয়নি।

### ৫. How React works internally regarding this concept
React-এর প্রপস (Props) পাস করার সময় যদি কোনো প্রপ না পাঠানো হয়, তবে রিসিভিং কম্পোনেন্টে তার মান হয় `undefined`। আমরা প্রায়ই `undefined` চেক করে ডিভল্ট ভ্যালু সেট করি (যেমন `const { theme = 'light' } = props;`)। কিন্তু যদি এমন কোনো ভ্যারিয়েবল রেন্ডার করতে যাই যা ডিক্লেয়ারই করা হয়নি, তবে React অ্যাপ ক্র্যাশ করবে এবং `ReferenceError: x is not defined` উইন্ডো চলে আসবে।

### ৬. Basic example
```javascript
var a;
console.log(a); // undefined

// console.log(b); // ReferenceError: b is not defined
```

### ৭. Step-by-step explanation of the code
১. মেমোরি ফেজে `var a` এর জন্য জায়গা রাখা হয় এবং ডিফল্ট মান `undefined` দেওয়া হয়।
২. কিন্তু `b` এর কোনো উপস্থিতি ফাইলে নেই, তাই এর জন্য কোনো মেমোরি রাখা হয়নি।
৩. কোড রান টাইমে `console.log(a)` মেমোরি থেকে `undefined` নিয়ে প্রিন্ট করে।
৪. কিন্তু `console.log(b)` লাইনে এসে ইঞ্জিন দেখে মেমোরিতে `b` নামে কিছুই নেই, তাই সে রানটাইম থামিয়ে দিয়ে `ReferenceError` ছুড়ে মারে।

### ৮. Another real-world example (React API Response check)
```javascript
// React API Handling
const [user, setUser] = React.useState(undefined); 

// user is undefined initially (valid state)
if (user === undefined) {
  console.log("Loading user...");
}
```
এখানে ইনিশিয়াল ভ্যালু `undefined` দেওয়া একটি ভ্যালিড ডিক্লেয়ারেশন। কিন্তু যদি আমরা ভুল করে `console.log(userData)` লিখি (যেখানে `userData` ডিক্লেয়ার্ড নেই), তবে অ্যাপ ক্র্যাশ করবে।

### ৯. Common mistakes beginners make
* নিজের কোডে ইচ্ছাকৃতভাবে কোনো ভ্যারিয়েবলের মান খালি করতে `undefined` অ্যাসাইন করা (উচিত হলো `null` অ্যাসাইন করা)।
* স্পেলিং ভুলের কারণে `not defined` এরর খাওয়া এবং বুঝতে না পারা কেন এটি হচ্ছে।

### ১০. Interview questions related to this topic
* **Q:** `undefined` এবং `null`-এর মধ্যে মূল পার্থক্য কী?
  * **Ans:** `undefined` মানে ভ্যারিয়েবল আছে কিন্তু ভ্যালু নেই (সিস্টেম অ্যাসাইন্ড)। আর `null` হলো একটি অ্যাসাইন্ড ভ্যালু যা নির্দেশ করে ভ্যারিয়েবলটি ফাঁকা বা খালি (ইউজার অ্যাসাইন্ড)।
* **Q:** `typeof undefined` এবং `typeof null` এর মান কী?
  * **Ans:** `typeof undefined` হলো `"undefined"`, কিন্তু `typeof null` হলো `"object"` (এটি জাভাস্ক্রিপ্টের একটি ঐতিহাসিক বাগ)।

### ১১. Best practices
* কোনো ভ্যারিয়েবলকে খালি নির্দেশ করতে কখনোই `undefined` অ্যাসাইন করবেন না, সবসময় `null` ব্যবহার করুন।
* ভ্যারিয়েবল চেক করার জন্য ট্রিপল ইকুয়াল (`===`) ব্যবহার করুন।

### ১২. Performance considerations
* জাভাস্ক্রিপ্ট ইঞ্জিনের ইন্টারনাল অপ্টিমাইজেশনের জন্য মেমোরিতে সরাসরি `undefined` অ্যাসাইন করা এড়িয়ে চললে ডিবাগিং ও মেমোরি ক্ল্যারিটি ভালো থাকে।

### ১৩. When NOT to use it
* ভ্যারিয়েবলকে রি-সেট বা এম্পটি করার জন্য `undefined` ব্যবহার করবেন না।

### ১৪. Comparison with similar concepts
| টাইপ | ডাটা টাইপ | তাৎপর্য |
| :--- | :--- | :--- |
| `undefined` | Undefined | ডিক্লেয়ার্ড কিন্তু অ্যাসাইন করা হয়নি |
| `null` | Object | ইচ্ছাকৃতভাবে খালি রাখা হয়েছে |
| `not defined` | N/A (Error) | মেমোরিতে অস্তিত্বই নেই |

### ১৫. Summary in simple Bangla
`undefined` হলো নাম লেখা খালি খাতা, আর `not defined` হলো যার কোনো খাতাই নেই।

### ১৬. 5 MCQ questions
১. `typeof undefined` কী রিটার্ন করে?
   (ক) "null" (খ) "object" (গ) "undefined" (ঘ) "error"
   **উত্তরঃ গ**
২. জাভাস্ক্রিপ্টে `not defined` মূলত কী?
   (ক) একটি ডাটা টাইপ (খ) একটি রানটাইম এরর (গ) একটি ওয়ার্নিং (ঘ) একটি অবজেক্ট
   **উত্তরঃ খ**
৩. নিচের কোডটির আউটপুট কী হবে?
```javascript
var x;
if (x) {
  console.log("Yes");
} else {
  console.log("No");
}
```
   (ক) Yes (খ) No (গ) undefined (ঘ) Error
   **উত্তরঃ খ (যেহেতু undefined একটি falsy value)**
৪. `undefined == null` এবং `undefined === null` যথাক্রমে কী রিটার্ন করবে?
   (ক) true, true (খ) false, false (গ) true, false (ঘ) false, true
   **উত্তরঃ গ**
৫. কোনো ভ্যারিয়েবল আসলেই মেমোরিতে ডিক্লেয়ার করা হয়েছে কি না তা চেক করার নিরাপদ উপায় কী?
   (ক) `x === undefined` (খ) `typeof x !== 'undefined'` (গ) `x === null` (ঘ) সরাসরি `if(x)` চালানো
   **উত্তরঃ খ (যদি x ডিক্লেয়ার্ড না থাকে তবে প্রথমটি এরর দেবে, কিন্তু typeof নিরাপদ)**

### ১৭. 5 Coding exercises
১. একটি কোড লিখুন যা দেখাবে কীভাবে একটি ভ্যারিয়েবল `undefined` থেকে ডাইনামিকালি অন্য টাইপে পরিবর্তিত হয়।
২. `typeof` অপারেটর ব্যবহার করে একটি সেফ-গার্ড ফাংশন লিখুন যা কোনো ভ্যারিয়েবল ডিক্লেয়ার করা না থাকলে ক্র্যাশ না করে বার্তা দেবে।
৩. এমন একটি অবজেক্ট তৈরি করুন যার একটি প্রপার্টির মান `undefined` এবং অন্যটির মান `null` এবং তাদের টাইপ চেক করুন।
৪. `ReferenceError` ক্যাচ করার জন্য একটি `try-catch` ব্লক লিখুন।
৫. React-এর কন্ডিশনাল রেন্ডারিং সিমুলেট করুন যেখানে প্রপস `undefined` হলে একটি প্লেসহোল্ডার টেক্সট দেখাবে।

---

## ৭. The Scope Chain, Scope & Lexical Environment

### ১. Simple definition (বাংলায়)
**Scope** হলো কোডের সেই সীমানা বা এলাকা যেখানে কোনো ভ্যারিয়েবল বা ফাংশনকে অ্যাক্সেস করা যায়। **Lexical Environment** হলো একটি মেমোরি স্পেস যা তার নিজস্ব লোকাল ভ্যারিয়েবল এবং তার প্যারেন্ট (Parent) বা আউটার এনভায়রনমেন্টের রেফারেন্স ধরে রাখে। আর এই প্যারেন্ট রেফারেন্স ধরে ধরে সবার ওপরে (Global) যাওয়ার যে লিংক বা শিকল তৈরি হয়, তাকে **Scope Chain** বলে।

### ২. Why this concept exists
নেস্টেড ফাংশনগুলোর ক্ষেত্রে ভেতরের ফাংশন যাতে বাইরের ফাংশনের ভ্যারিয়েবল অ্যাক্সেস করতে পারে, তা নিশ্চিত করার জন্য এটি প্রয়োজন। কোডের স্কোপিং নিয়ম ঠিক রাখতে এবং মেমোরির অ্যাক্সেসিবিলিটি নিয়ন্ত্রণ করতে এটি ব্যবহৃত হয়।

### ৩. What problem it solves
এটি নিশ্চিত করে যে ইন্টারনাল লজিক বাইরের ভ্যারিয়েবল রিড করতে পারবে, কিন্তু বাইরের বা গ্লোবাল কোড ভেতরের সিকিউরড বা প্রাইভেট ভ্যারিয়েবল অ্যাক্সেস করতে পারবে না। এটি একমুখী ডেটা সুরক্ষার ব্যবস্থা করে।

### ৪. Real-life analogy
চিন্তা করুন একটি বহুতল অ্যাপার্টমেন্টের কথা। আপনি যদি ৩য় তলার ফ্ল্যাটে থাকেন, তবে আপনি আপনার ঘরের জিনিসপত্র তো ব্যবহার করতে পারবেনই, সাথে অ্যাপার্টমেন্টের কমন স্পেস (নিচতলার লবি/গ্লোবাল এরিয়া) ব্যবহার করতে পারবেন। কিন্তু বাইরের কোনো পথিক আপনার ৩য় তলার ঘরের ভেতরের জিনিস ব্যবহার করতে পারবে না। তলার পর তলা বেয়ে নিচে নামার এই পথটিই হলো Scope Chain।

### ৫. How React works internally regarding this concept
React-এর স্টেট ও ফাংশনাল লজিক সম্পূর্ণভাবে Scope Chain-এর ওপর ভিত্তি করে কাজ করে। আমরা যখন কোনো কম্পোনেন্টের ভেতরে আরেকটি ইভেন্ট হ্যান্ডলার বা কাস্টম ফাংশন লিখি, তখন সেই ফাংশনটি তার বাইরের প্যারেন্ট কম্পোনেন্টের স্টেট বা প্রপসকে সরাসরি অ্যাক্সেস করতে পারে Scope Chain-এর মাধ্যমে।
```jsx
function ParentComponent() {
  const [data, setData] = useState("Hello");

  function childHandler() {
    console.log(data); // Scope chain-এর মাধ্যমে data অ্যাক্সেস পায়
  }
}
```

### ৬. Basic example
```javascript
function a() {
  var b = 10;
  c();
  function c() {
    console.log(b); // Prints 10
  }
}

a();
```

### ৭. Step-by-step explanation of the code
১. `c()` ফাংশন যখন `b` ভ্যারিয়েবলটি প্রিন্ট করতে চায়, তখন সে প্রথমে নিজের লোকাল Lexical Environment-এ খোঁজে।
২. সেখানে না পেয়ে সে তার প্যারেন্ট Lexical Environment (যা হলো `a()` এর পরিবেশ)-এ খোঁজে।
৩. সেখানে `b = 10` পেয়ে যায় এবং স্ক্রিনে ১০ প্রিন্ট করে।
৪. যদি `a()` এর ভেতরেও না থাকত, তবে সে গ্লোবাল এনভায়রনমেন্টে খুঁজত। এই চেইনটিই হলো Scope Chain।

### ৮. Another real-world example (React API Config Helper)
```javascript
const API_URL = "https://api.example.com";

function UserService() {
  const token = "XYZ123";

  return {
    getUser: function(userId) {
      // Accesses both 'token' (parent scope) and 'API_URL' (global scope)
      return `${API_URL}/users/${userId}?token=${token}`;
    }
  };
}
```

### ৯. Common mistakes beginners make
* মনে করা যে প্যারেন্ট ফাংশন চাইল্ড ফাংশনের ভেতরের ভ্যারিয়েবল অ্যাক্সেস করতে পারবে। (স্কোপ চেইন শুধুমাত্র একমুখী—ভেতর থেকে বাইরে যাওয়া যায়, বাইরে থেকে ভেতরে আসা যায় না)।

### ১০. Interview questions related to this topic
* **Q:** Lexical Environment কী কী নিয়ে গঠিত?
  * **Ans:** Environment Record (লোকাল ভ্যারিয়েবলসমূহ) এবং Reference to the outer environment (প্যারেন্ট রেফারেন্স)।
* **Q:** Lexical Scope বলতে কী বোঝায়?
  * **Ans:** কোড লেখার সময় (Compile Time) ভ্যারিয়েবল বা ফাংশনটি কোথায় ডিফাইন করা হয়েছে তার ওপর ভিত্তি করে যে স্কোপ নির্ধারিত হয়, তাকে Lexical Scope বলে (এটি রানটাইমে ডাইনামিকালি পরিবর্তিত হয় না)।

### ১১. Best practices
* গ্লোবাল ভ্যারিয়েবল অ্যাক্সেস কমিয়ে লোকাল ভ্যারিয়েবল ব্যবহারে মনোযোগ দিন, যা স্কোপ চেইন সার্চিংয়ের সময় বাঁচাবে।

### ১২. Performance considerations
* স্কোপ চেইন যদি অনেক বেশি গভীর বা নেস্টেড হয়, তবে ভ্যারিয়েবল খুঁজতে ইঞ্জিনের একটু বেশি সময় লাগতে পারে। একে Scope Resolution Time বলে।

### ১৩. When NOT to use it
* এটি এড়ানোর কোনো স্কোপ নেই। তবে অতিরিক্ত নেস্টেড স্কোপ এড়িয়ে চলা উচিত কোড রিডাবিলিটি বজায় রাখার স্বার্থে।

### ১৪. Comparison with similar concepts
| ধারণা | অর্থ |
| :--- | :--- |
| **Lexical Scope** | কোথায় ফাংশন লেখা হয়েছে তার ফিজিক্যাল লোকেশন |
| **Scope Chain** | প্যারেন্ট রেফারেন্সের লিঙ্কিং মেকানিজম |

### ১৫. Summary in simple Bangla
স্কোপ চেইন হলো ঘরের ভেতর থেকে জানালায় উঁকি দিয়ে বাইরের উঠান দেখা, কিন্তু উঠানের মানুষ ঘরের ভেতরের ড্রয়ার দেখতে পারে না।

### ১৬. 5 MCQ questions
১. Lexical Scope কখন নির্ধারিত হয়?
   (ক) রানটাইমে (খ) কম্পাইল/রাইট টাইমে (গ) বিল্ড টাইমে (ঘ) পলিফিলিং টাইমে
   **উত্তরঃ খ**
২. স্কোপ চেইনের শেষ সীমা বা রুট কোনটি?
   (ক) Function Scope (খ) Block Scope (গ) Global Scope (ঘ) Local Scope
   **উত্তরঃ গ**
৩. নিচের কোডটির আউটপুট কী হবে?
```javascript
function outer() {
  var x = 10;
  function inner() {
    console.log(x);
  }
  return inner;
}
var x = 20;
var close = outer();
close();
```
   (ক) 20 (খ) 10 (গ) undefined (ঘ) Error
   **উত্তরঃ খ (যেহেতু inner ডিক্লেয়ার হয়েছে outer-এর ভেতর, Lexical Scope অনুযায়ী x=10 পাবে)**
৪. নিচের কোনটি স্কোপ চেইনে খোঁজার ক্রম নির্দেশ করে?
   (ক) Global -> Parent -> Local (খ) Local -> Parent -> Global (গ) Parent -> Local -> Global (ঘ) Random
   **উত্তরঃ খ**
৫. জাভাস্ক্রিপ্ট স্কোপ চেইন তৈরিতে ভূমিকা রাখে কোনটি?
   (ক) Call Stack (খ) Lexical Environment (গ) Heap (ঘ) Web API
   **উত্তরঃ খ**

### ১৭. 5 Coding exercises
১. ৩টি স্তরের নেস্টেড ফাংশন তৈরি করে গ্লোবাল ভ্যারিয়েবল অ্যাক্সেস করে দেখান।
২. এমন একটি বাগ তৈরি করুন যেখানে বাইরের স্কোপ ভেতরের ভ্যারিয়েবল অ্যাক্সেস করতে গিয়ে এরর খাবে।
৩. লেক্সিকাল স্কোপের নিয়ম প্রমাণ করার জন্য একটি কোড লিখুন যেখানে রানটাইম রেফারেন্স নয়, বরং রাইটিং লোকেশন ডোমিনেট করবে।
৪. `IIFE` এবং স্কোপ চেইনের মিশ্রণে একটি কনফিগারেশন মডিউল বানান।
৫. নিচের কোডটির স্কোপ চেইন ডায়াগ্রাম বা ফ্লো চার্ট লিখে ব্যাখ্যা করুন:
```javascript
var g = "Global";
function outer() {
  var o = "Outer";
  function inner() {
    var i = "Inner";
    console.log(g, o, i);
  }
  inner();
}
outer();
```

---

## ৮. let & const in JS, Temporal Dead Zone

### ১. Simple definition (বাংলায়)
**let** এবং **const** হলো ES6-এ আনা ব্লক স্কোপড ভ্যারিয়েবল ডিক্লেয়ার করার কিওয়ার্ড। **Temporal Dead Zone (TDZ)** হলো একটি ভ্যারিয়েবলের স্কোপের শুরু থেকে তার ডিক্লেয়ারেশন ও ইনিশিয়ালাইজেশন সম্পন্ন হওয়ার মধ্যবর্তী সময়, যে সময়ে ওই ভ্যারিয়েবলকে অ্যাক্সেস করতে গেলে জাভাস্ক্রিপ্ট `ReferenceError` দেয়।

### ২. Why this concept exists
`var`-এর রি-ডিক্লেয়ারেশন এবং গ্লোবাল স্কোপ পল্যুশনের যে জটিলতা ছিল, তা দূর করতে এবং জাভাস্ক্রিপ্টকে অন্যান্য কড়া ভাষার মতো টাইট বা সুরক্ষিত করতে ES6-এ `let` ও `const` আনা হয়। আর কোড যাতে কোনো ভ্যারিয়েবল তৈরির আগেই তার ব্যবহার রুখতে পারে, সেজন্যই TDZ রাখা হয়েছে।

### ৩. What problem it solves
এটি ডিক্লেয়ার করার আগে ভ্যারিয়েবল ব্যবহার করার যে বিপজ্জনক স্বভাব (`var`-এর ক্ষেত্রে যা `undefined` দিয়ে কোনো এরর ছাড়াই পার পেয়ে যেত), তা বন্ধ করে বাগ দূর করতে সাহায্য করে।

### ৪. Real-life analogy
মনে করুন আপনি একটি নতুন ক্লাবে মেম্বারশিপ বুক করেছেন। ক্লাব কর্তৃপক্ষ আপনার আইডি কার্ড বানাচ্ছে। বুকিং করার দিন থেকে শুরু করে যেদিন আপনি কার্ডটি হাতে পাবেন, তার মধ্যবর্তী সময়ে আপনি ক্লাবে ঢুকতে পারবেন না। এই মাঝখানের নিষিদ্ধ সময়টিই হলো Temporal Dead Zone।

### ৫. How React works internally regarding this concept
React-এ স্টেট চেঞ্জ বা অন্য যেকোনো কাজের জন্য আমরা সাধারণত `const` বেশি ব্যবহার করি (যেমন: `const [state, setState] = useState()`)। কারণ `const` নিশ্চিত করে যে আমরা স্টেট ভ্যারিয়েবলটিকে দুর্ঘটনাবশত সরাসরি রি-অ্যাসাইন (`state = newValue`) করতে পারব না, যা React-এর ইমিউটেবিলিটি (Immutability) নীতির সাথে সামঞ্জস্যপূর্ণ।

### ৬. Basic example
```javascript
// console.log(a); // ReferenceError: Cannot access 'a' before initialization
let a = 10;
console.log(a); // 10
```

### ७. Step-by-step explanation of the code
১. মেমোরি ফেজে `let a` হোইস্ট হয়ে মেমোরিতে জায়গা নেয়, কিন্তু `var`-এর মতো `undefined` দিয়ে ইনিশিয়ালাইজ হয় না। এটি একটি স্পেশাল স্কোপে (Script Scope) থাকে।
২. লাইন ১ থেকে শুরু করে লাইন ২ এর আগ পর্যন্ত এলাকাটি হলো `a`-এর জন্য **Temporal Dead Zone**।
৩. তাই লাইন ১-এ `a` কে অ্যাক্সেস করতে গেলে ইঞ্জিন বাধা দেয় এবং `ReferenceError` দেয়।
৪. লাইন ২-এ ইনিশিয়ালাইজ হওয়ার পর `a` সফলভাবে অ্যাক্সেস করা যায়।

### ৮. Another real-world example (React Hook Usage)
```javascript
function Counter() {
  // console.log(step); // ReferenceError due to TDZ
  
  const [count, setCount] = React.useState(0);
  const step = 5; 
}
```
এখানে `step` ডিক্লেয়ার করার আগে ব্যবহার করতে গেলে TDZ-এর কারণে কম্পোনেন্ট রেন্ডারিং ক্র্যাশ করবে।

### ৯. Common mistakes beginners make
* `const` দিয়ে ভ্যারিয়েবল ডিক্লেয়ার করে পরবর্তীতে তার মান রি-অ্যাসাইন করার চেষ্টা করা (যেমন: `const user = "Amit"; user = "Hassan";` -> `TypeError`)।
* মনে করা যে `let` এবং `const` হোইস্টই হয় না। (তারা হোইস্ট হয়, কিন্তু মেমোরিতে আন-ইনিশিয়ালাইজড অবস্থায় থাকে)।

### ১০. Interview questions related to this topic
* **Q:** `let` এবং `const` এর মধ্যে মূল পার্থক্য কী?
  * **Ans:** `let` দিয়ে ডিক্লেয়ার করা ভ্যারিয়েবলের মান পরিবর্তন বা রি-অ্যাসাইন করা যায়, কিন্তু `const` দিয়ে ডিক্লেয়ার করা ভ্যারিয়েবল একবার ইনিশিয়ালাইজ করলে রি-অ্যাসাইন করা যায় না।
* **Q:** Temporal Dead Zone বলতে কী বোঝেন?
  * **Ans:** কোনো ভ্যারিয়েবল হোইস্ট হওয়ার সময় থেকে শুরু করে কোডে তার ডিক্লেয়ারেশন ও ইনিশিয়ালাইজেশন সম্পূর্ণ হওয়ার আগের মুহূর্ত পর্যন্ত সময়কে TDZ বলে।

### ১১. Best practices
* সবসময় ভ্যারিয়েবল এবং ফাংশন ব্যবহারের আগে ডিক্লেয়ার করুন।
* ভুলেও আর কখনো `var` ব্যবহার করবেন না।

### ১২. Performance considerations
* `const` এবং `let` জাভাস্ক্রিপ্ট অপ্টিমাইজারে কিছুটা ভালো পারফর্ম করে কারণ ইঞ্জিন আগে থেকেই জানে যে ভ্যালুর স্কোপ এবং রিয়াসাইনমেন্টের লিমিট কতটুকু।

### ১৩. When NOT to use it
* এমন কোনো সিনারিও নেই যেখানে `let`/`const` বাদ দিয়ে `var` ব্যবহার করা উচিত।

### ১৪. Comparison with similar concepts
| প্রপার্টি | var | let | const |
| :--- | :--- | :--- | :--- |
| **স্কোপ** | Function Scope | Block Scope | Block Scope |
| **রি-অ্যাসাইন** | সম্ভব | সম্ভব | অসম্ভব |
| **TDZ** | নেই | আছে | আছে |

### ১৫. Summary in simple Bangla
`let` ও `const` হলো আধুনিক সিকিউরিটি গার্ড, যারা ভ্যারিয়েবলকে আইডি কার্ড (Initialization) ছাড়া ক্লাবে (Scope) ঢুকতে দেয় না এবং এই অপেক্ষার সময়টিই হলো TDZ।

### ১৬. 5 MCQ questions
১. নিচের কোডটির আউটপুট কী হবে?
```javascript
const x;
x = 10;
console.log(x);
```
   (ক) 10 (খ) undefined (গ) SyntaxError (ঘ) TypeError
   **উত্তরঃ গ (const-এ ডিক্লেয়ারেশনের সাথেই ইনিশিয়ালাইজ করা বাধ্যতামূলক)**
২. Temporal Dead Zone-এর সময় কোনো ভ্যারিয়েবল অ্যাক্সেস করলে কোন এররটি পাওয়া যায়?
   (ক) TypeError (খ) ReferenceError (গ) SyntaxError (ঘ) RangeError
   **উত্তরঃ খ**
৩. `let` এবং `const` এর মেমোরি কোথায় জমা হয়?
   (ক) Global Window Object (খ) একটি আলাদা Script/Block Scope-এ (গ) Call Stack-এর বাইরে (ঘ) প্রোটোটাইপে
   **উত্তরঃ খ**
৪. নিচের কোডটির আউটপুট কী?
```javascript
{
  let a = 5;
}
console.log(a);
```
   (ক) 5 (খ) undefined (গ) ReferenceError (ঘ) null
   **উত্তরঃ গ (যেহেতু let ব্লক স্কোপড)**
৫. `const` দিয়ে ডিফাইন করা অবজেক্টের ভেতরের প্রপার্টির মান কি পরিবর্তন করা সম্ভব?
   (ক) না, কখনোই না (খ) হ্যাঁ, সম্ভব (গ) শুধুমাত্র strict mode ছাড়া সম্ভব (ঘ) এরর দেবে
   **উত্তরঃ খ (অবজেক্টের রেফারেন্স পরিবর্তন করা যাবে না, তবে ভেতরের প্রপার্টি পরিবর্তন সম্ভব)**

### ১৭. 5 Coding exercises
১. একটি কোড লিখে দেখান যেখানে `let` এর TDZ স্পষ্ট বোঝা যায়।
২. `const` দিয়ে একটি অবজেক্ট তৈরি করুন এবং দেখান কীভাবে এর ভেতর প্রপার্টি চেঞ্জ করা গেলেও পুরো অবজেক্ট রি-অ্যাসাইন করা যায় না।
৩. একটি `for` লুপের ভেতরে `let` এবং `var` ব্যবহার করে আলাদা আলাদা আউটপুট বুঝিয়ে লিখুন (বিখ্যাত setTimeout ইন্টারভিউ কোশ্চেন)।
৪. একই ব্লকে `let` ভ্যারিয়েবল রি-ডিক্লেয়ার করার ট্রাই করুন এবং এরর মেসেজটি ব্যাখ্যা করুন।
৫. ডিক্লেয়ারেশনের ওপর ভিত্তি করে TDZ জোনের শুরু এবং শেষ চিহ্নিত করে একটি ডেমো কোড কমেন্টসহ লিখুন।

---

## ৯. Block Scope & Shadowing in JS

### ১. Simple definition (বাংলায়)
**Block** বলতে বোঝায় দ্বিতীয় বন্ধনী বা কার্লি ব্রেসেস `{ }` দিয়ে ঘেরা জাভাস্ক্রিপ্ট কোডের অংশ। এই ব্লকের ভেতরের স্কোপকে **Block Scope** বলে। আর যখন কোনো ইনার স্কোপ বা ব্লকের ভেতরের ভ্যারিয়েবল তার বাইরের আউটার স্কোপের একই নামের ভ্যারিয়েবলকে ওভারল্যাপ বা আড়াল করে ফেলে, তখন তাকে **Shadowing** বলে।

### ২. Why this concept exists
জাভাস্ক্রিপ্টে ব্লক লেভেলে কাজ করার সময় (যেমন `if`, `for` ব্লকে) ডেভেলপারদের লোকাল লজিকের জন্য ভ্যারিয়েবল লাগতে পারে। সেই লোকাল ভ্যারিয়েবল যাতে বাইরের গ্লোবাল বা ফাংশন লেভেলের ভ্যারিয়েবলকে নষ্ট না করে, সেই মেকানিজম ট্র্যাকিংয়ের জন্য এই কনসেপ্ট দরকার।

### ৩. What problem it solves
এটি ভ্যারিয়েবলের লিকেজ (Leakage) রোধ করে। `var` ব্লক স্কোপড না হওয়ায় `if` ব্লকের ভেতরের পরিবর্তন বাইরের ভ্যালু বদলে দিত, যা `let` ও `const` দিয়ে সমাধান করা হয়েছে।

### ৪. Real-life analogy
মনে করুন আপনার বাড়ির ড্রয়িং রুমে একটি বড় রঙিন ছাতা খোলা আছে (Outer variable)। আপনি আপনার পড়ার ঘরে (Inner block) গিয়ে আরেকটি ছোট কালো ছাতা মাথায় দিয়ে বসে আছেন। পড়ার ঘরের মানুষের জন্য বড় রঙিন ছাতাটি আড়াল হয়ে গেছে। এটিই হলো Shadowing।

### ৫. How React works internally regarding this concept
React-এর `map` ফাংশন চালিয়ে লিস্ট রেন্ডারিং করার সময় আমরা প্রায়ই ব্লক স্কোপ এবং শ্যাডোয়িংয়ের মুখোমুখি হই।
```jsx
const items = [1, 2, 3];
// Outer scope variable 'item'
const item = "Global Item";

return items.map(item => {
  // Shadowing occurs here: 'item' now refers to the loop variable
  return <li key={item}>{item}</li>; 
});
```
এই শ্যাডোয়িংয়ের কারণে ভেতরের ব্লকে আউটার `item` অ্যাক্সেস করা যায় না।

### ৬. Basic example
```javascript
var a = 100;
let b = 200;
{
  var a = 10; // Shadows and overwrites the outer 'var a'
  let b = 20; // Shadows but doesn't overwrite outer 'let b'
  console.log(a); // 10
  console.log(b); // 20
}
console.log(a); // 10 (overwritten!)
console.log(b); // 200 (safe!)
```

### ৭. Step-by-step explanation of the code
১. গ্লোবাল স্কোপে `a = 100` এবং স্ক্রিপ্ট স্কোপে `b = 200` তৈরি হয়।
২. ব্লকের ভেতরে `var a = 10` লিখলে তা গ্লোবাল স্কোপের `a` কেই নির্দেশ করে এবং মান বদলে ১০ করে দেয়। একে শ্যাডোয়িং ও ওভাররাইটিং বলে।
৩. কিন্তু ব্লকের ভেতরের `let b = 20` একটি আলাদা ব্লক স্কোপ মেমোরিতে তৈরি হয়। এটি বাইরের `b` কে আড়াল (Shadow) করলেও নষ্ট করে না।
৪. ব্লক থেকে বের হওয়ার পর বাইরের `b` আবার তার আগের ২০০ মান ফিরে পায়, কিন্তু `a` এর মান ১০-ই রয়ে যায়।

### ৮. Another real-world example (Illegal Shadowing)
```javascript
let x = 10;
{
  // var x = 20; // Uncaught SyntaxError: Identifier 'x' has already been declared
}
```
যদি আমরা বাইরের `let` ভ্যারিয়েবলকে ভেতরে `var` দিয়ে শ্যাডো করতে যাই, তবে জাভাস্ক্রিপ্ট এরর দেবে। একে **Illegal Shadowing** বলে। তবে বাইরের `var` কে ভেতরে `let` দিয়ে শ্যাডো করা সম্ভব।

### ৯. Common mistakes beginners make
* `var` ব্যবহার করে ব্লকের ভেতর কাজ করে ভাবা যে বাইরের ভ্যালু সেফ থাকবে।
* Illegal Shadowing ঘটিয়ে SyntaxError খাওয়া।

### ১০. Interview questions related to this topic
* **Q:** Shadowing এবং Illegal Shadowing-এর মধ্যে পার্থক্য কী?
  * **Ans:** বাইরের ভ্যারিয়েবলকে ভেতরের ব্লকে বৈধভাবে আড়াল করা হলো Shadowing। কিন্তু কোনো ব্লকে `let` দিয়ে ডিক্লেয়ার করা ভ্যারিয়েবলকে সেই ব্লকের ভেতরেই একই নামের `var` দিয়ে শ্যাডো করতে গেলে যে এরর হয়, তাকে Illegal Shadowing বলে।
* **Q:** `{ }` ব্লকের মেমোরি লাইফটাইম কেমন?
  * **Ans:** ব্লকের কোড এক্সিকিউশন শেষ হওয়ার সাথে সাথে ব্লকের ভেতরের `let`/`const` ভ্যারিয়েবল মেমোরি থেকে মুছে যায়।

### ১১. Best practices
* ব্লকের ভেতরে বা লুপের ভেতরে ভ্যারিয়েবলের এমন নাম দিন যা বাইরের কোনো গুরুত্বপূর্ণ ভ্যারিয়েবলের নামের সাথে মিলে না যায় (শ্যাডোয়িং এড়িয়ে চলুন)।

### ১২. Performance considerations
* ব্লক মেমোরি দ্রুত তৈরি এবং ধ্বংস হয়, যা জাভাস্ক্রিপ্ট মেমোরি অপ্টিমাইজেশনে খুব কার্যকর।

### ১৩. When NOT to use it
* জটিল অ্যাপ্লিকেশনে কোড রিডাবিলিটি ঠিক রাখতে ইচ্ছাকৃত শ্যাডোয়িং পরিহার করাই বুদ্ধিমানের কাজ।

### ১৪. Comparison with similar concepts
| বিষয় | var | let / const |
| :--- | :--- | :--- |
| **ব্লক স্কোপড?** | না | হ্যাঁ |
| **শ্যাডোয়িং প্রভাব** | আউটার ভ্যালু বদলে দেয় | আউটার ভ্যালু সুরক্ষিত রাখে |

### ১৫. Summary in simple Bangla
ব্লক স্কোপ হলো একটি প্রাইভেট রুম, আর শ্যাডোয়িং হলো ঘরের ভেতরে একই নামের নতুন জিনিস এনে বাইরের জিনিসটাকে সাময়িকভাবে আড়াল করা।

### ১৬. 5 MCQ questions
১. নিচের কোডের আউটপুট কী হবে?
```javascript
let a = 5;
{
  let a = 10;
}
console.log(a);
```
   (ক) 10 (খ) 5 (গ) undefined (ঘ) ReferenceError
   **উত্তরঃ খ**
২. নিচের কোন শ্যাডোয়িংটি অবৈধ (Illegal)?
   (ক) `var` কে `let` দিয়ে শ্যাডো করা (খ) `let` কে `var` দিয়ে শ্যাডো করা (গ) `let` কে `let` দিয়ে শ্যাডো করা (ঘ) `const` কে `let` দিয়ে শ্যাডো করা
   **উত্তরঃ খ**
৩. জাভাস্ক্রিপ্টে ব্লক স্কোপ আনার পেছনে মূল অবদান কার?
   (ক) ES5 (খ) ES6 (গ) ES8 (ঘ) ES10
   **উত্তরঃ খ**
৪. নিচের কোডটির রেজাল্ট কী?
```javascript
var x = 1;
{
  var x = 2;
}
console.log(x);
```
   (ক) 1 (খ) 2 (গ) undefined (ঘ) Error
   **উত্তরঃ খ**
৫. ব্লক স্কোপের ভ্যারিয়েবলগুলো কোথায় সংরক্ষিত থাকে?
   (ক) Global Execution Context-এ (খ) Window-তে (গ) Block Scope নামক স্পেশাল মেমোরি জোনে (ঘ) Stack Overflow-তে
   **উত্তরঃ গ**

### ১৭. 5 Coding exercises
১. একটি কোড লিখুন যা ব্লকের ভেতরে Shadowing এবং ব্লকের বাইরে তার প্রভাব চমৎকারভাবে প্রদর্শন করবে।
২. Illegal Shadowing এর একটি উদাহরণ লিখে তা কীভাবে ঠিক করতে হয় তা দেখান।
৩. `for` লুপের ভেতরের ব্লক স্কোপ কীভাবে কাজ করে তা ৩টি ভিন্ন ব্লকের ভ্যারিয়েবল ডিক্লেয়ার করে দেখান।
৪. কন্ডিশনাল `if-else` ব্লকের ভেতরে `var` এবং `let` এর ব্যবহারের পার্থক্য দেখান।
৫. নিচের কোডটি এনালাইসিস করে এর আউটপুট বের করুন:
```javascript
const a = 10;
{
  const a = 20;
  {
    const a = 30;
    console.log(a);
  }
  console.log(a);
}
console.log(a);
```

---

## ১০. Closures in JS

### ১. Simple definition (বাংলায়)
**Closure** হলো জাভাস্ক্রিপ্টের এমন একটি মেকানিজম বা আচরণ, যার মাধ্যমে কোনো ফাংশন তার লেক্সিকাল স্কোপের (Lexical Scope) ভ্যারিয়েবলগুলোকে মনে রাখতে পারে, এমনকি যখন ফাংশনটি তার মূল স্কোপের বাইরে গিয়ে এক্সিকিউট বা রান হয়। সহজ কথায়, একটি ফাংশন এবং তার আশেপাশের লেক্সিকাল এনভায়রনমেন্টের যে বান্ডেল, তাকেই Closure বলে।

### ২. Why this concept exists
জাভাস্ক্রিপ্টে ফাংশনগুলো হলো ফার্স্ট-ক্লাস সিটিজেন (First-class citizens), অর্থাৎ ফাংশনকে অন্য ফাংশন থেকে রিটার্ন ভ্যালু হিসেবে পাঠানো যায়। কিন্তু কোনো ফাংশন যখন তার বাপের বাড়ি (Parent scope) ছেড়ে চলে যায়, তখনো যাতে সে তার বাপের বাড়ির ভ্যারিয়েবলগুলোর অ্যাক্সেস ধরে রাখতে পারে, সেজন্যই ক্লোজারের জন্ম।

### ৩. What problem it solves
এটি ডেটা প্রাইভেসি (Data Privacy) এবং স্টেট ধরে রাখার সমস্যার সমাধান করে। কোনো গ্লোবাল ভ্যারিয়েবল ডিক্লেয়ার না করেই আমরা ফাংশন কলের মধ্যবর্তী সময়ে লোকাল ডেটা বা স্টেট পরিবর্তন ও মনে রাখতে পারি।

### ৪. Real-life analogy
মনে করুন আপনার মা আপনাকে একটি বিশেষ রান্নার রেসিপি শিখিয়ে দিয়েছেন। আপনি এখন নিজের বাড়ি ছেড়ে অন্য একটি দেশে চলে গেছেন এবং সেখানে রান্না করছেন। রান্না করার সময় আপনি আপনার মায়ের শেখানো রেসিপির ইনস্ট্রাকশনগুলো মেমোরি থেকে অ্যাক্সেস করছেন। এখানে আপনি হলেন ইনার ফাংশন, আপনার মায়ের বাড়ি হলো আউটার লেক্সিকাল স্কোপ, আর মায়ের রেসিপি মনে রাখাটি হলো Closure।

### ৫. How React works internally regarding this concept
React-এর অতি পরিচিত Hook `useState` এবং `useEffect` সম্পূর্ণভাবে Closure-এর ওপর ভিত্তি করে তৈরি। আপনি যখন কম্পোনেন্টের ভেতরে `useState` কল করেন এবং স্টেট চেঞ্জ করতে কোনো ইভেন্ট হ্যান্ডলার বানান, সেই হ্যান্ডলারটি ক্লোজারের মাধ্যমে স্টেটের বর্তমান ভ্যালুকে লক বা ক্যাপচার করে রাখে।
> [!WARNING]
> যদি আপনি `useEffect` বা কোনো অ্যাসিনক্রোনাস ফাংশনে ডিফেন্ডেন্সি অ্যারে (Dependency Array) সঠিক না দেন, তবে সেটি প্রুড়নো ভ্যালুকে ক্লোজারের মধ্যে ধরে রাখে। একে **Stale Closure** বাগ বলে।

### ৬. Basic example
```javascript
function outer() {
  let count = 0;
  return function inner() {
    count++;
    console.log(count);
  };
}

const counter = outer();
counter(); // 1
counter(); // 2
```

### ७. Step-by-step explanation of the code
১. `outer()` ফাংশন রান হয়ে `inner` ফাংশনটিকে রিটার্ন করে এবং তার এক্সিকিউশন শেষ হয়ে যায় (Call stack থেকে মুছে যায়)।
২. কিন্তু `inner` ফাংশনটি যখন রিটার্ন হচ্ছিল, তখন সে তার বাপের বাড়ির ভ্যারিয়েবল `count` এর লেক্সিকাল রেফারেন্সটি নিজের সাথে বেঁধে নিয়ে গেছে।
৩. যখন আমরা `counter()` কল করি, তখন `inner` ফাংশনটি রান হয় এবং সে তার ক্লোজার মেমোরি থেকে `count` খুঁজে বের করে তার মান ১ বাড়ায়।
৪. এভাবে বারবার কল করলেও সে তার ক্লোজার স্পেসের মানটি বজায় রাখতে পারে।

### ৮. Another real-world example (Data Hiding/Encapsulation)
```javascript
function createBankAccount(initialBalance) {
  let balance = initialBalance; // Private variable

  return {
    deposit: function(amount) {
      balance += amount;
      return balance;
    },
    getBalance: function() {
      return balance;
    }
  };
}

const myAccount = createBankAccount(1000);
console.log(myAccount.deposit(500)); // 1500
console.log(myAccount.balance); // undefined (Safe and Private!)
```

### ৯. Common mistakes beginners make
* লুপের ভেতরে `var` দিয়ে `setTimeout` ব্যবহার করলে ক্লোজারটি লুপের শেষের মানটি ধরে ফেলে, যার ফলে সব লুপের আউটপুট একই আসে।
* React-এ `useEffect`-এর ডিপেন্ডেন্সি অ্যারে ফাঁকা রেখে ভেতরের স্টেটের ডাইনামিক ভ্যালু পাওয়ার চেষ্টা করা (Stale Closure)।

### ১০. Interview questions related to this topic
* **Q:** Closure কী এবং এর প্রধান ব্যবহার কী?
  * **Ans:** ক্লোজার হলো একটি ফাংশন এবং তার লেক্সিকাল স্কোপের বান্ডেল। এর প্রধান ব্যবহার হলো ডেটা এনক্যাপসুলেশন, প্রাইভেট মেথড তৈরি এবং React-এর মতো স্টেটের স্টেটফুল ট্র্যাকিং।
* **Q:** Closure-এর কারণে মেমোরি লিক (Memory Leak) কীভাবে হতে পারে?
  * **Ans:** ক্লোজার যেহেতু আউটার ফাংশনের ভ্যারিয়েবলের রেফারেন্স ধরে রাখে, তাই ইনার ফাংশনটি যতক্ষণ অ্যাক্টিভ থাকবে, ততক্ষণ আউটার ভ্যারিয়েবলগুলো মেমোরি থেকে রিলিজ হবে না। একে ওল্ড মেমোরি লিকেজ বলা হয়।

### ১১. Best practices
* কাজ শেষে যখন ক্লোজার যুক্ত ফাংশনের আর প্রয়োজন নেই, তখন তার রেফারেন্সকে `null` করে দিন (যেমন `counter = null`), যা মেমোরি রিলিজ করতে সাহায্য করবে।

### ১২. Performance considerations
* ক্লোজার ব্যবহারের সময় মেমোরি ম্যানেজমেন্টের দিকে খেয়াল রাখা উচিত, কারণ অযথা অতিরিক্ত ক্লোজার মেমোরির ওপর চাপ বাড়ায়।

### ১৩. When NOT to use it
* এটি এড়ানো উচিত যখন সাধারণ ফাংশনে আউটার স্টেট বা ভ্যারিয়েবল মনে রাখার কোনো প্রয়োজন নেই, অহেতুক ক্লোজার মেমোরি ব্লক এড়াতে।

### ১৪. Comparison with similar concepts
| মেকানিজম | স্টোরেজ পদ্ধতি | লাইফটাইম |
| :--- | :--- | :--- |
| **Normal Variable** | Stack Memory | ফাংশন শেষে ধ্বংস হয় |
| **Closure Variable** | Heap Memory (Lexical Scope) | রেফারেন্স থাকা পর্যন্ত জীবিত থাকে |

### ১৫. Summary in simple Bangla
ক্লোজার হলো এমন এক অদ্ভুত জাদুর ব্যাগ যা কোনো ফাংশন তার নিজের বাড়ি ছেড়ে যাওয়ার সময় সাথে নিয়ে যায়, এবং ব্যাগের ভেতর বাড়ির সব স্মৃতি (Variables) বন্দি থাকে।

### ১৬. 5 MCQ questions
১. ক্লোজার নিচের কোনটির সাথে সম্পর্কিত?
   (ক) Event Loop (খ) Lexical Scope (গ) Call Stack (ঘ) Web API
   **উত্তরঃ খ**
২. নিচের কোডের আউটপুট কী হবে?
```javascript
for (var i = 0; i < 3; i++) {
  setTimeout(() => console.log(i), 1000);
}
```
   (ক) 0, 1, 2 (খ) 3, 3, 3 (গ) 2, 2, 2 (ঘ) Error
   **উত্তরঃ খ (যেহেতু var গ্লোবাল/ফাংশন স্কোপড, লুপ শেষে i=3 হয়ে যায়)**
৩. উপরের ২ নং প্রশ্নের সমস্যাটি `var`-এর পরিবর্তে কী ব্যবহার করলে সহজে সমাধান করা যায়?
   (ক) const (খ) let (গ) undefined (ঘ) function
   **উত্তরঃ খ (let ব্লক স্কোপড হওয়ায় প্রতি লুপে নতুন বাইন্ডিং তৈরি করে)**
৪. ক্লোজার ভ্যারিয়েবলগুলো কম্পিউটারের কোন মেমোরিতে সেভ থাকে?
   (ক) Call Stack (খ) Cache Memory (গ) Heap Memory (ঘ) ROM
   **উত্তরঃ গ**
৫. React-এর কোন Hook-এ Stale Closure বাগ হওয়ার ঝুঁকি সবচেয়ে বেশি থাকে?
   (ক) useState (খ) useEffect (গ) useMemo (ঘ) useRef
   **উত্তরঃ খ**

### ১৭. 5 Coding exercises
১. একটি ফাংশন লিখুন যা প্রতি কলে একটি ইউনিক আইডি (যেমন: ID_1, ID_2...) জেনারেট করবে ক্লোজার ব্যবহার করে।
২. একটি মেমোইজেশন (Memoization) ফাংশন তৈরি করুন যা ক্যালকুলেশন রেজাল্ট ক্লোজার ক্যাশে সেভ রাখবে।
৩. `var` দিয়ে করা লুপের ভেতরের `setTimeout` বাগটি ক্লোজার দিয়ে সমাধান করুন (IIFE ব্যবহার করে)।
４. React-এর `useRef` এর একটি সাধারণ ক্লোজার-ভিত্তিক বিকল্প জাভাস্ক্রিপ্ট কোড লিখুন।
৫. নিচের কোডটির আউটপুট ব্যাখ্যা করুন:
```javascript
function outmost() {
  var a = 10;
  return function outer(b) {
    return function inner() {
      console.log(a + b);
    };
  };
}
outmost()(5)();
```

---

## ১১. First Class Functions ft. Anonymous Functions

### ১. Simple definition (বাংলায়)
জাভাস্ক্রিপ্টে ফাংশন যখন **First-class citizens** হিসেবে আচরণ করে, তখন তাকে **First Class Functions** বলে। এর মানে হলো জাভাস্ক্রিপ্টে একটি ফাংশনকে অন্য সাধারণ ভ্যারিয়েবলের মতোই ব্যবহার করা যায় (যেমন: ফাংশনকে অন্য ফাংশনে আর্গুমেন্ট হিসেবে পাঠানো, ফাংশন থেকে রিটার্ন করা এবং ভ্যারিয়েবলে স্টোর করা)। আর যে ফাংশনের কোনো নাম থাকে না, তাকে **Anonymous Function** বলে।

### ২. Why this concept exists
ফাংশনাল প্রোগ্রামিং (Functional Programming) প্যারাডাইমকে জাভাস্ক্রিপ্টে সহজ করার জন্য এই ডিজাইন মেকানিজম রাখা হয়েছে। এর ফলে কোডকে আরও মডুলার, ডাইনামিক এবং রি-ইউজেবল করা যায়।

### ৩. What problem it solves
এটি কোডের ডুপ্লিকেশন কমায়। আমাদের যদি কোনো কাজের লজিককে ডাইনামিক করতে হয়, তবে আমরা আস্ত একটি ফাংশনকে অন্য ফাংশনে পাস করে দিতে পারি, যা অনেক বড় বড় লজিক্যাল ব্লকের পুনরাবৃত্তি কমায়।

### ৪. Real-life analogy
চিন্তা করুন আপনি একটি পার্সেল ডেলিভারি সার্ভিস চালাচ্ছেন। আপনি পার্সেল হিসেবে বই, জামা বা অন্য যেকোনো বস্তু ডেলিভারি করতে পারেন। এখানে পার্সেল হলো ভ্যারিয়েবল। এখন যদি আপনাকে বলা হয় যে আপনি আস্ত একটি আদেশের ইনস্ট্রাকশন বুক বা অন্য একটি ডেলিভারি বক্সকেও পার্সেল হিসেবে পাঠাতে পারেন, তবে সেটি হবে ফার্স্ট ক্লাস ডেলিভারি। আর যদি পার্সেলের গায়ে প্রেরকের নাম না থাকে, তবে তা বেনামী (Anonymous)।

### ৫. How React works internally regarding this concept
React-এর মূল ভিত্তিই হলো ফার্স্ট ক্লাস ফাংশন। 
১. **Components as Functions:** React-এর ফাংশনাল কম্পোনেন্টগুলো মূলত সাধারণ জাভাস্ক্রিপ্ট ফাংশন।
২. **Callback Props:** আমরা যখন চাইল্ড কম্পোনেন্টে কোনো ইভেন্ট পাস করি (যেমন: `onClick={handleClick}`), তখন আমরা ফাংশনকে আর্গুমেন্ট হিসেবে পাঠাচ্ছি।
৩. **Higher-Order Components (HOC):** এমন ফাংশন যা একটি কম্পোনেন্ট ইনপুট নিয়ে অন্য একটি কম্পোনেন্ট রিটার্ন করে। এটি ফার্স্ট ক্লাস ফাংশনের প্রকৃষ্ট উদাহরণ।

### ৬. Basic example
```javascript
// Anonymous function stored in a variable (Function Expression)
const greet = function() {
  console.log("Hello from Anonymous!");
};

// First Class Function: Passing function as an argument
function processUser(callback) {
  callback();
}

processUser(greet);
```

### ৭. Step-by-step explanation of the code
১. প্রথমে একটি নামহীন বা Anonymous Function তৈরি করা হয়েছে এবং তা `greet` ভ্যারিয়েবলে রাখা হয়েছে।
২. `processUser` নামের একটি ফাংশন ডিক্লেয়ার করা হয়েছে যা ইনপুট হিসেবে একটি `callback` ফাংশন নেয়।
৩. আমরা `processUser(greet)` কল করে `greet` ফাংশনটিকে আর্গুমেন্ট হিসেবে পাঠিয়েছি।
৪. `processUser` এর ভেতরে গিয়ে এটি `callback()` হিসেবে এক্সিকিউট হয় এবং কনসোলে বার্তা দেখায়।

### ৮. Another real-world example (HOC Pattern in React)
```javascript
// Higher-Order Function returning another function
function withLogging(WrappedComponent) {
  return function(props) {
    console.log("Rendering component...");
    return <WrappedComponent {...props} />;
  };
}
```
এটি React-এর HOC প্যাটার্ন যা জাভাস্ক্রিপ্টের ফার্স্ট ক্লাস ফাংশন ও ক্লোজারের কম্বিনেশনে তৈরি।

### ৯. Common mistakes beginners make
* Anonymous Function-কে স্টেটমেন্ট হিসেবে সরাসরি লেখার চেষ্টা করা (যেমন: `function() {}` যা SyntaxError দেবে যদি না এটি ব্র্যাকেটে ঘেরা থাকে বা কোনো এক্সপ্রেশনে থাকে)।
* ফাংশন পাস করার সময় কল করে ফেলা (যেমন: `onClick={handleClick()}` এর পরিবর্তে সঠিক হলো `onClick={handleClick}`).

### ১০. Interview questions related to this topic
* **Q:** First Class Functions এবং Higher-Order Functions-এর মধ্যে পার্থক্য কী?
  * **Ans:** First Class Function হলো ভাষার একটি ফিচার (যেখানে ফাংশনকে ভ্যারিয়েবলের মতো ট্রিট করা যায়)। আর Higher-Order Function হলো সেই বিশেষ ফাংশন যা অন্য ফাংশনকে ইনপুট নেয় অথবা রিটার্ন করে।
* **Q:** Anonymous Function কেন এবং কোথায় বেশি ব্যবহৃত হয়?
  * **Ans:** যখন কোনো ফাংশন শুধুমাত্র একবার বা সাময়িকভাবে ব্যবহৃত হবে (যেমন callback হিসেবে `setTimeout` বা `map` এর ভেতরে), তখন Anonymous Function বেশি ব্যবহৃত হয়।

### ১১. Best practices
* মডার্ন জাভাস্ক্রিপ্ট বেনামী ফাংশনের জন্য অ্যারো ফাংশন (`() => {}`) ব্যবহার করুন, যা কোডকে আরও সংক্ষিপ্ত করে।
* রি-ইউজেবল লজিক হলে নামযুক্ত ফাংশন (Named Function) ব্যবহার করা ভালো, যাতে স্ট্যাক ট্রেসে বাগ সনাক্ত করা সহজ হয়।

### ১২. Performance considerations
* লুপ বা রেন্ডার ব্লকের ভেতরে বারবার বেনামী ফাংশন তৈরি করলে প্রতিবার নতুন মেমোরি রেফারেন্স তৈরি হয়। React-এ এটি এড়াতে `useCallback` ব্যবহার করা হয়।

### ১৩. When NOT to use it
* যদি কোনো ফাংশন একাধিক জায়গায় ব্যবহার করতে হয়, তবে বেনামী ফাংশন ব্যবহার করবেন না; একটি নেমড ফাংশন বানিয়ে তা রি-ইউজ করুন।

### ১৪. Comparison with similar concepts
| ধরন | সিনট্যাক্স | স্ট্যাক ট্রেস রিডাবিলিটি |
| :--- | :--- | :--- |
| **Named Function** | `function foo() {}` | চমৎকার (নাম শো করে) |
| **Anonymous Function** | `function() {}` / `() => {}` | কঠিন (anonymous দেখায়) |

### ১৫. Summary in simple Bangla
জাভাস্ক্রিপ্টে ফাংশন হলো ভিআইপি মেহমান, যাকে আপনি ভ্যারিয়েবলে ভরতে পারেন, পার্সেল করে পাঠাতে পারেন এবং প্রয়োজনে তার নাম গোপন (Anonymous) রাখতে পারেন।

### ১৬. 5 MCQ questions
১. জাভাস্ক্রিপ্টে ফাংশনকে ভ্যারিয়েবলের মতো আচরণ করতে পারার বৈশিষ্ট্যকে কী বলে?
   (ক) Scope Chain (খ) First Class Functions (গ) Callback Hell (ঘ) Hoisting
   **উত্তরঃ খ**
২. নিচের কোনটি একটি বৈধ Anonymous Function এক্সপ্রেশন?
   (ক) `const x = function() {};` (খ) `function x() {};` (গ) `() => {};` (ঘ) ক এবং গ উভয়ই
   **উত্তরঃ ঘ**
৩. React-এ `onClick={handleClick()}` লিখলে কী ঘটবে?
   (ক) ক্লিক করার পর রান হবে (খ) রেন্ডার হওয়ার সাথে সাথে কল হয়ে যাবে (গ) এরর দেবে (ঘ) কিছুই হবে না
   **উত্তরঃ খ**
৪. স্ট্যাক ট্রেস ডিবাগিংয়ের ক্ষেত্রে নিচের কোনটির ব্যবহার বেশি সুবিধাজনক?
   (ক) Anonymous Function (খ) Named Function (গ) Arrow Function (ঘ) Generator Function
   **উত্তরঃ খ**
৫. Higher-Order Function বলতে কী বোঝায়?
   (ক) যা সবচেয়ে উঁচুতে হোইস্ট হয় (খ) যা অন্য ফাংশনকে আর্গুমেন্ট হিসেবে নেয় বা ফাংশন রিটার্ন করে (গ) যা কোনো মেমোরি নেয় না (ঘ) অ্যাসিনক্রোনাস ফাংশন
   **উত্তরঃ খ**

### ১৭. 5 Coding exercises
১. একটি ফাংশন লিখুন যা ইনপুট হিসেবে দুটি ফাংশন এবং একটি ভ্যালু নেবে, তারপর ভ্যালুটিকে প্রথম ফাংশনে দিয়ে প্রাপ্ত মান দ্বিতীয় ফাংশনে রান করবে।
২. একটি অ্যারো ফাংশন তৈরি করুন যা আরেকটি বেনামী অ্যারো ফাংশন রিটার্ন করবে।
৩. `Array.prototype.sort` এ একটি বেনামী কম্পারেটর ফাংশন পাস করে একটি অ্যারে সর্ট করুন।
৪. এমন একটি কোড লিখুন যেখানে রানটাইম স্ট্যাক ট্রেসে `anonymous` এরর দেখাবে এবং তা সমাধান করে দেখান।
৫. React-এর কাস্টম ইভেন্ট হ্যান্ডলার স্টাইলের একটি জাভাস্ক্রিপ্ট কোড লিখুন যেখানে বাটন ক্লিকে একটি কলব্যাক ফাংশন ফায়ার হবে।

---

## ১২. Callback Functions in JS ft. Event Listeners

### ১. Simple definition (বাংলায়)
**Callback Function** হলো এমন একটি ফাংশন যা অন্য একটি ফাংশনের ভেতরে আর্গুমেন্ট বা প্যারামিটার হিসেবে পাঠানো হয় এবং পরবর্তীতে কোনো বিশেষ ঘটনার পর বা কোনো কাজ সম্পন্ন হওয়ার পর সেই ফাংশনের ভেতর থেকে কল বা এক্সিকিউট করা হয়। আর ব্রাউজারে ইউজার ইভেন্ট (যেমন: ক্লিক, কি-প্রেস) ট্র্যাক করার সিস্টেমকে **Event Listener** বলে, যা কলব্যাক ফাংশনের ওপর ভিত্তি করে কাজ করে।

### ২. Why this concept exists
জাভাস্ক্রিপ্ট একটি অ্যাসিনক্রোনাস ভাষা। কোনো দীর্ঘমেয়াদী কাজ (যেমন: ফাইল রিড, এপিআই রিকোয়েস্ট) শেষ হওয়ার পর কোডকে কীভাবে রেজাল্ট হ্যান্ডেল করতে হবে তা জানানোর জন্য এবং ইউজারের রিয়েল-টাইম ইভেন্টে সাড়া দেওয়ার জন্য কলব্যাক মেকানিজম দরকার।

### ৩. What problem it solves
এটি ব্লকিং কোড (Blocking Code) দূর করে। কলব্যাক ফাংশনের সাহায্যে জাভাস্ক্রিপ্ট কোনো বড় কাজ ব্যাকগ্রাউন্ডে পাঠিয়ে অন্য কোড রান করতে পারে এবং কাজ শেষ হলে কলব্যাকটি ফায়ার করে রেসপন্স দেয়।

### ৪. Real-life analogy
মনে করুন আপনি একটি অনলাইন শপ থেকে পণ্য অর্ডার করেছেন। আপনি সারাদিন দরজার সামনে দাঁড়িয়ে ডেলিভারি ম্যানের অপেক্ষা করছেন না (এটি হলো ব্লকিং)। তার বদলে আপনি আপনার ফোন নম্বরটি (Callback) শপকে দিয়ে অন্য কাজ করছেন। ডেলিভারি ম্যান যখন আপনার বাড়ির সামনে আসবে, সে আপনাকে কল করবে (Executing callback)।

### ৫. How React works internally regarding this concept
React-এর রেন্ডারিং অপ্টিমাইজেশন এবং ইভেন্ট হ্যান্ডলিং সম্পূর্ণভাবে কলব্যাক ও ইভেন্ট লিসেনারের ওপর দাঁড়িয়ে আছে। React সরাসরি ডমে ইভেন্ট লিসেনার বসায় না; বরং এটি **Event Delegation** ব্যবহার করে। React অ্যাপের রুট এলিমেন্টে (`#root` কন্টেইনারে) একটিমাত্র গ্লোবাল ইভেন্ট লিসেনার বসায়। যখন কোনো বাটনে ক্লিক হয়, ইভেন্টটি বাবল আপ (Bubble up) হয়ে রুটে যায় এবং React তার ইন্টারনাল ইভেন্ট ম্যাপ থেকে সঠিক ইভেন্ট হ্যান্ডলার (Callback) খুঁজে নিয়ে রান করায়।

### ৬. Basic example
```javascript
// A simple callback example
function fetchData(userId, callback) {
  setTimeout(() => {
    const data = { id: userId, name: "Rohit" };
    callback(data);
  }, 1000);
}

fetchData(101, function(user) {
  console.log("User received:", user);
});
```

### ৭. Step-by-step explanation of the code
১. `fetchData` ফাংশন কল হওয়ার সময় আমরা আর্গুমেন্ট হিসেবে একটি বেনামী ফাংশন পাস করেছি।
২. `fetchData` এর ভেতরে `setTimeout` ব্যাকগ্রাউন্ডে ১ সেকেন্ড ওয়েট করে।
৩. ১ সেকেন্ড পর যখন ডেটা রেডি হয়, তখন প্যারামিটার হিসেবে পাঠানো `callback(data)` এক্সিকিউট করা হয়।
৪. এর ফলে বেনামী ফাংশনটি রান হয় এবং স্ক্রিনে ইউজার ডেটা শো করে।

### ৮. Another real-world example (Event Listener with closure memory)
```javascript
function attachClickTracker() {
  let clickCount = 0;
  document.getElementById("btn-track").addEventListener("click", function() {
    clickCount++;
    console.log(`Button clicked ${clickCount} times`);
  });
}
// attachClickTracker();
```
এখানে `addEventListener` একটি কলব্যাক ফাংশন নিচ্ছে, যা ক্লোজারের মাধ্যমে `clickCount` মনে রাখতে পারছে।

### ৯. Common mistakes beginners make
* ইভেন্ট লিসেনার অ্যাড করার পর কম্পোনেন্ট আনমাউন্ট হওয়ার সময় তা রিমুভ না করা, যার ফলে মেমোরি লিক হয়।
* কলব্যাক ফাংশনকে প্যারামিটার হিসেবে পাস না করে রান করে ফেলা (যেমন: `addEventListener("click", handler())`)।

### ১০. Interview questions related to this topic
* **Q:** ইভেন্ট লিসেনারের ক্ষেত্রে মেমোরি লিক কীভাবে হয় এবং কীভাবে এটি প্রতিরোধ করা যায়?
  * **Ans:** যদি বারবার ইভেন্ট লিসেনার যুক্ত করা হয় কিন্তু কাজ শেষে তা রিমুভ না করা হয়, তবে ব্রাউজার মেমোরিতে সেগুলো জমা হতে থাকে। সমাধান হলো `removeEventListener` ব্যবহার করা (React-এ `useEffect`-এর ক্লিনআপ ফাংশনে এটি করা হয়)।
* **Q:** Synchronous Callback এবং Asynchronous Callback-এর মধ্যে পার্থক্য কী?
  * **Ans:** `Array.prototype.forEach` এর ভেতরের কলব্যাকটি সিঙ্ক্রোনাস (তাত্ক্ষণিক চলে), আর `setTimeout` বা `fetch` এর ভেতরের কলব্যাকটি অ্যাসিনক্রোনাস (ভবিষ্যতে কোনো এক সময় চলে)।

### ১১. Best practices
* React-এর `useEffect` এ কোনো ইভেন্ট লিসেনার অ্যাড করলে সবসময় তার ক্লিনআপ রিটার্ন ফাংশনে লিসেনারটি রিমুভ করুন।
* কলব্যাক হেল এড়াতে অ্যাসিনক্রোনাস কাজে প্রমিজ বা আসিংক-অ্যাওয়েট ব্যবহার করুন।

### ১২. Performance considerations
* পেইজে শত শত আলাদা আলাদা ইভেন্ট লিসেনার যুক্ত করলে ব্রাউজার স্লো হতে পারে। তাই ইভেন্ট ডেলিগেশন ব্যবহার করা উচিত।

### ১৩. When NOT to use it
* যদি লজিক যদি সরল ও সিঙ্ক্রোনাস হয়, তবে অতিরিক্ত কলব্যাক ব্যবহার করে কোড জটিল করার প্রয়োজন নেই।

### ১৪. Comparison with similar concepts
| মেকানিজম | সিঙ্ক্রোনাস / অ্যাসিনক্রোনাস | সিনট্যাক্স জটিলতা |
| :--- | :--- | :--- |
| **Callback** | উভয়ই হতে পারে | সহজে জটিল (Callback Hell) হয়ে যায় |
| **Promise** | শুধুমাত্র অ্যাসিনক্রোনাস | বেশ গোছানো ও চেইনেবল |

### ১৫. Summary in simple Bangla
কলব্যাক হলো কাউকে নিজের কাজের দায়িত্ব এবং ফোন নম্বর দিয়ে আসা, যাতে সে কাজ শেষ করে আপনাকে ফোন দিয়ে (কলব্যাক করে) আপডেট জানিয়ে দিতে পারে।

### ১৬. 5 MCQ questions
১. নিচের কোনটি Asynchronous Callback-এর উদাহরণ?
   (ক) `array.map(x => x * 2)` (খ) `setTimeout(fn, 1000)` (গ) `array.filter(x => x > 5)` (ঘ) `array.forEach(x => console.log(x))`
   **উত্তরঃ খ**
২. React-এ ইভেন্ট লিসেনারের মেমোরি লিক রোধ করার সর্বোত্তম স্থান কোনটি?
   (ক) useEffect-এর ডিপেন্ডেন্সি অ্যারে (খ) useEffect-এর ক্লিনআপ (Cleanup) ফাংশন (গ) কম্পোনেন্টের রেন্ডার বডি (ঘ) useState-এর ইনিশিয়াল স্টেট
   **উত্তরঃ খ**
৩. `addEventListener("click", myFn)`-এ `myFn` কে কী বলা হয়?
   (ক) Invoker (খ) Event Listener Callback (গ) Thread (ঘ) Promise
   **উত্তরঃ খ**
৪. নিচের কোডটির আউটপুট কী হবে?
```javascript
console.log("Start");
setTimeout(() => console.log("Timeout"), 0);
console.log("End");
```
   (ক) Start -> Timeout -> End (খ) Start -> End -> Timeout (গ) Timeout -> Start -> End (ঘ) Random
   **উত্তরঃ খ (যেহেতু setTimeout অ্যাসিনক্রোনাস এবং ইভেন্ট লুপের মাধ্যমে রান হয়)**
৫. ইভেন্ট লিসেনার রিমুভ করতে হলে নিচের কোনটি করা obligatorio?
   (ক) লিসেনারে বেনামী ফাংশনের বদলে একই রেফারেন্সের নেমড ফাংশন ব্যবহার করা (খ) ব্রাউজার রিলোড দেওয়া (গ) useMemo ব্যবহার করা (ঘ) লুপ চালানো
   **উত্তরঃ ক (বেনামী ফাংশন রিমুভ করা যায় না কারণ তাদের মেমোরি রেফারেন্স আলাদা থাকে)**

### ১৭. 5 Coding exercises
১. একটি কাস্টম `calculate` ফাংশন লিখুন যা দুটি সংখ্যা এবং একটি ম্যাথমেটিক্যাল অপারেশন কলব্যাক (যেমন যোগ/বিয়োগ) নিয়ে রেজাল্ট রিটার্ন করবে।
২. React-এর `useEffect` মডেল সিমুলেট করে একটি উইন্ডো রিসাইজ ইভেন্ট লিসেনার অ্যাড ও ক্লিনআপ করার ডেমো কোড লিখুন।
৩. একটি সাকসেস এবং এরর হ্যান্ডলার কলব্যাক সহ ডামি নেটওয়ার্ক রিকোয়েস্ট ফাংশন তৈরি করুন।
৪. ৩টি আলাদা বাটনে সিঙ্গেল ইভেন্ট ডেলিগেশন মেকানিজম ব্যবহার করে ক্লিক ইভেন্ট হ্যান্ডেল করুন।
৫. নিচের কোডটির এরর বা সমস্যা খুঁজে বের করুন এবং তা ঠিক করুন:
```javascript
// link.addEventListener("click", greet());
```

---

## ১৩. Asynchronous JavaScript & EVENT LOOP from scratch

### ১. Simple definition (বাংলায়)
**Asynchronous JavaScript** হলো এমন একটি প্রোগ্রামিং মেথড যার মাধ্যমে জাভাস্ক্রিপ্ট ইঞ্জিন কোনো দীর্ঘ কাজকে ব্যাকগ্রাউন্ডে পাঠিয়ে দিয়ে অন্য কোড রান করা অব্যাহত রাখতে পারে। আর **Event Loop** হলো জাভাস্ক্রিপ্ট রানটাইমের সেই হার্ট বা চক্র যা অনবরত চেক করতে থাকে যে Call Stack ফাঁকা হয়েছে কি না, এবং ফাঁকা হলে Callback Queue বা Microtask Queue থেকে অপেক্ষমাণ ফাংশনগুলোকে রান করার জন্য Call Stack-এ নিয়ে আসে।

### ২. Why this concept exists
জাভাস্ক্রিপ্ট যেহেতু Single-threaded, তাই যদি আমরা সার্ভার থেকে ডেটা আনার মতো দীর্ঘ কাজগুলো সিঙ্ক্রোনাসলি করতাম, তবে ব্রাউজার ওই সময়ে সম্পূর্ণ হ্যাং হয়ে যেত এবং ইউজার স্ক্রিনে কোনো ক্লিক বা স্ক্রল করতে পারতেন না। এই প্রধান সমস্যা এড়াতেই অ্যাসিনক্রোনাস আর্কিটেকচার এবং ইভেন্ট লুপের উৎপত্তি।

### ৩. What problem it solves
এটি জাভাস্ক্রিপ্টকে নন-ব্লকিং (Non-blocking I/O) বানায়। এর ফলে একাধিক অ্যাসিনক্রোনাস অপারেশন সমান্তরালে চলতে পারে এবং ইউজার এক্সপেরিয়েন্স মসৃণ থাকে।

### ৪. Real-life analogy
চিন্তা করুন একটি ফাস্টফুড রেস্টুরেন্টের কথা। ক্যাশিয়ার (Call Stack) আপনার অর্ডার নিয়ে কিচেনে (Web API/Background) পাঠিয়ে দিল এবং আপনাকে একটি রিসিট নম্বর দিয়ে পরবর্তী কাস্টমারের অর্ডার নিতে শুরু করল। কিচেনে খাবার তৈরি হয়ে গেলে তা কাউন্টারে (Callback Queue) আসে। কাউন্টার থেকে ক্যাশিয়ার যখন ফ্রি হয়, তখন আপনাকে ডেকে খাবারটি বুঝিয়ে দেয়। এই ক্যাশিয়ারের ফ্রি হওয়া চেক করা এবং খাবার বুঝিয়ে দেওয়ার পুরো প্রসেসটিই হলো Event Loop।

### ৫. How React works internally regarding this concept
React-এর রেন্ডার আপডেট এবং ব্যাচিং (Batching) মেকানিজম ইভেন্ট লুপের টাস্ক ও মাইক্রোটাস্কের ওপর দারুণভাবে নির্ভর করে। React যখন স্টেট চেইঞ্জ করে, তখন সে তাত্ক্ষণিকভাবে ডম আপডেট করে না। এটি তার নিজস্ব রেন্ডার প্রসেসকে মাইক্রোটাস্ক কিউতে (Microtask Queue) শিডিউল করে। এর ফলে একই ফাংশনের ভেতরে ১০ বার `setState` কল করলেও ইভেন্ট লুপের পরবর্তী টাস্ক রান হওয়ার আগে React সবকিছু একবারে ব্যাচ করে একবার মাত্র রি-রেন্ডার করে।

### ৬. Basic example
```javascript
console.log("Start");

setTimeout(() => {
  console.log("Macrotask (Timeout)");
}, 0);

Promise.resolve().then(() => {
  console.log("Microtask (Promise)");
});

console.log("End");
```

### ७. Step-by-step explanation of the code
১. প্রথমে সিঙ্ক্রোনাস কোড `console.log("Start")` রান হয় এবং Call Stack থেকে চলে যায়।
২. `setTimeout` কল হয়ে ব্রাউজার এর Web API জোনে চলে যায়। টাইমার ০ মিলি সেকেন্ড থাকায় এটি সাথে সাথে **Callback Queue** (বা Macrotask Queue)-এ এসে জমা হয়।
৩. `Promise.resolve()` এর থেন ব্লকটি সাথে সাথে **Microtask Queue**-এ চলে যায়। (মনে রাখবেন, প্রমিজের কাজ মাইক্রোটাস্ক কিউতে যায় এবং এর প্রায়োরিটি ম্যাক্রোটাস্কের চেয়ে বেশি)।
৪. সিঙ্ক্রোনাস কোড `console.log("End")` রান হয়।
৫. এখন Call Stack ফাঁকা। Event Loop প্রথমে Microtask Queue চেক করে এবং `Microtask (Promise)` কে স্ট্যাকে পাঠিয়ে রান করায়।
৬. মাইক্রোটাস্ক কিউ খালি হওয়ার পর ইভেন্ট লুপ Callback Queue থেকে `Macrotask (Timeout)` কে নিয়ে রান করায়।
৭. স্ক্রিনে আউটপুট আসে: `Start` -> `End` -> `Microtask (Promise)` -> `Macrotask (Timeout)`.

### ৮. Another real-world example (React API Polling Simulation)
```javascript
function startPolling(apiEndpoint) {
  console.log("Polling started...");
  setInterval(() => {
    fetch(apiEndpoint)
      .then(res => res.json())
      .then(data => console.log("Fetched data in background", data));
  }, 5000);
}
```
এখানে ইভেন্ট লুপ প্রতি ৫ সেকেন্ড পর পর ব্যাকগ্রাউন্ডে এপিআই কল ফায়ার করবে, যার ফলে মূল ইউজার ইন্টারফেসে কোনো ল্যাগ হবে না।

### ৯. Common mistakes beginners make
* মনে করা যে `setTimeout(fn, 0)` মানে কোডটি এখনই রান হবে। (এটি আসলে সিঙ্ক্রোনাস কোড শেষ হওয়ার পরেই রান হবে, Call Stack ফাঁকা হওয়ার আগে নয়)।
* মাইক্রোটাস্ক কিউতে ইনফিনিট লুপ তৈরি করে ব্রাউজার ক্র্যাশ করা (যেহেতু মাইক্রোটাস্ক শেষ না হওয়া পর্যন্ত ইভেন্ট লুপ ম্যাক্রোটাস্ক বা রেন্ডারিং ফেজে যায় না)।

### ১০. Interview questions related to this topic
* **Q:** Microtask Queue এবং Macrotask (Callback) Queue-এর মধ্যে পার্থক্য কী?
  * **Ans:** Promises, MutationObserver-এর কাজগুলো যায় Microtask Queue-এ। আর `setTimeout`, `setInterval`, I/O অপারেশন যায় Macrotask Queue-এ। মাইক্রোটাস্ক কিউয়ের প্রায়োরিটি ম্যাক্রোটাস্কের চেয়ে অনেক বেশি থাকে।
* **Q:** Event Loop-এর প্রধান কাজ কী?
  * **Ans:** কল স্ট্যাক খালি আছে কি না এবং পেন্ডিং টাস্ক কিউতে কোনো কাজ আছে কি না তা ক্রমাগত পর্যবেক্ষণ করা এবং পেন্ডিং কাজগুলোকে স্ট্যাকে পুশ করা।

### ১১. Best practices
* অ্যাসিনক্রোনাস প্রসেস হ্যান্ডল করার সময় ভারী ক্যালকুলেশন মেইন থ্রেডে না করে প্রয়োজনে Web Worker ব্যবহার করুন।

### ১২. Performance considerations
* মাইক্রোটাস্ক কিউতে অতিরিক্ত কাজ জমিয়ে রাখা এড়ানো উচিত, কারণ এটি স্ক্রিনের ফ্রেম রেন্ডারিং ব্লক করতে পারে (Render Starvation)।

### ১৩. When NOT to use it
* যদি কোনো কাজ খুব ছোট এবং তাত্ক্ষণিকভাবে দরকার হয়, তবে অ্যাসিনক্রোনাস মেকানিজমে গিয়ে কোডের জটিলতা বাড়াবেন না।

### ১৪. Comparison with similar concepts
| কিউ (Queue) | টাস্ক টাইপ | অগ্রাধিকার (Priority) |
| :--- | :--- | :--- |
| **Microtask Queue** | Promises, queueMicrotask | অত্যন্ত উচ্চ (১ম) |
| **Callback Queue** | setTimeout, setInterval | সাধারণ (২য়) |

### ১৫. Summary in simple Bangla
ইভেন্ট লুপ হলো ট্রাফিক পুলিশের মতো, যে সিঙ্ক্রোনাস কোডের বড় গাড়ি চলে যাওয়া পর্যন্ত অ্যাসিনক্রোনাস কোডের ছোট বাইকগুলোকে ট্রাফিক সিগন্যালে দাঁড় করিয়ে রাখে এবং রাস্তা ফাঁকা হলে তাদের ছেড়ে দেয়।

### ১৬. 5 MCQ questions
১. ইভেন্ট লুপ কোন কিউ থেকে কাজ সবার আগে কল স্ট্যাকে নিয়ে আসে?
   (ক) Callback Queue (খ) Microtask Queue (গ) Render Queue (ঘ) Local Queue
   **উত্তরঃ খ**
২. নিচের কোনটি Macrotask-এর অন্তর্ভুক্ত?
   (ক) Promise.then (খ) MutationObserver (গ) setInterval (ঘ) queueMicrotask
   **উত্তরঃ গ**
৩. JavaScript-এর ক্ষেত্রে নিচের কোন কথাটি সত্য?
   (ক) এটি Multi-threaded (খ) এটি Single-threaded এবং Non-blocking (গ) এটি সিঙ্ক্রোনাস কোড রান করতে পারে না (ঘ) এটি ব্রাউজারের বাইরে চলে না
   **উত্তরঃ খ**
৪. নিচের কোডটির আউটপুট কী হবে?
```javascript
setTimeout(() => console.log("A"), 0);
console.log("B");
```
   (ক) A -> B (খ) B -> A (গ) A (ঘ) Error
   **উত্তরঃ খ**
৫. ইভেন্ট লুপের বর্তমান স্টেটাস এবং এক্সিকিউশন ডিবাগ করার সবচেয়ে ভালো মাধ্যম কোনটি?
   (ক) Global Object চেক করা (খ) Performance tab এবং Loupe tool ব্যবহার করা (গ) window-তে লুপ চালানো (ঘ) try-catch ব্যবহার
   **উত্তরঃ খ**

### ১৭. 5 Coding exercises
১. একটি কোড লিখুন যা ইভেন্ট লুপের তিনটি স্তর (Synchronous, Microtask, Macrotask) এর এক্সিকিউশন অর্ডার প্রমাণ করবে।
২. `queueMicrotask` ব্যবহার করে একটি কাস্টম অ্যাসিনক্রোনাস কাজকে মাইক্রোটাস্ক কিউতে পুশ করুন এবং তার আচরণ দেখুন।
৩. এমন একটি কোড লিখুন যেখানে টাইমার সেট করার পরেও সেটি নির্দিষ্ট সময়ের চেয়ে বেশি দেরি করে রান করবে (Call Stack ব্লকড থাকার কারণে)।
৪. `Promise` এবং `setTimeout` এর চেইনিং দিয়ে একটি পাইপলাইন তৈরি করুন।
৫. নিচের কোডটির সঠিক আউটপুট ক্রমানুসারে ব্যাখ্যা করুন:
```javascript
console.log(1);
setTimeout(() => console.log(2), 10);
Promise.resolve().then(() => console.log(3));
setTimeout(() => console.log(4), 0);
console.log(5);
```

---

## ১৪. Higher-Order Functions ft. Functional Programming

### ১. Simple definition (বাংলায়)
**Higher-Order Function (HOF)** হলো এমন একটি ফাংশন যা অন্য এক বা একাধিক ফাংশনকে আর্গুমেন্ট (Argument) হিসেবে গ্রহণ করে, অথবা রিটার্ন ভ্যালু হিসেবে একটি নতুন ফাংশন ফেরত দেয়। এটি ফাংশনাল প্রোগ্রামিংয়ের অন্যতম মূল স্তম্ভ।

### ২. Why this concept exists
কোড যাতে ড্রায় বা DRY (Don't Repeat Yourself) নীতি মেনে চলতে পারে এবং ডেটার ওপর ক্রিয়াকলাপগুলোকে সহজে অ্যাবস্ট্রাক্ট (Abstract) বা সাধারণীকরণ করা যায়, সেজন্যই এই কনসেপ্ট দরকার।

### ৩. What problem it solves
এটি কোডের বড় বড় লুপ ও কন্ডিশনাল ব্লকের পুনরাবৃত্তি কমায়। ডেটা ফিল্টারিং, ম্যাপিং বা যেকোনো প্রসেসিং লজিককে বারবার না লিখে আমরা একটি HOF বানিয়ে ডাইনামিক করতে পারি।

### ৪. Real-life analogy
চিন্তা করুন একটি বহুমুখী ওভেন বা মাইক্রোওভেনের কথা। ওভেন নিজেই একটি মেশিন (HOF)। এটি নিজের ভেতরে অন্য একটি ডিশ বা খাবার রান্নার রেসিপি ইনপুট নেয় এবং সেই অনুযায়ী হিট জেনারেট করে রান্না শেষ করে। আপনি ওভেনের ভেতর পিজা রেসিপি দিলে পিজা পাবেন, আর কেক দিলে কেক পাবেন।

### ৫. How React works internally regarding this concept
React-এ Higher-Order Components (HOC) তৈরি হয় এই HOF কনসেপ্ট ব্যবহার করে। এছাড়াও React-এর রেন্ডারিংয়ের সময় আমরা প্রচুর HOF ব্যবহার করি। যেমন:
```jsx
// A higher-order function returning a React Component
function withAuth(Component) {
  return function AuthenticatedComponent(props) {
    const isAuth = checkAuth();
    return isAuth ? <Component {...props} /> : <Login />;
  };
}
```
এটি React-এর সিকিউরিটি এবং রাউটিং সুরক্ষায় বহুল ব্যবহৃত একটি আর্কিটেকচার।

### ৬. Basic example
```javascript
// A simple HOF that accepts a function
function repeatAction(count, action) {
  for (let i = 0; i < count; i++) {
    action(i);
  }
}

repeatAction(3, function(index) {
  console.log(`Action number: ${index}`);
});
```

### ৭. Step-by-step explanation of the code
১. `repeatAction` একটি HOF কারণ এটি প্যারামিটার হিসেবে `action` নামক একটি ফাংশন গ্রহণ করে।
২. আমরা একে কল করার সময় ৩ এবং একটি বেনামী ফাংশন পাস করেছি।
৩. লুপটি ৩ বার চলে এবং প্রতিবার আমাদের পাঠানো কলব্যাক ফাংশনটিকে তার লুপের ইনডেক্স সহ রান করায়।
৪. এর ফলে স্ক্রিনে তিনটি আলাদা লাইন প্রিন্ট হয়।

### ৮. Another real-world example (Functional Multiplier)
```javascript
// HOF returning another function
function createMultiplier(factor) {
  return function(number) {
    return number * factor;
  };
}

const double = createMultiplier(2);
console.log(double(5)); // 10
const triple = createMultiplier(3);
console.log(triple(5)); // 15
```

### ৯. Common mistakes beginners make
* HOF থেকে ফাংশন রিটার্ন করার পর তা সরাসরি রান না করা (যেমন `createMultiplier(2)` কল করে ভাবা যে রেজাল্ট সরাসরি চলে আসবে, অথচ এটি আরেকটি ফাংশন রিটার্ন করে যা আবার কল করতে হয়)।

### ১০. Interview questions related to this topic
* **Q:** Higher-Order Function-এর দুটি প্রধান বৈশিষ্ট্য কী কী?
  * **Ans:** এটি এক বা একাধিক ফাংশনকে ইনপুট হিসেবে নিতে পারে অথবা এটি একটি ফাংশন আউটপুট/রিটার্ন করতে পারে।
* **Q:** React-এ HOC-এর চেয়ে কাস্টম হুক্স (Custom Hooks) কেন বেশি জনপ্রিয় হচ্ছে?
  * **Ans:** কারণ HOC ব্যবহারের ফলে র‍্যাপার হেল (Wrapper Hell) তৈরি হয় এবং প্রপস ডিলিউশন বা ক্লাশিং ঘটে, যা কাস্টম হুক্সে সহজে এড়ানো যায়।

### ১১. Best practices
* HOF ব্যবহারের সময় কোলব্যাক ফাংশনগুলোকে পিউর ফাংশন (Pure Functions) রাখার চেষ্টা করুন, যা কোনো সাইড-ইফেক্ট ছাড়াই প্রেডিক্টেবল আউটপুট দেবে।

### ১২. Performance considerations
* ডাইনামিকালি বারবার HOF-এর ভেতর নতুন ফাংশন ডিফাইন করলে মেমোরি কনজাম্পশন বাড়তে পারে। রেগুলার মেথড বাইরে ডিফাইন করে রেফারেন্স পাস করা ভালো।

### ১৩. When NOT to use it
* খুব সাধারণ সিঙ্গেল-লাইন কোড বা লজিকে অযথা HOF ঢুকিয়ে কোড রিডাবিলিটি নষ্ট করবেন না।

### ১৪. Comparison with similar concepts
| বিষয় | Regular Function | Higher-Order Function |
| :--- | :--- | :--- |
| **প্যারামিটার** | সাধারণ মান (string, object) | ফাংশন ও সাধারণ মান |
| **রিটার্ন** | সাধারণ মান | ফাংশন বা সাধারণ মান |

### ১৫. Summary in simple Bangla
Higher-Order Function হলো এমন এক রাজকীয় ফাংশন যে তার দরবারে অন্য ফাংশনকে কাজের মানুষ হিসেবে আনতে পারে অথবা নতুন একটি ফাংশন জন্ম দিয়ে ফেরত দিতে পারে।

### ১৬. 5 MCQ questions
১. নিচের কোনটি Higher-Order Function-এর উদাহরণ?
   (ক) `Math.sqrt` (খ) `Array.prototype.map` (গ) `parseInt` (ঘ) `console.log`
   **উত্তরঃ খ**
২. HOF ব্যবহার করার প্রধান সুবিধা কোনটি?
   (ক) কোডের পুনঃব্যবহারযোগ্যতা ও রিডাবিলিটি বাড়ানো (খ) রানটাইম মেমোরি কমানো (গ) ফাইল সাইজ ছোট করা (ঘ) এরর প্রিভেন্ট করা
   **উত্তরঃ ক**
৩. নিচের কোডটির আউটপুট কী হবে?
```javascript
const add = (a) => (b) => a + b;
console.log(add(2)(3));
```
   (ক) 5 (খ) undefined (গ) Function (ঘ) Error
   **উত্তরঃ ক**
৪. Functional Programming-এ কোনো স্টেট পরিবর্তন না করার নীতিকে কী বলে?
   (ক) Shadowing (খ) Immutability (গ) Hoisting (ঘ) Threading
   **উত্তরঃ খ**
৫. React-এ HOC তৈরি করতে মূলত জাভাস্ক্রিপ্টের কোন ধারণাটি সবচেয়ে বেশি ব্যবহৃত হয়?
   (ক) Event loop (খ) Higher-Order Function and Closures (গ) Temporal Dead Zone (ঘ) Web API
   **উত্তরঃ খ**

### ১৭. 5 Coding exercises
১. একটি কাস্টম `filter` HOF নিজে থেকে তৈরি করুন (বিল্ট-ইন filter ব্যবহার না করে)।
২. একটি HOF লিখুন যা কোনো ফাংশন রান হওয়ার সময় কতক্ষণ লেগেছে তা ট্র্যাক করে কনসোলে প্রিন্ট করবে।
৩. একটি `compose` ফাংশন লিখুন যা একাধিক ফাংশনকে একসাথে যুক্ত করে একটি পাইপলাইন বানাবে।
৪. React-এর মেমোইজেশন মেকানিজম ডেমো করতে একটি HOF লিখুন যা ইনপুট ডেটা ক্যাশ করে রাখবে।
৫. নিচের কোডটির আউটপুট ব্যাখ্যা করুন:
```javascript
const logger = (prefix) => (message) => console.log(`[${prefix}] ${message}`);
```

---

## ১৫. map, filter & reduce

### ১. Simple definition (বাংলায়)
**map**, **filter**, এবং **reduce** হলো জাভাস্ক্রিপ্টের তিনটি অত্যন্ত শক্তিশালী Higher-Order অ্যারে মেথড। **map** একটি অ্যারেকে রূপান্তর করে সমদৈর্ঘ্যের নতুন অ্যারে দেয়; **filter** কন্ডিশন ম্যাচ করা আইটেমগুলো নিয়ে নতুন ছোট অ্যারে দেয়; আর **reduce** পুরো অ্যারেকে প্রসেস করে একটিমাত্র একক মান বা রেজাল্ট (যেমন সংখ্যা, অবজেক্ট বা অ্যারে) রিটার্ন করে।

### ২. Why this concept exists
ঐতিহ্যগত `for` লুপ চালিয়ে অ্যারে প্রসেস করা অনেক বেশি ম্যানুয়াল এবং বাগ-প্রবণ। কোডকে আরও ডিক্ল্যারেটিভ (Declarative) ও রিডাবল করতে এবং সরাসরি মেমোরি মিউটেশন এড়াতে এগুলো তৈরি করা হয়েছে।

### ৩. What problem it solves
এটি ইমিউটেবিলিটি বজায় রাখতে সাহায্য করে। মূল অ্যারেকে কোনো রকম পরিবর্তন বা মিউটেট (Mutate) না করে আমরা ডেটা প্রসেসিং করতে পারি, যা স্টেটফুল অ্যাপ্লিকেশনে অত্যন্ত জরুরি।

### ৪. Real-life analogy
* **map:** আপনি এক ঝুড়ি খোসাযুক্ত আম দিলেন একটি মেশিনে, মেশিন সবগুলো আমের খোসা ছাড়িয়ে সমান সংখ্যক ছাড়ানো আম ফেরত দিল।
* **filter:** আপনি ওই আমগুলো থেকে কেবল মিষ্টি আমগুলো বেছে নেওয়ার জন্য ফিল্টার মেশিনে দিলেন, যা টক আমগুলো বাদ দিয়ে দিল।
* **reduce:** মিষ্টি আমগুলো জুসারে দিয়ে ব্লেন্ড করে এক গ্লাস ম্যাঙ্গো জুস (Single Value) বানিয়ে নিলেন।

### ৫. How React works internally regarding this concept
React-এর UI রেন্ডারিংডে `map` মেথডটি অপরিহার্য। ডেটা অ্যারে থেকে JSX এলিমেন্ট তৈরি করতে আমরা সবসময় `map` ব্যবহার করি। 
```jsx
// Rendering a list in React
return (
  <ul>
    {products.map(product => (
      <li key={product.id}>{product.name}</li>
    ))}
  </ul>
);
```
React-এ স্টেট ফিল্টার করতে (যেমন ডিলিট অপশনে) `filter` ব্যবহার করা হয়। আর রিডাক্স বা useReducer-এর স্টেট ম্যানেজমেন্ট মূলত `reduce` এর ধারণার ওপর ভিত্তি করে তৈরি।

### ৬. Basic example
```javascript
const numbers = [1, 2, 3, 4];

// Map: Double all numbers
const doubled = numbers.map(num => num * 2); // [2, 4, 6, 8]

// Filter: Get even numbers
const evens = numbers.filter(num => num % 2 === 0); // [2, 4]

// Reduce: Sum of all numbers
const sum = numbers.reduce((accumulator, current) => accumulator + current, 0); // 10
```

### ৭. Step-by-step explanation of the code
১. `map` প্রতিটি উপাদানের ওপর লুপ চালায়, তাকে ২ দিয়ে গুণ করে এবং একটি নতুন অ্যারে `doubled` রিটার্ন করে। মূল `numbers` অ্যারে আগের মতোই থাকে।
২. `filter` কন্ডিশন চেক করে যে উপাদানটি জোড় সংখ্যা কি না। সত্য হলে তা নতুন অ্যারোফিল্টারে যুক্ত করে।
৩. `reduce` এ প্রথম প্যারামিটার হলো একটি এক্যুমুলেটর (যা রানটাইমে ভ্যালু জমা রাখে) এবং দ্বিতীয়টি হলো কারেন্ট ভ্যালু। ইনিশিয়াল ভ্যালু `0` দিয়ে শুরু করে সে সবগুলো সংখ্যার যোগফল শেষে `10` রিটার্ন করে।

### ৮. Another real-world example (Cart Calculation in React)
```javascript
const cart = [
  { id: 1, name: "Book", price: 200, qty: 2 },
  { id: 2, name: "Pen", price: 50, qty: 5 }
];

// Calculate total cart price using reduce
const totalPrice = cart.reduce((total, item) => total + (item.price * item.qty), 0);
console.log(totalPrice); // 650
```

### ৯. Common mistakes beginners make
* `map` ব্যবহার করে লুপের রিটার্ন স্টেটমেন্ট লিখতে ভুলে যাওয়া (রিটার্ন না করলে নতুন অ্যারোটি `undefined` দিয়ে ভরে যাবে)।
* React-এ `map` চালানোর সময় ইউনিক `key` প্রপ না দেওয়া।
* `reduce` ব্যবহারের সময় ইনিশিয়াল ভ্যালু (Initial Value) সেট না করা, যার ফলে প্রথম আইটেমটি সরাসরি এক্যুমুলেটর হয়ে যায় এবং ভুল আউটপুট দিতে পারে।

### ১০. Interview questions related to this topic
* **Q:** `forEach` এবং `map`-এর মধ্যে প্রধান পার্থক্য কী?
  * **Ans:** `forEach` কোনো নতুন অ্যারে রিটার্ন করে না এবং এটি শুধু লুপ চালিয়ে যায়। আর `map` লুপ চালিয়ে একটি নতুন রূপান্তরিত অ্যারে রিটার্ন করে।
* **Q:** `reduce` এর এক্যুমুলেটর (Accumulator) কী কাজ করে?
  * **Ans:** এটি প্রতিটি ইটারেশন বা লুপের পর অন্তর্বর্তীকালীন হিসাব বা মান সংরক্ষণ করে পরবর্তী লুপের জন্য পাস করে।

### ১১. Best practices
* অ্যারে মেথডের চেইনিং করার সময় ওভার-কমপ্লেক্স করবেন না, প্রয়োজনে রিডাবিলিটি ঠিক রাখতে আলাদা ধাপে করুন।
* React স্টেটে মিউটেশন এড়াতে অ্যারে মডিফাই করার সময় সবসময় `map`/`filter` ব্যবহার করুন।

### ১২. Performance considerations
* খুব বড় অ্যারের ক্ষেত্রে একের পর এক মেথড চেইনিং (যেমন `.map().filter().reduce()`) করলে একাধিকবার পুরো অ্যারে স্ক্যান হয়। বড় ডেটার ক্ষেত্রে সিঙ্গেল `reduce` দিয়ে সব কাজ একবারে করা পারফরম্যান্স ভালো দেয়।

### १३. When NOT to use it
* যদি আপনার অ্যারে মডিফাই করার প্রয়োজন না থাকে এবং শুধু সাইড ইফেক্ট (যেমন কনসোল প্রিন্ট বা ডম অ্যাকশন) ঘটাতে হয়, তবে `map` এর পরিবর্তে `forEach` ব্যবহার করুন।

### ১৪. Comparison with similar concepts
| মেথড | মূল অ্যারে কিউ পরিবর্তন করে? | রিটার্ন টাইপ |
| :--- | :--- | :--- |
| **map** | না | নতুন অ্যারে (সমান সাইজ) |
| **filter** | না | নতুন অ্যারে (কন্ডিশনাল সাইজ) |
| **reduce** | না | যেকোনো সিঙ্গেল ভ্যালু (number, object, etc.) |

### ১৫. Summary in simple Bangla
`map` হলো মেকআপ রুম যা সবাইকে সাজিয়ে দেয়, `filter` হলো সিকিউরিটি গার্ড যে দেখে টিকিট আছে কি না, আর `reduce` হলো পিগি ব্যাংক যেখানে সব পয়সা জমে এক হয়ে যায়।

### ১৬. 5 MCQ questions
১. React-এ লিস্ট আইটেম ডিলিট করার পর স্টেট আপডেট করতে সাধারণত নিচের কোনটি সবচেয়ে বেশি ব্যবহৃত হয়?
   (ক) map (খ) filter (গ) reduce (ঘ) push
   **উত্তরঃ খ**
২. নিচের কোডের আউটপুট কী হবে?
```javascript
const res = [1, 2, 3].map(x => { x * 2 });
console.log(res);
```
   (ক) [2, 4, 6] (খ) [undefined, undefined, undefined] (গ) Error (ঘ) [1, 2, 3]
   **উত্তরঃ খ (যেহেতু কার্লি ব্র্যাকেট দেওয়া হয়েছে কিন্তু return কিওয়ার্ড লেখা হয়নি)**
৩. `reduce` মেথডে যদি ইনিশিয়াল ভ্যালু ডিফাইন করা না থাকে, তবে এক্যুমুলেটর ডিফল্ট হিসেবে কোন মানটি নেয়?
   (ক) 0 (খ) null (গ) অ্যারের প্রথম উপাদান (ঘ) undefined
   **উত্তরঃ গ**
৪. নিচের কোনটি সিলেক্টিভ বা ফিল্টারড এলিমেন্ট বাছাই করার জন্য ব্যবহৃত হয়?
   (ক) filter (খ) find (গ) map (ঘ) ক এবং খ উভয়ই
   **উত্তরঃ ঘ (filter অ্যারে দেয়, find প্রথম ম্যাচিং এলিমেন্ট দেয়)**
৫. `numbers.map().filter()` এই স্টাইলকে জাভাস্ক্রিপ্টে কী বলা হয়?
   (ক) Method Shadowing (খ) Method Chaining (গ) Scope Resolution (ঘ) Dynamic Polling
   **উত্তরঃ খ**

### ১৭. 5 Coding exercises
১. একটি অবজেক্ট অ্যারে থেকে নির্দিষ্ট ক্যাটাগরির প্রোডাক্টগুলোর মোট প্রাইজ বের করার জন্য `filter` এবং `reduce` চেইন করুন।
২. কাস্টম `map` ফাংশন নিজে থেকে তৈরি করুন (পদ্ধতি: Array.prototype-এ অ্যাড করে)।
৩. `reduce` মেথড ব্যবহার করে একটি স্ট্রিং অ্যারে থেকে প্রতিটি শব্দের কাউন্ট বের করে একটি অবজেক্ট বানান (যেমন: `{ apple: 2, orange: 1 }`)।
৪. এমন একটি কোড লিখুন যা একটি সংখ্যা অ্যারের শুধুমাত্র বিজোড় সংখ্যাগুলোকে ডাবল করবে (map + filter কম্বিনেশন)।
৫. React-এর কাস্টম ইভেন্ট হ্যান্ডলার প্যাটার্ন অনুযায়ী ইমিউটেবল অ্যারে আপডেট করার একটি ডেমো কোড লিখুন।

---

## ১৬. Callback and callback Hell

### ১. Simple definition (বাংলায়)
যখন কোনো অ্যাসিনক্রোনাস কাজকে ক্রমানুসারে করার জন্য একের পর এক কলব্যাক ফাংশনকে অন্য কলব্যাকের ভেতরে নেস্ট বা ঢুকিয়ে দেওয়া হয়, তখন কোডটি দেখতে ত্রিভুজাকৃতির বা অনেক ডান দিকে সরে যায়। কোডের এই জটিল ও অপাঠ্য অবস্থাকে **Callback Hell** বা **Pyramid of Doom** বলে।

### ২. Why this concept exists
জাভাস্ক্রিপ্ট রানটাইমে অ্যাসিনক্রোনাস কাজগুলোর ডিপেন্ডেন্সি মেইনটেইন করার জন্য (যেমন: কাজ A শেষ হওয়ার পর কাজ B শুরু হবে, B এর পর C...) আগে অন্য কোনো ভালো অপশন ছিল না। ডেভেলপাররা বাধ্য হয়ে একটির ভেতর আরেকটি কলব্যাক নেস্ট করতেন।

### ৩. What problem it solves
এটি মূলত কোনো সমস্যার সমাধান করে না; Privatization-এর বদলে এটি নিজেই একটি বড় সমস্যা যা পরবর্তীতে Promises এবং Async-Await এর জন্ম দিয়েছে।

### ৪. Real-life analogy
মনে করুন আপনি একটি সরকারি অফিসে গেছেন লাইসেন্স করতে। 
১ম কাউন্টারে আপনাকে বলল ফরম পূরণ করতে। ফরম পূরণ শেষ হলে (Callback 1) আপনাকে পাঠালো ২য় কাউন্টারে ফাইল জমা দিতে। ফাইল জমা হলে (Callback 2) ৩য় কাউন্টারে বলল ফি দিতে। ফি দেওয়া হলে (Callback 3) ৪র্থ কাউন্টারে বলল ছবি তুলতে। এই জটিল ও দীর্ঘ গোলকধাঁধাই হলো Callback Hell।

### ৫. How React works internally regarding this concept
React-এর আদিম যুগে বা বড় বড় নেটওয়ার্ক রিকোয়েস্ট ম্যানেজ করার সময় Callback Hell একটি বড় অভিশাপ ছিল। যেমন: ইউজারের ডেটা লোড করো -> ইউজার ভেরিফায়েড হলে প্রোডাক্ট লিস্ট লোড করো -> প্রোডাক্ট লোড হলে দাম ক্যালকুলেট করো। React কম্পোনেন্টের ভেতর এমন কোড লিখলে তা সম্পূর্ণ আন-রিডাবল হয়ে যেত। আধুনিক React-এ আমরা এটি সমাধান করতে `async/await` ব্যবহার করি।

### ৬. Basic example
```javascript
// Callback Hell Simulation
getUser(1, function(user) {
  getPosts(user.id, function(posts) {
    getComments(posts[0].id, function(comments) {
      console.log("Comments of first post:", comments);
    });
  });
});
```

### ७. Step-by-step explanation of the code
১. `getUser` ডেটা পাওয়ার পর তার ভেতরের কলব্যাক ফাংশনটি রান করে।
২. প্রাপ্ত ইউজারের আইডি দিয়ে `getPosts` কল করা হয় এবং এর রেসপন্স পেতে আরেকটি ইনার কলব্যাক দেওয়া হয়।
৩. প্রথম পোস্টের আইডি দিয়ে `getComments` কল করা হয় যা আরেকটি নেস্টেড কলব্যাক।
৪. কোডটি ক্রমান্বয়ে ভেতরের দিকে ঢুকতে থাকে, ফলে ব্র্যাকেট মেলানো কঠিন হয়ে পড়ে এবং কোড রিডাবিলিটি হারিয়ে ফেলে।

### ৮. Another real-world example (Animation sequence)
```javascript
// Callback hell in DOM animation
moveElement("box", 100, function() {
  moveElement("box", 200, function() {
    moveElement("box", 300, function() {
      console.log("Animation sequence finished!");
    });
  });
});
```

### ৯. Common mistakes beginners make
* এরর হ্যান্ডলিং করতে ভুলে যাওয়া (প্রতিটি নেস্টেড লেভেলে আলাদা আলাদা এরর প্যারামিটার হ্যান্ডেল না করলে কোড মঝপথে ক্র্যাশ করতে পারে কিন্তু কোনো ডিবাগ মেসেজ পাওয়া যায় না)।

### ১০. Interview questions related to this topic
* **Q:** Callback Hell থেকে বাঁচার প্রধান উপায়গুলো কী কী?
  * **Ans:** ১. কোড মডিউলার করে আলাদা আলাদা ফাংশন বানানো, ২. Promises ব্যবহার করা এবং ৩. Async-Await ব্যবহার করা।
* **Q:** Pyramid of Doom বলতে কী বোঝায়?
  * **Ans:** এটি Callback Hell-এর অপর নাম। কোডের অতিরিক্ত নেস্টিংয়ের ফলে কোডের আকার পিরামিডের মতো দেখায় বলে একে Pyramid of Doom বলা হয়।

### ১১. Best practices
* কখনোই ৩ স্তরের বেশি কলব্যাক নেস্ট করবেন না।
* অ্যাসিনক্রোনাস ডিপেন্ডেন্সির জন্য সবসময় Promises বা `async/await` ব্যবহার করুন।

### ১২. Performance considerations
* ক্লোজার মেমোরির কারণে অনেক বেশি নেস্টেড কলব্যাক মেমোরিতে ভ্যারিয়েবল ধরে রাখে, যা মেমোরি পারফরম্যান্সের ক্ষতি করে।

### ১৩. When NOT to use it
* প্রোডাকশন লেভেলের অ্যাসিনক্রোনাস ফ্লোতে ট্র্যাডিশনাল নেস্টেড কলব্যাক ব্যবহার করবেন না।

### ১৪. Comparison with similar concepts
| মেথড | রিডাবিলিটি | এরর হ্যান্ডলিং |
| :--- | :--- | :--- |
| **Callback Hell** | অত্যন্ত জটিল | প্রতি লেভেলে ম্যানুয়াল ও কঠিন |
| **Promise Chain** | পরিষ্কার | একক `.catch()` দিয়ে সহজ |
| **Async / Await** | সাধারণ সিঙ্ক্রোনাস কোডের মতো | `try-catch` দিয়ে চমৎকার |

### ১৫. Summary in simple Bangla
কলব্যাক হেল হলো গোলকধাঁধার মতো একটার ভেতর আরেকটা চাবি খুঁজে যাওয়ার জটিল কাজ, যা কোডকে নোংরা এবং পড়ার অযোগ্য করে ফেলে।

### ১৬. 5 MCQ questions
১. Callback Hell-এর অপর নাম কী?
   (ক) Callback Chain (খ) Pyramid of Doom (গ) Promise Hell (ঘ) Shadow Box
   **উত্তরঃ খ**
২. নিচের কোনটি Callback Hell এড়ানোর আধুনিক সমাধান নয়?
   (ক) Promise (খ) Async-Await (গ) Generator functions (ঘ) var variable definition
   **উত্তরঃ ঘ**
৩. কলব্যাক হেলে এরর হ্যান্ডলিং করা কেন কঠিন?
   (ক) কারণ এরর ছড়ায় না (খ) প্রতিটি নেস্টেড ফাংশনে আলাদা করে এরর ক্যাচ করতে হয় (গ) ব্রাউজার এরর দেখায় না (ঘ) কোনোটিই নয়
   **উত্তরঃ খ**
৪. নিচের কোডটির রিডাবিলিটি স্কোর কেমন হতে পারে?
```javascript
a(x => b(y => c(z => console.log(z))));
```
   (ক) অত্যন্ত ভালো (খ) অত্যন্ত জটিল ও রিডাবিলিটি কম (গ) সিঙ্ক্রোনাস (ঘ) ফাস্ট পারফর্মিং
   **উত্তরঃ খ**
৫. মডার্ন জাভাস্ক্রিপ্ট ইঞ্জিনে অ্যাসিনক্রোনাস কাজগুলোকে আরও স্মুথ করতে ES6-এ কী যুক্ত হয়েছিল?
   (ক) prototype (খ) Promises (গ) closure (ঘ) arrow functions
   **উত্তরঃ খ**

### ১৭. 5 Coding exercises
১. একটি ডামি অ্যাসিনক্রোনাস লাইব্রেরি (যেমন ৩টি ফাংশন) বানিয়ে তাদের কলব্যাক হেলের মাধ্যমে কল করুন।
২. ১ নং অনুশীলনের কোডটি রি-ফ্যাক্টর করে আলাদা আলাদা Named Function ব্যবহার করে সুন্দর ও পঠনযোগ্য করুন।
৩. প্রমিজ ব্যবহার করে কলব্যাক হেলের কোডকে ফ্ল্যাট চেইনে (Flat Chain) রূপান্তর করুন।
৪. কাস্টম ইভেন্ট চালিত ৩টি অ্যাসিনক্রোনাস ধাপ কলব্যাক দিয়ে সিকোয়েন্সিয়ালি রান করুন।
৫. এমন একটি স্ক্রিপ্ট লিখুন যা দেখাবে কীভাবে কলব্যাক হেলের মাঝে কোনো ধাপে এরর হলে পুরো চেইনটি ব্রেক হয়ে যায়।

---

## ১৭. Promises and Creating a Promise

### ১. Simple definition (বাংলায়)
**Promise** হলো জাভাস্ক্রিপ্টের একটি বিশেষ অবজেক্ট যা কোনো অ্যাসিনক্রোনাস অপারেশনের চূড়ান্ত ফলাফল (সফলতা অথবা ব্যর্থতা) নির্দেশ করে। এটি মূলত ভবিষ্যতের একটি প্রতিশ্রুতি—কাজটি এখন ব্যাকগ্রাউন্ডে চলছে এবং শেষ হলে এটি সফল (Resolved) অথবা ব্যর্থ (Rejected) হবে।

### ২. Why this concept exists
কলব্যাক হেলের জটিলতা দূর করতে এবং অ্যাসিনক্রোনাস ইভেন্টগুলোকে একটি স্ট্যান্ডার্ড, চেইনেবল এবং সহজে হ্যান্ডেল করা যায় এমন অবজেক্ট ফর্মে রূপান্তর করতে প্রমিজের সৃষ্টি।

### ৩. What problem it solves
এটি অ্যাসিনক্রোনাস কোডের এরর হ্যান্ডলিং এবং কোড ফ্লোকে সিঙ্ক্রোনাস কোডের মতো পড়তে সাহায্য করে। এটি নিশ্চিত করে যে একটি কাজের পর আরেকটি কাজ চেইনের মতো ইজি ফ্লোতে চলতে পারবে।

### ৪. Real-life analogy
মনে করুন আপনি একটি রেস্টুরেন্টে গিয়ে খাবার অর্ডার করলেন। ওয়েটার আপনাকে একটি ডিভাইস বা বাজার (Buzzer) দিল এবং বলল খাবার রেডি হলে এটি ভাইব্রেট করবে। এই বাজারটিই হলো Promise। খাবার রেডি হলে বাজারটি সংকেত দেবে (Resolved)। আর যদি খাবার ফুরিয়ে যায়, তবে লাল লাইট জ্বলবে (Rejected)। এই মাঝখানের সময়ে আপনি ফ্রি হয়ে অন্য কাজ করতে পারেন।

### ৫. How React works internally regarding this concept
React 19 এবং আধুনিক সাসপেন্স (Suspense) এ প্রমিজের ব্যবহার অত্যন্ত বেশি। React 19-এর `use` হুকটি সরাসরি একটি প্রমিজ গ্রহণ করতে পারে। প্রমিজটি pending থাকা অবস্থায় React কম্পোনেন্ট রেন্ডারিং সাসপেন্ড (Suspend) করে এবং প্রমিজটি resolve হলে কম্পোনেন্ট রেন্ডার কমপ্লিট করে। এছাড়াও API ডাটা ফেচিংয়ের সব কাস্টম হুক প্রমিজের ওপর ভিত্তি করেই কাজ করে।

### ৬. Basic example
```javascript
const myPromise = new Promise((resolve, reject) => {
  const success = true;
  setTimeout(() => {
    if (success) {
      resolve("Data loaded successfully!");
    } else {
      reject("Error: Failed to load data.");
    }
  }, 1000);
});

myPromise
  .then(result => console.log(result))
  .catch(error => console.error(error));
```

### ৭. Step-by-step explanation of the code
১. `new Promise` কনস্ট্রাক্টর দিয়ে একটি নতুন প্রমিজ অবজেক্ট তৈরি করা হয়েছে যা একটি এক্সিকিউটর (Executor) ফাংশন নেয়।
২. এক্সিকিউটর ফাংশনটির দুটি আর্গুমেন্ট থাকে: `resolve` (সফল হলে কল করতে হয়) এবং `reject` (ব্যর্থ হলে কল করতে হয়)।
৩. `setTimeout` দিয়ে ১ সেকেন্ড পর কন্ডিশনাল চেক করা হয়। সত্য হলে `resolve()` কল করা হয়।
৪. প্রমিজটি কল করার সময় সফল ফলাফলটি ক্যাচ করতে আমরা `.then()` ব্যবহার করি এবং ব্যর্থতা ক্যাচ করতে `.catch()` ব্যবহার করি।

### ৮. Another real-world example (Fetch Simulation)
```javascript
function fetchProductData(productId) {
  return new Promise((resolve, reject) => {
    if (productId <= 0) {
      reject("Invalid Product ID");
    } else {
      resolve({ id: productId, name: "Laptop", price: 1200 });
    }
  });
}
```

### ৯. Common mistakes beginners make
* প্রমিজ কনস্ট্রাক্টরের ভেতরে `resolve` বা `reject` কল করতে ভুলে যাওয়া (এর ফলে প্রমিজটি চিরকাল `pending` অবস্থায় আটকে থাকে)।
* `.catch()` দিয়ে এরর হ্যান্ডল না করা, যার ফলে ব্রাউজারে `Uncaught (in promise)` লাল ওয়ার্নিং আসে।

### ১০. Interview questions related to this topic
* **Q:** Promise-এর তিনটি স্টেট বা অবস্থা কী কী?
  * **Ans:** ১. **Pending** (ইনিশিয়াল স্টেট, কাজ চলছে), ২. **Fulfilled / Resolved** (সফলভাবে শেষ হয়েছে), এবং ৩. **Rejected** (ব্যর্থতায় শেষ হয়েছে)।
* **Q:** Promise-এর স্টেট কী একবার সেট হওয়ার পর পরিবর্তন করা যায়?
  * **Ans:** না, প্রমিজ হলো ইমিউটেবল। একবার এটি Resolved বা Rejected হয়ে গেলে এর স্টেট আর কখনো পরিবর্তন করা যায় না।

### ১১. Best practices
* সবসময় প্রমিজ কল করার সময় `.catch()` ব্লক বা `try-catch` ব্যবহার করে এরর হ্যান্ডল করুন।
* বড় ক্যালকুলেশন প্রমিজের ভেতরে করলেও তা মেইন থ্রেডকে ব্লক করতে পারে, তাই দীর্ঘ মেয়াদী কাজের জন্য মেমোরি ডিস্ট্রিবিউশন ঠিক রাখুন।

### ১২. Performance considerations
* প্রতিবার `new Promise` তৈরি করলে মেমোরিতে একটি নতুন অবজেক্ট তৈরি হয়। অপ্রয়োজনে প্রমিজ রি-ক্রিয়েট না করে ক্যাশ বা প্রমিজ রেফারেন্স ব্যবহার করা ভালো।

### ১৩. When NOT to use it
* যদি লজিকটি সিঙ্ক্রোনাস এবং খুব সহজ হয়, তবে প্রমিজ ব্যবহার করে জটিলতা বাড়াবেন না।

### ১৪. Comparison with similar concepts
| বৈশিষ্ট্য | Callback | Promise |
| :--- | :--- | :--- |
| **স্টেট ট্র্যাকিং** | নেই (শুধু কোড এক্সিকিউট করে) | Pending, Fulfilled, Rejected |
| **চেইনিং** | জটিল (Nested) | সহজ ও সোজা (.then.then) |

### ১৫. Summary in simple Bangla
প্রমিজ হলো একটি আগাম প্রতিশ্রুতি, যা আপনাকে আশ্বস্ত করে যে কাজ ভালো হোক বা খারাপ, সে আপনাকে তার শেষ রেজাল্ট অবশ্যই জানাবে।

### ১৬. 5 MCQ questions
১. একটি প্রমিজের প্রারম্ভিক (Initial) স্টেট কোনটি?
   (ক) Resolved (খ) Pending (গ) Rejected (ঘ) Settled
   **উত্তরঃ খ**
২. প্রমিজ সফলভাবে সম্পন্ন হলে কোন ব্লকে ডেটা রিসিভ করা হয়?
   (ক) catch() (খ) resolve() (গ) then() (ঘ) finally()
   **উত্তরঃ গ**
৩. প্রমিজের কোনো এরর হলে তা ক্যাচ করার ব্লক কোনটি?
   (ক) catch() (খ) error() (গ) reject() (ঘ) finally()
   **উত্তরঃ ক**
৪. প্রমিজ সফল বা ব্যর্থ যাই হোক না কেন, কোন ব্লকটি সবসময় রান করবেই?
   (ক) then() (খ) catch() (গ) finally() (ঘ) complete()
   **উত্তরঃ গ**
৫. নিচের কোডটির আউটপুট কী হবে?
```javascript
const p = new Promise((res, rej) => {
  res(1);
  res(2);
});
p.then(val => console.log(val));
```
   (ক) 1 (খ) 2 (গ) 1 এবং 2 উভয়ই (ঘ) Error
   **উত্তরঃ ক (কারণ প্রমিজের স্টেট একবার সেট হয়ে গেলে আর পরিবর্তন হয় না)**

### ১৭. 5 Coding exercises
১. একটি প্রমিজ ফাংশন লিখুন যা র্যান্ডম সংখ্যা জেনারেট করবে এবং সংখ্যাটি ৫-এর বেশি হলে সফল এবং কম হলে ব্যর্থ হবে।
２. ইমেজ ইউআরএল লোড করার জন্য একটি প্রমিজ-ভিত্তিক লোডার ফাংশন তৈরি করুন।
৩. ৩টি ভিন্ন সময়ে রেজলভ হওয়া প্রমিজ তৈরি করুন এবং তাদের এক্সিকিউশন টাইম চেক করুন।
৪. `Promise.reject("Immediate Error")` বানিয়ে তা ক্যাচ করে কনসোলে প্রিন্ট করার কোড লিখুন।
৫. React 19 সাসপেন্স সিমুলেট করতে পেন্ডিং প্রমিজের একটি ডেমো স্ট্যাটাস ট্র্যাকার স্ক্রিপ্ট লিখুন।

---

## ১৮. Chaining & Error Handling

### ১. Simple definition (বাংলায়)
**Promise Chaining** হলো একের পর এক একাধিক `.then()` ব্লককে যুক্ত করে সিকোয়েন্সিয়াল বা ক্রমানুসার অ্যাসিনক্রোনাস কাজগুলো সম্পন্ন করার একটি মেথড। আর এই চেইনের যেকোনো স্থানে ঘটা এরর বা ব্যর্থতাকে একটিমাত্র `.catch()` ব্লকের সাহায্যে ক্যাচ করে সুন্দরভাবে হ্যান্ডেল করাকে **Error Handling** বলে।

### ২. Why this concept exists
বাস্তব জীবনে একটি অ্যাসিনক্রোনাস কাজের ফলাফলের ওপর ভিত্তি করে আরেকটি কাজ শুরু করতে হয়। প্রমিজ চেইনিং ডেভেলপারদের এই সিকোয়েন্স বজায় রাখার একটি অত্যন্ত পরিষ্কার ও পঠনযোগ্য উপায় দেয়, এবং একটিমাত্র কমন জায়গায় সব এরর ম্যানেজ করার সুবিধা দেয়।

### ৩. What problem it solves
এটি Callback Hell-এর বড় নেস্টিং স্ট্রাকচারকে সোজা ও ফ্ল্যাট লাইনে রূপান্তর করে। এর ফলে কোড পড়তে এবং মেইনটেইন করতে অত্যন্ত সুবিধা হয়।

### ৪. Real-life analogy
মনে করুন আপনি একটি অনলাইন অর্ডার ট্র্যাকিং করছেন। 
প্রথমে পেমেন্ট কনফার্ম হতে হবে (.then) -> পেমেন্ট হলে শিপিং লেবেল তৈরি হবে (.then) -> শিপিং হলে ট্র্যাকিং আইডি জেনারেট হবে (.then)। এই পুরো চেইনের যেকোনো জায়গায় যদি কোনো এরর হয় (যেমন পেমেন্ট ফেইলড), তবে চেইনটি সাথে সাথে ব্রেক হয়ে সিকিউরিটি গার্ডের কাছে এরর মেসেজ চলে যাবে (.catch)।

### ৫. How React works internally regarding this concept
React অ্যাপে একাধিক এপিআই রিকোয়েস্ট ডিপেন্ডেন্সি হ্যান্ডল করার সময় আমরা প্রমিজ চেইনিং করি। এছাড়াও গ্লোবাল এরর বাউন্ডারি (Error Boundary) তৈরি করার সময় রেন্ডার ফেজের এরর ট্র্যাক করা যায়, কিন্তু অ্যাসিনক্রোনাস এরর (যেমন এপিআই ফেইল) প্রমিজের `.catch()` বা `try-catch`-এর মাধ্যমেই রিয়্যাক্টকে জানাতে হয়, যাতে স্ক্রিন ক্র্যাশ না করে সুন্দর এরর মেসেজ UI দেখায়।

### ৬. Basic example
```javascript
// Promise Chaining and Error Handling
function add(value) {
  return Promise.resolve(value + 10);
}

add(5)
  .then(res1 => {
    console.log("Res 1:", res1); // 15
    return add(res1);
  })
  .then(res2 => {
    console.log("Res 2:", res2); // 25
    throw new Error("Something went wrong!");
  })
  .then(res3 => {
    console.log("Res 3:", res3); // Won't run
  })
  .catch(err => {
    console.error("Caught error:", err.message); // Caught error: Something went wrong!
  });
```

### ७. Step-by-step explanation of the code
১. `add(5)` কল করলে একটি রেজলভড প্রমিজ তৈরি হয় যার ভ্যালু ১৫।
২. প্রথম `.then` ব্লকে ১৫ প্রিন্ট হয় এবং সে আবার `add(15)` রিটার্ন করে যা ২৫ ভ্যালুসহ নতুন প্রমিজ দেয়।
৩. দ্বিতীয় `.then` ব্লকে ২৫ প্রিন্ট হয় এবং আমরা একটি কৃত্রিম এরর `throw` করেছি।
৪. এরর হওয়ার সাথে সাথে জাভাস্ক্রিপ্ট পরবর্তী সব `.then` ব্লক স্কিপ করে সরাসরি সবার নিচে থাকা `.catch` ব্লকে চলে যায় এবং এরর মেসেজ প্রিন্ট করে।

### ৮. Another real-world example (API Fetch Chaining)
```javascript
// Fetch user, then fetch posts, then handle errors
fetch("https://api.example.com/user")
  .then(res => res.json())
  .then(user => fetch(`https://api.example.com/posts?userId=${user.id}`))
  .then(res => res.json())
  .then(posts => console.log("User posts:", posts))
  .catch(err => console.error("API Call Failed:", err));
```

### ৯. Common mistakes beginners make
* কোনো `.then` ব্লকের ভেতরে পরবর্তী প্রমিজটি `return` করতে ভুলে যাওয়া (এর ফলে পরবর্তী `.then` ব্লকটি ভ্যালু ছাড়াই রান হয়ে যাবে)।
* চেইনের ওপরেই `.catch` দিয়ে ফেলা, যার ফলে এর নিচের `.then` ব্লকে কোনো এরর হলে তা আন-ক্যাপচারড থেকে যায়।

### ১০. Interview questions related to this topic
* **Q:** প্রমিজ চেইনে যদি কোনো `.then` ব্লকের ভেতরে এরর হয়, তবে এক্সিকিউশন ফ্লো কোথায় যায়?
  * **Ans:** এক্সিকিউশন ফ্লো চেইনের নিচের পরবর্তী নিকটতম `.catch()` ব্লকে চলে যায়।
* **Q:** `.then()` কি নিজে একটি প্রমিজ রিটার্ন করে?
  * **Ans:** হ্যাঁ, `.then()` সবসময় একটি নতুন প্রমিজ রিটার্ন করে, যা চেইনিংকে সম্ভব করে তোলে। আমরা যদি কোনো সাধারণ ভ্যালু রিটার্ন করি, তবে তা রেজলভড প্রমিজ হিসেবে পরবর্তী ব্লকে পাস হয়।

### ১১. Best practices
* চেইনের শেষে অবশ্যই একটি ক্যাচ ব্লক রাখুন।
* বড় চেইনগুলোর প্রতিটি ধাপে ডেটা ট্রান্সফর্মেশনের সময় রিটার্ন টাইপ নিশ্চিত করুন।

### ১২. Performance considerations
* অতিরিক্ত বড় চেইন কোড রিডাবিলিটি কমাতে পারে, তাই প্রয়োজনে বড় প্রমিজ চেইনকে ছোট ছোট ফাংশনে বিভক্ত করুন।

### ১৩. When NOT to use it
* যদি অ্যাসিনক্রোনাস কাজগুলোর মধ্যে কোনো ডিপেন্ডেন্সি না থাকে (অর্থাৎ তারা সমান্তরালে চলতে পারে), তবে চেইনিং না করে `Promise.all` ব্যবহার করুন।

### ১৪. Comparison with similar concepts
| মেকানিজম | নেস্টিং লেভেল | এরর বাবলিং |
| :--- | :--- | :--- |
| **Callback Chaining** | গভীর ও জটিল | জটিল (প্রতিটি স্তরে আলাদা) |
| **Promise Chaining** | ফ্ল্যাট ও সোজা | অত্যন্ত সহজ (স্বয়ংক্রিয় বাবলিং) |

### ১৫. Summary in simple Bangla
প্রমিজ চেইনিং হলো রিলে রেসের মতো ব্যাটন একজন থেকে অন্য জনের কাছে পাস করা, আর মাঝপথে কেউ পড়ে গেলে (Error) লাল পতাকা দেখিয়ে খেলা স্থগিত করা।

### ১৬. 5 MCQ questions
১. প্রমিজ চেইন একের পর এক ডেটা পাস করার প্রধান শর্ত কী?
   (ক) yield ব্যবহার করা (খ) প্রতি then ব্লকে মান বা প্রমিজ return করা (গ) console.log ব্যবহার করা (ঘ) await ব্যবহার করা
   **উত্তরঃ খ**
২. নিচের কোডটির আউটপুট কী হবে?
```javascript
Promise.resolve(10)
  .then(x => x + 2)
  .then(y => { throw "Error" })
  .catch(err => 20)
  .then(z => console.log(z));
```
   (ক) Error (খ) 20 (গ) 12 (ঘ) undefined
   **উত্তরঃ খ (যেহেতু catch-এর রিটার্ন ভ্যালু পরবর্তী then-এ পাস হয়)**
৩. নিচের কোনটি প্রমিজ চেইনে এরর ক্যাচ করার জন্য ব্যবহৃত হয়?
   (ক) catch() (খ) throw() (গ) error() (ঘ) complete()
   **উত্তরঃ ক**
４. `.catch()` ব্লক চেইনের কোথায় রাখা বুদ্ধিমানের কাজ?
   (ক) চেইনের শুরুতে (খ) প্রতিটি then-এর ঠিক পরে (গ) চেইনের একদম শেষে (ঘ) যেকোনো জায়গায় রাখলেই হয়
   **উত্তরঃ গ**
৫. প্রমিজ চেইনে প্রমিজ ফেইল হওয়ার প্রক্রিয়াকে কী বলে?
   (ক) Error Shadowing (খ) Error Bubbling/Propagation (গ) Stack Tracing (ঘ) Promise Rejection Hook
   **উত্তরঃ খ**

### ১৭. 5 Coding exercises
১. একটি প্রমিজ চেইন লিখুন যা প্রথমে একটি সংখ্যাকে ২ দিয়ে গুণ করবে, তারপর ৩ যোগ করবে, এবং শেষে ৫ বিয়োগ করে ফাইনাল রেজাল্ট প্রিন্ট করবে।
২. ১ নং কোডের মাঝে ইচ্ছাকৃতভাবে একটি এরর তৈরি করুন এবং দেখান কীভাবে চেইনের শেষ ক্যাচ ব্লক তা হ্যান্ডেল করে।
৩. চেইনের মাঝামাঝি একটি ক্যাচ ব্লক দিয়ে এরর রিকভার করার উদাহরণ তৈরি করুন (অর্থাৎ এরর হলেও পরবর্তী then রান করবে)।
৪. ৩টি এপিআই ডাটা রিকোয়েস্ট সিকোয়েন্সিয়ালি চেইন করুন এবং তাদের লোডিং ও এরর স্ট্যাটাস ট্র্যাক করুন।
৫. নিচের কোডটিতে কী ভুল আছে তা চিহ্নিত করে ঠিক করুন:
```javascript
// fetch().then(res => { res.json() }).then(data => console.log(data))
```

---

## ১৯. async await

### ১. Simple definition (বাংলায়)
**async/await** হলো জাভাস্ক্রিপ্টে প্রমিজ বা অ্যাসিনক্রোনাস কোড হ্যান্ডেল করার একটি অত্যন্ত সহজ ও আধুনিক সিনট্যাক্স। এটি মূলত প্রমিজের ওপরে থাকা একটি "সিনট্যাক্টিক সুগার" (Syntactic Sugar), যা কোডকে দেখতে পুরোপুরি সিঙ্ক্রোনাস কোডের মতো সোজা ও পঠনযোগ্য করে তোলে।

### ২. Why this concept exists
`.then()` এবং `.catch()` দিয়ে প্রমিজ চেইনিং করলে কোড ক্লিন হলেও অনেক সময় ব্র্যাকেট এবং কমা চেইনের জটলা তৈরি হয়। অ্যাসিনক্রোনাস কোড লেখার এই অভিজ্ঞতাকে আরও মসৃণ ও প্রাকৃতিক করতেই ES8-এ `async/await` নিয়ে আসা হয়।

### ৩. What problem it solves
এটি প্রমিজ কোডের জটিল নেস্টিং দূর করে এবং ট্র্যাডিশনাল `try-catch` ব্লক ব্যবহার করে এরর হ্যান্ডলিং করার ক্লাসিক সুবিধা ফিরিয়ে দেয়।

### ৪. Real-life analogy
মনে করুন আপনার বস আপনাকে একটি রিপোর্ট তৈরি করতে বললেন। প্রমিজ চেইন ব্যবহারের স্টাইল হলো: বস আপনাকে নির্দেশ দিয়ে চলে গেলেন এবং আপনি রিপোর্ট বানানোর পর অন্য আরেকজনকে গিয়ে বললেন তা বসের টেবিলে রাখতে। আর `async/await` হলো বস আপনার সামনে এসে দাঁড়ালেন (await) এবং বললেন, "তুমি রিপোর্ট বানাও, আমি এখানেই দাঁড়ালাম। কাজ শেষ হলে রিপোর্টটি নিয়ে আমি চলে যাব।"

### ৫. How React works internally regarding this concept
React-এর ডাটা ফেচিং এবং ইভেন্ট হ্যান্ডলিং লজিকে `async/await` ব্যাপকভাবে ব্যবহৃত হয়। 
> [!IMPORTANT]
> React-এর `useEffect` হুকের মেইন কলব্যাককে সরাসরি `async` বানানো যায় না (যেমন: `useEffect(async () => {})` অবৈধ), কারণ React-এর `useEffect` ক্লিনআপ ফাংশন রিটার্ন করার কথা কিন্তু আসিংক ফাংশন রিটার্ন করে প্রমিজ। তাই হুকের ভেতরে আলাদা করে আসিংক ফাংশন ডিফাইন করে কল করতে হয়।
```jsx
useEffect(() => {
  async function loadData() {
    const data = await fetchData();
    setData(data);
  }
  loadData();
}, []);
```

### ৬. Basic example
```javascript
const getMockData = () => new Promise(res => setTimeout(() => res("Hello Await!"), 1000));

async function execute() {
  console.log("Fetching...");
  const result = await getMockData(); // Pauses execution until promise resolves
  console.log(result);
}

execute();
```

### ৭. Step-by-step explanation of the code
১. `execute` ফাংশনের শুরুতে `async` কিওয়ার্ড ব্যবহারের ফলে এটি একটি বিশেষ অ্যাসিনক্রোনাস ফাংশন হিসেবে চিহ্নিত হয়।
২. ফাংশনের ভেতরে `await` কিওয়ার্ডটি জাভাস্ক্রিপ্ট ইঞ্জিনকে বলে যে, `getMockData` প্রমিজটি সম্পন্ন না হওয়া পর্যন্ত পরবর্তী লাইনে যেও না।
৩. প্রমিজটি ১ সেকেন্ড পর রেজলভ হলে রেজাল্টটি সরাসরি `result` ভ্যারিয়েবলে স্টোর হয় এবং কনসোলে প্রিন্ট হয়।
৪. এই ওয়েটিং টাইমে ব্রাউজারের মেইন থ্রেড কিন্তু ফ্রি থাকে, এটি মোটেও ব্লকড হয় না।

### ৮. Another real-world example (Error handling in React API Call)
```javascript
async function loadUserData(userId) {
  try {
    const response = await fetch(`https://api.example.com/user/${userId}`);
    if (!response.ok) throw new Error("Network response was not ok");
    const user = await response.json();
    return user;
  } catch (error) {
    console.error("Failed to load user:", error);
  }
}
```

### ৯. Common mistakes beginners make
* `await` ব্যবহার করতে গিয়ে আউটার ফাংশনে `async` দিতে ভুলে যাওয়া (যার ফলে SyntaxError হবে)।
* `try-catch` ব্লক ছাড়া `await` ব্যবহার করা, যার ফলে প্রমিজ রিজেক্ট হলে অ্যাপ ক্র্যাশ করবে।
* দুটি স্বাধীন বা ইন্ডিপেন্ডেন্ট প্রমিজকে পরপর অহেতুক `await` করে রিকোয়েস্ট ওয়াটারফল (Waterfall) তৈরি করা।

### ১০. Interview questions related to this topic
* **Q:** `async` ফাংশন ডিফল্টভাবে কী রিটার্ন করে?
  * **Ans:** `async` ফাংশন ডিফল্টভাবে সবসময় একটি **Promise** রিটার্ন করে। আপনি যদি সাধারণ ভ্যালুও রিটার্ন করেন, তা প্রমিজে মোড়ানো থাকে।
* **Q:** `await` কি জাভাস্ক্রিপ্টের এক্সিকিউশন স্ট্যাককে ব্লক করে দেয়?
  * **Ans:** না, এটি শুধুমাত্র ওই নির্দিষ্ট `async` ফাংশনের কোড এক্সিকিউশন পজ বা সাময়িক স্থগিত করে, কিন্তু মূল ব্রাউজার থ্রেড বা কল স্ট্যাক অন্য সব কাজ স্বাভাবিকভাবে চালাতে পারে।

### ১১. Best practices
* সবসময় `async/await` কোডকে `try-catch` ব্লকের ভেতরে রাখুন।
* সমান্তরাল কাজের জন্য `await Promise.all()` ব্যবহার করুন।

### ১২. Performance considerations
* রিকোয়েস্ট ওয়াটারফল এড়াতে একাধিক এপিআই রিকোয়েস্ট থাকলে আগে প্রমিজগুলো ফায়ার করে নিয়ে পরে একসাথে `await` করুন।

### ১৩. When NOT to use it
* যদি কোনো সিঙ্ক্রোনাস ডেটা প্রসেসিং লজিক হয়, তবে অযথা `async/await` ব্যবহার করবেন না।

### ১৪. Comparison with similar concepts
| মেকানিজম | সিনট্যাক্স জটিলতা | এরর হ্যান্ডলিং টাইপ |
| :--- | :--- | :--- |
| **Promises (.then)** | মাঝারি | `.catch()` |
| **Async / Await** | অত্যন্ত সহজ | `try / catch` |

### ১৫. Summary in simple Bangla
`async/await` হলো প্রমিজ চেইনিংয়ের সুন্দর রূপ, যার ফলে আমাদের জটিল ও বাঁকা অ্যাসিনক্রোনাস কোড দেখতে চমৎকার সোজা ও সরল সিঙ্ক্রোনাস কোডের মতো মনে হয়।

### ১৬. 5 MCQ questions
১. React-এর `useEffect` হুকের মূল ফাংশনকে সরাসরি `async` ডিফাইন করা যায় না কেন?
   (ক) React আসিংক বোঝে না (খ) আসিংক ফাংশন প্রমিজ রিটার্ন করে, যা ক্লিনআপ ফাংশন রুল ব্রেক করে (গ) পারফরম্যান্স কমে যায় (ঘ) এরর বাউন্ডিং কাজ করে না
   **উত্তরঃ খ**
২. `async` ফাংশনের ভেতর সাধারণ কোনো রিটার্ন ভ্যালু কী হিসেবে ফেরত আসে?
   (ক) String (খ) Resolved Promise (গ) undefined (ঘ) rejected promise
   **উত্তরঃ খ**
৩. `await` শুধুমাত্র কার ভেতরে ব্যবহার করা যায়?
   (ক) সাধারণ ফাংশনে (খ) async ফাংশনে (গ) গ্লোবাল স্কোপে (ঘ) ব্লক স্কোপে
   **উত্তরঃ খ (Top-level await মডিউলে চললেও সাধারণ ফাংশনের ক্ষেত্রে async মাস্ট)**
৪. নিচের কোডের আউটপুট কী হবে?
```javascript
async function show() {
  return "Done";
}
console.log(show());
```
   (ক) "Done" (খ) Promise { 'Done' } (গ) undefined (ঘ) Error
   **উত্তরঃ খ**
৫. আসিংক-অ্যাওয়েট কোডের এরর হ্যান্ডলিংয়ের জন্য কোন ব্লকটি ব্যবহৃত হয়?
   (ক) then-catch (খ) try-catch (গ) error-catch (ঘ) complete-block
   **উত্তরঃ খ**

### ১৭. 5 Coding exercises
১. একটি অ্যাসিনক্রোনাস ফাংশন লিখুন যা পরপর ৩টি ভিন্ন সংখ্যার প্রমিজকে `await` করবে এবং যোগফল দেবে।
২. ১ নং অনুশীলনের কোডটি রি-ফ্যাক্টর করে প্যারালাল বা সমান্তরালভাবে রান করুন (রানিং ওয়াটারফল দূর করুন)।
৩. এমন একটি কোড লিখুন যা দেখাবে কীভাবে `fetch` কলের কোনো এরর হলে তা `try-catch` ক্যাচ করে কাস্টম মেসেজ দেবে।
৪. React-এর `useEffect` এর ভেতরের ডেটা ফেচিং মেথডটির হুবহু একটি ডেমো কোড লিখুন।
৫. নিচের কোডটির এক্সিকিউশন ফ্লো লাইন বাই লাইন বুঝিয়ে লিখুন:
```javascript
async function task() {
  console.log("A");
  await Promise.resolve();
  console.log("B");
}
task();
console.log("C");
```

---

## ২০. Promise APIs (all, allSettled, race, any)

### ১. Simple definition (বাংলায়)
জাভাস্ক্রিপ্টে সমান্তরালভাবে বা একসাথে অনেকগুলো প্রমিজ নিয়ে কাজ করার জন্য ৪টি বিশেষ এপিআই রয়েছে:
১. **Promise.all:** সব প্রমিজ সফল হলে রেজাল্ট অ্যারে দেয়, যেকোনো একটি ব্যর্থ হলেই সরাসরি রিজেক্ট হয়।
২. **Promise.allSettled:** সফল বা ব্যর্থ যাই হোক না কেন, সব প্রমিজ শেষ হওয়ার পর প্রতিটির অবস্থা ও রেজাল্টের অ্যারে দেয়।
৩. **Promise.race:** সবার আগে যে প্রমিজটি ফিনিশ লাইন টাচ করে (হক তা সফল বা ব্যর্থ), তার রেজাল্ট দেয়।
৪. **Promise.any:** সবার আগে যে প্রথম প্রমিজটি সফল বা রেজলভ হয়, তার রেজাল্ট দেয়। সব ব্যর্থ হলে তবেই রিজেক্ট হয়।

### ২. Why this concept exists
বাস্তব ওয়েব অ্যাপে আমাদের অনেক সময় একসাথে একাধিক এপিআই রিকোয়েস্ট ফায়ার করতে হয়। একটি একটি করে অপেক্ষা না করে একসাথে সমান্তরালভাবে পাঠিয়ে পারফরম্যান্স বুস্ট করার জন্য এই এপিআইগুলো দরকার।

### ৩. What problem it solves
এটি রিকোয়েস্ট ওয়াটারফল (Sequential Delay) কমায়। অ্যাপ লোডিং টাইম অনেক কমিয়ে দেয়।

### ৪. Real-life analogy
* **Promise.all:** আপনার বন্ধুদের একটি গ্রুপ পিকনিক করতে যাবে। গাড়িতে সবাই না উঠলে গাড়ি ছাড়বে না (All success required)। একজন ফেইল করলেই ট্রিপ বাতিল।
* **Promise.allSettled:** পিকনিক শেষে কে কে সুস্থ ফিরে এল আর কে কে অসুস্থ হলো, তার একটি কমপ্লিট লিস্ট সবাই ফিরে আসার পর নেওয়া।
* **Promise.race:** বন্ধুদের মধ্যে রেস হচ্ছে। সবার আগে যে ফিনিশ লাইনে পৌঁছাবে সে প্রথম, সে ফাস্ট হয়ে ট্রফি নিক বা হোঁচট খেয়ে বাদ পড়ুক।
* **Promise.any:** বন্ধুরা জিপিএস-এ একটি টেক্সি ডাকছে। যে কোনো একটি টেক্সি আসলেই হলো (First success)। সবাই রিজেক্ট করলেই শুধু যাত্রা বাতিল।

### ৫. How React works internally regarding this concept
React-এর ড্যাশবোর্ডগুলোতে যখন আমরা একাধিক উইজেটের ডেটা আনি (যেমন: ইউজারের প্রোফাইল, নোটিফিকেশন, এবং অ্যাক্টিভিটি লগ), তখন আমরা এই এপিআইগুলো ব্যবহার করি। 
```jsx
// Dashboard Loading in React
useEffect(() => {
  Promise.all([fetchProfile(), fetchNotifications(), fetchLogs()])
    .then(([profile, notifications, logs]) => {
      setProfile(profile);
      setNotifications(notifications);
      setLogs(logs);
    });
}, []);
```
এটি আলাদা আলাদাভাবে ওয়েট করার চেয়ে তিন গুণ দ্রুত উইজেটগুলোর ডেটা এনে দেয়।

### ৬. Basic example
```javascript
const p1 = new Promise(res => setTimeout(() => res("P1 Success"), 1000));
const p2 = new Promise((res, rej) => setTimeout(() => rej("P2 Fail"), 500));
const p3 = new Promise(res => setTimeout(() => res("P3 Success"), 1500));

// Promise.allSettled
Promise.allSettled([p1, p2, p3]).then(results => console.log(results));
/* Output:
[
  { status: 'fulfilled', value: 'P1 Success' },
  { status: 'rejected', reason: 'P2 Fail' },
  { status: 'fulfilled', value: 'P3 Success' }
]
*/
```

### ৭. Step-by-step explanation of the code
১. আমরা তিনটি প্রমিজ নিয়েছি যা ভিন্ন ভিন্ন সময়ে সাকসেস বা ফেইল হবে।
২. `Promise.allSettled` রান করলে সে সবার কাজ শেষ হওয়ার জন্য অপেক্ষা করে (সর্বোচ্চ ১৫০০ মিলি সেকেন্ড)।
৩. সব কাজ শেষ হলে সে একটি অবজেক্ট অ্যারে দেয় যেখানে প্রতিটি প্রমিজের স্ট্যাটাস (fulfilled/rejected) এবং সংশ্লিষ্ট value/reason পাওয়া যায়।
৪. এখানে `p2` ফেইল হওয়া সত্ত্বেও পুরো কলটি বাতিল হয়নি, এটি অন্য সফল ডেটাগুলোও ধরে রেখেছে।

### ৮. Another real-world example (Race for Timeout)
```javascript
// Fetch data with a timeout limit
const fetchRequest = fetch("https://api.example.com/data");
const timeoutRequest = new Promise((_, rej) => setTimeout(() => rej("Request Timeout"), 3000));

Promise.race([fetchRequest, timeoutRequest])
  .then(data => console.log("Data loaded", data))
  .catch(err => console.error("Error or timeout occurred:", err));
```
এটি ৩ সেকেন্ডের মধ্যে ডাটা লোড না হলে রিকোয়েস্ট বাতিল করার একটি দুর্দান্ত রিয়েল-ওয়ার্ল্ড টেকনিক।

### ৯. Common mistakes beginners make
* `Promise.all` ব্যবহারের সময় যেকোনো একটি প্রমিজ রিজেক্ট হলে বাকি সফল প্রমিজগুলোর রেসপন্স হারিয়ে ফেলা।
* `Promise.any` ব্যবহারের পর সব ফেইল করলে যে `AggregateError` আসে, তা হ্যান্ডেল না করা।

### ১০. Interview questions related to this topic
* **Q:** `Promise.all` এবং `Promise.allSettled` এর মূল পার্থক্য কী?
  * **Ans:** `Promise.all` এর কোনো একটি প্রমিজ ফেইল করলেই সম্পূর্ণ প্রমিজটি তাত্ক্ষণিকভাবে ফেইল করে। আর `Promise.allSettled` সব প্রমিজের রেজাল্টের জন্য শেষ পর্যন্ত ওয়েট করে, কাজ ফেইল করলেও সফল ডেটাগুলো রিটার্ন করে।
* **Q:** `Promise.any` কি ফেইল হওয়া প্রমিজকে রিটার্ন করতে পারে?
  * **Ans:** না, `Promise.any` শুধুমাত্র সফল হওয়া প্রথম প্রমিজকে রিটার্ন করে। এটি ফেইল করা প্রমিজগুলোকে স্কিপ করে। যদি সব প্রমিজ ফেইল করে তবেই এটি এরর দেয়।

### ১১. Best practices
* যদি আপনার ড্যাশবোর্ডের উইজেটগুলো ইন্ডিপেন্ডেন্ট হয় (একটি ফেইল করলেও অন্যটি সচল থাকবে), তবে সবসময় `Promise.all` এর পরিবর্তে `Promise.allSettled` ব্যবহার করুন।

### ১২. Performance considerations
* সমান্তরাল রিকোয়েস্ট পাঠানোর সময় ব্যাকএন্ড সার্ভারের লোড লিমিটের কথা মাথায় রাখুন, একসাথে অনেক প্রমিজ পাঠালে সার্ভার রেসপন্স ডিলে হতে পারে।

### १३. When NOT to use it
* যদি আপনার কাজগুলো সিকোয়েন্সিয়াল হয় (যেমন কাজ ২ করার জন্য কাজ ১ এর রেজাল্ট লাগবেই), তবে প্রমিজ এপিআই দিয়ে প্যারালাল করার কোনো সুযোগ নেই।

### ১৪. Comparison with similar concepts
| এপিআই | শর্ত | রিটার্ন ভ্যালু |
| :--- | :--- | :--- |
| **Promise.all** | সব সফল হতে হবে | রেজাল্টের অ্যারে |
| **Promise.allSettled** | সব সম্পন্ন হতে হবে (সফল/ব্যর্থ) | স্ট্যাটাস সহ অবজেক্ট অ্যারে |
| **Promise.race** | প্রথম যেকোনো একটি (সফল বা ব্যর্থ) | ১ম প্রমিজের রেজাল্ট |
| **Promise.any** | ১ম সফল প্রমিজ | ১ম সফল প্রমিজের রেজাল্ট |

### ১৫. Summary in simple Bangla
প্রমিজ এপিআইগুলো হলো সমান্তরাল কাজের টিম ম্যানেজার। কেউ টিম মেম্বারদের সব সাকসেস খোঁজে (all), কেউ সবার ভালো-মন্দ রিপোর্ট নেয় (allSettled), আর কেউ দ্রুত দৌড়ানো ফাস্ট মেম্বারকে ট্রিট দেয় (race/any)।

### ১৬. 5 MCQ questions
১. নিচের কোন এপিআই-তে একটি প্রমিজ ফেইল করলেই পুরো সেট ফেইল করে?
   (ক) Promise.any (খ) Promise.race (গ) Promise.all (ঘ) Promise.allSettled
   **উত্তরঃ গ**
২. `Promise.race` এ কোনটি রিটার্ন হয়?
   (ক) প্রথম সফল প্রমিজ (খ) প্রথম ব্যর্থ প্রমিজ (গ) সবার আগে নিষ্পত্তি হওয়া (সফল বা ব্যর্থ) প্রমিজ (ঘ) সবগুলো প্রমিজ
   **উত্তরঃ গ**
৩. যদি `Promise.any` এ দেওয়া সব প্রমিজ ব্যর্থ হয়, তবে কোন এররটি পাওয়া যায়?
   (ক) TypeError (খ) AggregateError (গ) ReferenceError (ঘ) RangeError
   **উত্তরঃ খ**
৪. ড্যাশবোর্ডের ডেটা লোড করার সময় যেখানে একটি কম্পোনেন্ট ফেইল করলেও অন্যটি ঠিক থাকা উচিত, সেখানে কোনটি বেস্ট অপশন?
   (ক) Promise.all (খ) Promise.allSettled (গ) Promise.race (ঘ) Promise.any
   **উত্তরঃ খ**
৫. নিচের কোডটির আউটপুট কী হবে?
```javascript
const p1 = Promise.reject("Err 1");
const p2 = Promise.resolve("Success 2");
Promise.any([p1, p2]).then(val => console.log(val));
```
   (ক) Err 1 (খ) Success 2 (গ) AggregateError (ঘ) Error
   **উত্তরঃ খ (যেহেতু any প্রথম সফল প্রমিজ খোঁজে)**

### ১৭. 5 Coding exercises
১. ৩টি ডামি API কল তৈরি করুন এবং `Promise.all` দিয়ে তাদের রেজাল্ট একসাথে কনসোলে শো করুন।
২. ১ নং অনুশীলনের যেকোনো একটি প্রমিজ রিজেক্ট করে এরর হ্যান্ডল করুন।
৩. `Promise.allSettled` ব্যবহার করে ৩টি এপিআই কলের রেসপন্সের সফল এবং ব্যর্থতার রেশিও ক্যালকুলেট করুন।
৪. ৫ সেকেন্ডের জন্য একটি রিকোয়েস্টTimeout সেট করে `Promise.race` ইমপ্লিমেন্ট করুন।
৫. ৪টি ইমেজ সোর্স ইউআরএল নিয়ে `Promise.any` ব্যবহার করে যেকোনো একটি ফাস্টেস্ট সোর্স থেকে ইমেজ রেন্ডার করার ডেমো কোড লিখুন।

---

## ২১. this keyword in JavaScript

### ১. Simple definition (বাংলায়)
জাভাস্ক্রিপ্টে **this** হলো একটি বিশেষ কিওয়ার্ড যা নির্দেশ করে বর্তমান এক্সিকিউশন লাইন বা কোড ব্লকটি কোন অবজেক্টের আন্ডারে রান হচ্ছে। এটি কোনো ফিক্সড বা স্ট্যাটিক ভ্যালু নয়; এটি ফাংশনটি কোথায় এবং কীভাবে কল বা এক্সিকিউট করা হয়েছে, তার ওপর ডাইনামিকালি নির্ধারিত হয়।

### ২. Why this concept exists
অবজেক্ট ওরিয়েন্টেড প্রোগ্রামিং (OOP) এ কোড রি-ইউজেবিলিটি বাড়ানোর জন্য এটি অত্যন্ত জরুরি। মেথডগুলো যাতে সহজেই তাদের প্যারেন্ট অবজেক্টের অন্যান্য প্রপার্টি এবং ডাটা অ্যাক্সেস করতে পারে, সেজন্য এটি ব্যবহৃত হয়।

### ৩. What problem it solves
এটি মেথড ডুপ্লিকেশন কমায়। আমরা একই মেথড ভিন্ন ভিন্ন অবজেক্টের ওপর ডাইনামিকালি কল করতে পারি `this` কিওয়ার্ডের কল্যাণে।

### ৪. Real-life analogy
মনে করুন আপনার হাতে একটি আইডি কার্ড আছে। আপনি যদি কাস্টমার হন, তবে কার্ডটি দেখাবে আপনার নাম ও কাস্টমার আইডি। আপনি যদি কোম্পানির কর্মচারী হন, তবে একই টাইপের কার্ডটি দেখাবে আপনার এমপ্লয়ি আইডি। এখানে আইডি কার্ড হলো `this`, যা ইউজারের কনটেক্সট পরিবর্তনের সাথে সাথে তার আচরণ পরিবর্তন করে।

### ৫. How React works internally regarding this concept
React-এর পুরোনো Class Component-এ `this` ছিল পুরো লাইফ সাইকেল ম্যানেজমেন্টের চাবিকাঠি। `this.state`, `this.setState`, `this.props` দিয়ে সবকিছু হ্যান্ডেল করা হতো। তবে সবচেয়ে বড় জটিলতা তৈরি হতো মেথড বাইন্ডিংয়ে। জাভাস্ক্রিপ্টের রুল অনুযায়ী, কোনো মেথডকে যখন অন্য কোনো অবজেক্টে (যেমন React-এর নিজস্ব ইভেন্ট সিস্টেমে) পাস করা হতো, তখন তার `this` হারিয়ে যেত। এই সমস্যা এড়াতে কনস্ট্রাক্টরে `this.handleClick = this.handleClick.bind(this)` বা অ্যারো ফাংশন ব্যবহার করতে হতো।

### ৬. Basic example
```javascript
const user = {
  name: "Amit",
  greet: function() {
    console.log(`Hello, my name is ${this.name}`);
  }
};

user.greet(); // Hello, my name is Amit

const looseGreet = user.greet;
looseGreet(); // Hello, my name is undefined (or empty due to window.name)
```

### ৭. Step-by-step explanation of the code
১. `user.greet()` রান করার সময় `greet` ফাংশনটি `user` অবজেক্টের সাপেক্ষে কল করা হয়েছে। তাই এখানে `this` পয়েন্ট করেছে `user` অবজেক্টকে এবং `this.name` এসেছে "Amit"।
২. কিন্তু `looseGreet = user.greet` লাইনে আমরা ফাংশনের মেমোরি রেফারেন্সটি আরেকটি ভ্যারিয়েবলে স্টোর করেছি।
৩. যখন `looseGreet()` একা একা কল করা হয়, তখন এর সাথে কোনো অবজেক্ট লিংকড ছিল না। তাই এটি গ্লোবাল উইন্ডো বা strict mode-এ `undefined` কে `this` হিসেবে ধরে নিয়েছে।
৪. উইন্ডো অবজেক্টে `name` প্রপার্টি না থাকায় আউটপুট `undefined` এসেছে।

### ৮. Another real-world example (Explicit binding using call, apply, bind)
```javascript
const person1 = { name: "Rohit" };
const person2 = { name: "Shakib" };

function sayHello(greeting) {
  console.log(`${greeting}, I am ${this.name}`);
}

sayHello.call(person1, "Hello"); // Hello, I am Rohit
sayHello.call(person2, "Hi"); // Hi, I am Shakib
```

### ৯. Common mistakes beginners make
* অ্যারো ফাংশনের ভেতরে `this` ব্যবহার করে ভাবা যে এটি কলিং অবজেক্টকে ধরবে (অ্যারো ফাংশনের নিজস্ব `this` থাকে না, এটি তার লেক্সিকাল প্যারেন্ট থেকে `this` ধার করে)।
* ইভেন্ট লিসেনারের ভেতরে `this` ব্যবহার করে আশা করা যে এটি মূল ক্লাসের রেফারেন্স দেবে (এটি আসলে ডম এলিমেন্টকে নির্দেশ করে)।

### ১০. Interview questions related to this topic
* **Q:** `call`, `apply` এবং `bind` এর মধ্যে পার্থক্য কী?
  * **Ans:** `call` ফাংশনকে তাত্ক্ষণিকভাবে আর্গুমেন্ট কমা দিয়ে পাস করে কল করে। `apply` একই কাজ করে কিন্তু আর্গুমেন্টগুলো অ্যারো বা অ্যারে আকারে নেয়। আর `bind` ফাংশনটি রান করে না, বরং চিরকালের জন্য কনটেক্সট লক করে একটি নতুন ফাংশন রিটার্ন করে।
* **Q:** অ্যারো ফাংশনে `this` কীভাবে কাজ করে?
  * **Ans:** অ্যারো ফাংশনের নিজস্ব কোনো `this` নেই। এটি তৈরির সময় তার চারপাশের লেক্সিকাল প্যারেন্ট স্কোপ থেকে `this` এর মান গ্রহণ করে এবং এটি ডাইনামিকালি পরিবর্তন করা যায় না।

### ১১. Best practices
* মডার্ন React-এ ফাংশনাল কম্পোনেন্ট এবং হুক্স ব্যবহার করুন, যা `this` সম্পর্কিত ৯০% জটিলতা দূর করে।
* যদি অবজেক্ট ওরিয়েন্টেড ডিজাইন করতেই হয়, তবে কনটেক্সট সুরক্ষিত রাখতে অ্যারো ফাংশন ব্যবহার করতে পারেন।

### ১২. Performance considerations
* বারবার রানটাইমে `bind` কল করলে নতুন ফাংশন অবজেক্ট তৈরি হয়, যা মেমোরির ওপর চাপ তৈরি করে। ক্লাসের ক্ষেত্রে কনস্ট্রাক্টরে একবার বাইন্ড করাই নিয়ম।

### १३. When NOT to use it
* সাধারণ ইউটিলিটি বা পিউর ম্যাথমেটিক্যাল ফাংশনে `this` ব্যবহার করার কোনো প্রয়োজন নেই।

### ১৪. Comparison with similar concepts
| মেথড | আর্গুমেন্ট পাসিং | রান করার সময় |
| :--- | :--- | :--- |
| **call** | কমা সেপারেটেড (arg1, arg2...) | তাত্ক্ষণিক |
| **apply** | অ্যারে বা অ্যারেলিস্ট ([arg1, arg2...]) | তাত্ক্ষণিক |
| **bind** | কমা সেপারেটেড | ভবিষ্যতে (রিটার্নস ফাংশন) |

### ১৫. Summary in simple Bangla
`this` হলো একটি আয়না, যা যেখানে দাঁড়িয়ে আছে সেই চারপাশের পরিবেশকে প্রতিফলিত করে। এটি কার বাড়িতে দাঁড়িয়ে আছে, তার ওপর ভিত্তি করে এর রূপ বদলায়।

### ১৬. 5 MCQ questions
১. গ্লোবাল স্কোপে strict mode অন থাকলে সাধারণ ফাংশনের ভেতরে `this` এর মান কী হয়?
   (ক) window (খ) undefined (গ) null (ঘ) Object
   **উত্তরঃ খ**
২. অ্যারো ফাংশনের ভেতরে `this` এর মান কী দ্বারা নির্ধারিত হয়?
   (ক) runtime call location (খ) lexical scope/parent scope (গ) global window (ঘ) target prototype
   **উত্তরঃ খ**
৩. নিচের কোডটির আউটপুট কী হবে?
```javascript
const obj = {
  x: 10,
  show: () => console.log(this.x)
};
obj.show();
```
   (ক) 10 (খ) undefined (গ) Error (ঘ) null
   **উত্তরঃ খ (যেহেতু অ্যারো ফাংশন lexical this ব্যবহার করে যা গ্লোবাল উন্ডোকে পয়েন্ট করছে)**
৪. কোন মেথডটি চিরতরে একটি অবজেক্টকে মেথডের `this` হিসেবে লক করে নতুন ফাংশন দেয়?
   (ক) call (খ) apply (গ) bind (ঘ) lock
   **উত্তরঃ গ**
৫. `call` এবং `apply` এর প্রধান তফাৎ কোথায়?
   (ক) স্পিডে (খ) মেমোরি ব্যবহারে (গ) আর্গুমেন্ট পাস করার সিনট্যাক্সে (ঘ) ব্রাউজার সাপোর্টে
   **উত্তরঃ গ**

### ১৭. 5 Coding exercises
১. একটি ক্যালকুলেটর অবজেক্ট তৈরি করুন যা `this` ব্যবহার করে মান যোগ-বিয়োগ করবে।
২. `call` এবং `apply` ব্যবহার করে একই মেথড দুটি ভিন্ন ইউজার অবজেক্টের ওপর রান করান।
৩. একটি বাটন ক্লিকে `bind` ব্যবহার করে অবজেক্টের মেথড ফায়ার করার কোড লিখুন।
৪. এমন একটি সিনারিও ডেমো করুন যেখানে অ্যারো ফাংশন এবং সাধারণ ফাংশন মেথডের `this` নিয়ে ভিন্ন ভিন্ন ফলাফল দেয়।
৫. নিচের কোডটির ভুল ঠিক করুন যাতে সঠিক নাম প্রিন্ট হয়:
```javascript
// setTimeout(function() { console.log(this.name) }, 100)
```

---

## ২২. currying

### ১. Simple definition (বাংলায়)
**Currying** হলো ফাংশনাল প্রোগ্রামিংয়ের এমন একটি বিশেষ টেকনিক, যার মাধ্যমে একাধিক আর্গুমেন্ট বিশিষ্ট একটি ফাংশনকে রূপান্তরিত করে এমন একটি ফাংশনের সিরিজে পরিণত করা হয়, যেখানে প্রতিটি ফাংশন শুধুমাত্র একটি করে আর্গুমেন্ট গ্রহণ করবে এবং কাজ শেষ না হওয়া পর্যন্ত আরেকটি নতুন ফাংশন রিটার্ন করবে।

### ২. Why this concept exists
এটি কোড রি-ইউজেবিলিটি এবং ফাংশনাল কম্পোজিশনকে অনেক সহজ করে তোলে। এর সাহায্যে আমরা ফাংশনের আংশিক আর্গুমেন্ট পাস করে সহায়ক আংশিক ফাংশন (Partially Applied Functions) তৈরি করে রাখতে পারি।

### ৩. What problem it solves
এটি বড় বড় কোডের ডুপ্লিকেশন কমায়। কোনো ফাংশনে যদি একটি আর্গুমেন্ট প্রায়ই ফিক্সড থাকে, তবে বারবার তা পাস না করে আমরা কারিং মেথডে আংশিক কনফিগারড ফাংশন বানিয়ে রাখতে পারি।

### ৪. Real-life analogy
চিন্তা করুন একটি অ্যাসেম্বলি লাইনের কথা। আপনি একটি গাড়ি বানানোর কারখানায় কাজ করছেন। গাড়ি পেইন্টিংয়ের সময় প্রথমে বডি কালার করা হয় (.color) -> তারপর চাকা লাগানো হয় (.wheels) -> তারপর সিট বসানো হয় (.seats)। প্রতিটি স্টেশন তার কাজ শেষ করে গাড়িটি পরবর্তী স্টেশনে পাস করে দেয়। এই ধাপে ধাপে কাজ করার মেথডটিই হলো Currying।

### ৫. How React works internally regarding this concept
React-এ কারিং মেকানিজম চমৎকারভাবে ব্যবহৃত হয় ইভেন্ট হ্যান্ডলার ডিজাইন করার সময়, বিশেষ করে যখন লুপের ভেতর ডাইনামিক ডেটা পাস করতে হয়।
```jsx
// Currying in React Event Handler
const handleChange = (fieldName) => (event) => {
  setFormData({
    ...formData,
    [fieldName]: event.target.value
  });
};

return (
  <input onChange={handleChange("email")} />
);
```
এখানে `handleChange("email")` কল হয়ে প্রথমে একটি ফাংশন রিটার্ন করে, যা পরবর্তীতে ইভেন্ট পাওয়ার পর রান হয়। এটি অনাবশ্যক অ্যারো ফাংশন রেন্ডার ব্লক করে।

### ৬. Basic example
```javascript
// Non-curried function
const simpleAdd = (a, b) => a + b;

// Curried function
const curriedAdd = (a) => (b) => a + b;

console.log(simpleAdd(2, 3)); // 5
console.log(curriedAdd(2)(3)); // 5
```

### ৭. Step-by-step explanation of the code
১. `curriedAdd` একটি ফাংশন যা ইনপুট হিসেবে `a` নেয় এবং আরেকটি ইনার ফাংশন রিটার্ন করে।
২. ইনার ফাংশনটি আবার `b` কে ইনপুট নেয় এবং ক্লোজারের সাহায্যে `a` কে মনে রেখে `a + b` যোগ করে।
৩. যখন আমরা `curriedAdd(2)(3)` লিখি, প্রথম ব্র্যাকেটে ২ পাস হয়ে একটি আংশিক যোগের ফাংশন রিটার্ন করে এবং দ্বিতীয় ব্র্যাকেটে ৩ পাস হয়ে সম্পূর্ণ যোগের কাজটি সম্পন্ন করে।

### ৮. Another real-world example (Logger Configuration)
```javascript
const log = (level) => (module) => (message) => {
  console.log(`[${level}] [${module}] ${message}`);
};

const userErrorLog = log("ERROR")("UserModule");
userErrorLog("Database connection failed"); // [ERROR] [UserModule] Database connection failed
```

### ৯. Common mistakes beginners make
* কারিং ফাংশন কল করার সময় ব্র্যাকেটের সিকোয়েন্স দিতে ভুলে যাওয়া (যেমন `curriedAdd(2, 3)` কল করা, যা কোনো এরর ছাড়াই একটি ফাংশন রিটার্ন করে বসে থাকবে কিন্তু আসল রেজাল্ট দেবে না)।

### ১০. Interview questions related to this topic
* **Q:** Currying এবং Partial Application-এর মধ্যে তফাৎ কী?
  * **Ans:** Currying একটি ফাংশনকে কনভার্ট করে একগুচ্ছ ফাংশনে রূপ দেয় যেখানে প্রতিটি ফাংশন কঠোরভাবে **একটি মাত্র** আর্গুমেন্ট নেয়। আর Partial Application হলো একই কনসেপ্ট কিন্তু সেখানে ইনার ফাংশনগুলো একের বেশি আর্গুমেন্টও নিতে পারে।
* **Q:** ইনফিনিট কারিং (Infinite Currying) কীভাবে লিখবেন?
  * **Ans:** রিকার্সিভলি ফাংশন রিটার্ন করে যতক্ষণ না আর্গুমেন্ট ফাঁকা পাওয়া যায়। (নিচে কোড এক্সারসাইজে দেওয়া আছে)।

### ১১. Best practices
* কোড ক্লিনিংয়ের জন্য অ্যারো ফাংশন চেইনিং দিয়ে কারিং লিখুন, যা পড়তে অনেক সহজ হয়।
* ডাইনামিক কনফিগারেশন হ্যান্ডলিংয়ে কারিং ব্যবহার করুন।

### ১২. Performance considerations
* কারিং ইন্টারনাল ক্লোজার এবং ফাংশন কল চেইনের ওপর নির্ভর করে। এটি অতি ক্ষুদ্র মেমোরি ওভারহেড তৈরি করতে পারে (যদিও তা খুব নগণ্য)।

### १३. When NOT to use it
* এটি এড়ানো উচিত যখন কোনো ফাংশনে আর্গুমেন্টের পরিমাণ খুব কম বা ফাংশনটি খুবই সরল হয়।

### ১৪. Comparison with similar concepts
| টেকনিক | আর্গুমেন্ট গ্রহণ নীতি | রিটার্ন টাইপ |
| :--- | :--- | :--- |
| **Normal Function** | সব আর্গুমেন্ট একসাথে | সরাসরি ফাইনাল ভ্যালু |
| **Curried Function** | ধাপে ধাপে একটি করে আর্গুমেন্ট | একটি ফাংশন (যতক্ষণ না সব আর্গুমেন্ট শেষ হয়) |

### ১৫. Summary in simple Bangla
কারিং হলো কোনো খাবারে একবারে সব মসলা না দিয়ে ধাপে ধাপে একটি একটি করে মসলা দিয়ে রান্না সুস্বাদু করা।

### ১৬. 5 MCQ questions
১. Currying মেকানিজম মূলত নিচের কোন জাভাস্ক্রিপ্ট ধারণার সাহায্যে কাজ সম্পন্ন করে?
   (ক) Hoisting (খ) Event loop (গ) Closures (ঘ) Garbage Collector
   **উত্তরঃ গ**
২. নিচের কারিং ফাংশনের সঠিক কলিং সিনট্যাক্স কোনটি?
   (ক) `fn(a, b, c)` (খ) `fn(a)(b)(c)` (গ) `fn(a, b)(c)` (ঘ) `fn([a,b,c])`
   **উত্তরঃ খ**
৩. নিচের কোডটির আউটপুট কী হবে?
```javascript
const mul = x => y => x * y;
const double = mul(2);
console.log(double(5));
```
   (ক) 2 (খ) 5 (গ) 10 (ঘ) Error
   **উত্তরঃ গ**
৪. Currying মূলত কোন প্রোগ্রামিং প্যারাডাইম বা স্টাইলের অংশ?
   (ক) Imperative Programming (খ) Functional Programming (গ) Object Oriented Programming (ঘ) Procedural Programming
   **উত্তরঃ খ**
৫. `const f = a => b => c => a + b + c;` ফাংশনটিতে মোট কয়টি ফাংশন মেমোরিতে চেইনড হয়ে আছে?
   (ক) ১টি (খ) ২টি (গ) ৩টি (ঘ) ৪টি
   **উত্তরঃ গ**

### ১৭. 5 Coding exercises
১. ৩টি সংখ্যা গুণ করার একটি কারিং ফাংশন লিখুন।
২. একটি ডিসকাউন্ট ক্যালকুলেশন কারিং ফাংশন লিখুন যেখানে ডিসকাউন্ট রেট (যেমন ১০%) ফিক্সড রেখে বিভিন্ন প্রাইজের ওপর রান করা যাবে।
৩. ইনফিনিট কারিংয়ের একটি ফাংশন লিখুন যা এভাবে কাজ করবে: `sum(1)(2)(3)(4)...()` এবং শেষে সবগুলোর যোগফল দেবে।
৪. React-এর অন-চেঞ্জ ইভেন্ট হ্যান্ডল করার জন্য একটি কারিং মেথড ডেমো তৈরি করুন।
৫. সাধারণ ৩ আর্গুমেন্টের ফাংশনকে কারিং ফাংশনে রূপান্তর করার একটি সাহায্যকারী `curry(fn)` র্যাপার ফাংশন লিখুন।

---

## ২৩. Event Bubbling & Delegation

### ১. Simple definition (বাংলায়)
**Event Bubbling** হলো ব্রাউজারের ডম (DOM) ইভেন্ট প্রপাগেশনের (Propagation) একটি ডিফল্ট আচরণ, যার ফলে কোনো নির্দিষ্ট নেস্টেড বা চাইল্ড এলিমেন্টে কোনো ইভেন্ট (যেমন ক্লিক) ঘটলে, সেই ইভেন্টটি বুদবুদের মতো ধীরে ধীরে তার ওপরের সমস্ত প্যারেন্ট এলিমেন্ট হয়ে একদম রুট (Document/Window) পর্যন্ত পৌঁছায়। আর এই বাবলিং আচরণকে কাজে লাগিয়ে প্রতিটি চাইল্ড এলিমেন্টে আলাদা লিসেনার না বসিয়ে তাদের কমন প্যারেন্ট এলিমেন্টে একটিমাত্র ইভেন্ট লিসেনার বসিয়ে সব চাইল্ডের ইভেন্ট ম্যানেজ করার টেকনিককে **Event Delegation** বলে।

### ২. Why this concept exists
ডম আর্কিটেকচার হলো হায়ারার্কিকাল বা ট্রির মতো নেস্টেড। কোনো এলিমেন্টে ক্লিক করলে তার প্যারেন্টও একই সাথে ক্লিক রেঞ্জে পড়ে। এটি ট্র্যাক করার জন্য ডম ইভেন্ট ফ্লো মেকানিজম প্রয়োজন।

### ৩. What problem it solves
১. **Event Bubbling:** এটি নিশ্চিত করে যে চাইল্ড এলিমেন্টের ইভেন্ট প্যারেন্টকে ট্রিগার করতে পারে, যা কম্পোজিট বা কন্ডিশনাল ডম ইন্টারঅ্যাকশনে সাহায্য করে।
২. **Event Delegation:** এটি মেমোরি বাঁচায়। যদি একটি লিস্টে ১০০০টি আইটেম থাকে, তবে ১০০০টি আলাদা ক্লিক লিসেনার বসানোর বদলে আমরা কেবল প্যারেন্ট `ul`-এ একটিমাত্র লিসেনার বসিয়ে কাজ সম্পন্ন করতে পারি, যা ব্রাউজার মেমোরির ওপর প্রেশার কমায়।

### ৪. Real-life analogy
মনে করুন আপনি একটি বড় বাক্সের ভেতরে একটি মাঝারি বাক্স রেখেছেন, এবং মাঝারি বাক্সের ভেতরে একটি ছোট সোনার বাক্স রেখেছেন। আপনি যদি হাত দিয়ে সরাসরি ছোট সোনার বাক্সটিকে স্পর্শ করেন, তবে আপনি একই সাথে মাঝারি বাক্স এবং বড় বাক্সের ভেতরের সীমানাকেও স্পর্শ করলেন। এই স্পর্শের অনুভূতি বাইরে ছড়িয়ে পড়ার মেকানিজমটিই হলো Bubbling।

### ৫. How React works internally regarding this concept
React-এর রেন্ডারিং অপ্টিমাইজেশনের পেছনে Event Bubbling এবং Event Delegation মূল ভূমিকা পালন করে। React সরাসরি ব্রাউজার ডমে ইভেন্ট লিসেনার মাউন্ট করে না। এটি অ্যাপের রুটে (`document` বা `#root` কন্টেইনারে) একটিমাত্র লিসেনার বসায়। যখন আপনি কোনো ডিপ-নেস্টেড বাটনে `onClick` দেন, বাটন ক্লিকে ব্রাউজারের ইভেন্ট বাবল হয়ে রুটে যায় এবং React তার সিন্থেটিক ইভেন্ট প্রসেসর দিয়ে আপনার কলব্যাকটি ফায়ার করায়।

### ৬. Basic example
```html
<div id="parent" style="padding: 20px; background: grey;">
  Parent
  <button id="child">Child Button</button>
</div>

<script>
  document.getElementById("parent").addEventListener("click", () => {
    console.log("Parent Clicked");
  });

  document.getElementById("child").addEventListener("click", (event) => {
    console.log("Child Clicked");
    // event.stopPropagation();
  });
</script>
```

### ७. Step-by-step explanation of the code
১. আমরা একটি প্যারেন্ট `div` এবং তার ভেতরে একটি `button` নিয়েছি। দুটিতেই আলাদা ক্লিক লিসেনার দেওয়া আছে।
২. যখন আমরা "Child Button" এ ক্লিক করি, কনসোলে প্রথমে "Child Clicked" দেখায়।
৩. এরপর ইভেন্টটি বাবল আপ হয়ে প্যারেন্টে যায়, ফলে কোনো সরাসরি ক্লিক না করা সত্ত্বেও প্যারেন্টের লিসেনার ফায়ার করে এবং কনসোলে "Parent Clicked" প্রিন্ট করে।
৪. আমরা যদি `event.stopPropagation()` ব্যবহার করতাম, তবে চাইল্ড ক্লিকের পর ইভেন্টটি আর ওপরে উঠত না, ফলে শুধু "Child Clicked" প্রিন্ট হতো।

### ৮. Another real-world example (Event Delegation pattern)
```javascript
// Capture clicks on all list items using a single parent listener
document.getElementById("my-list").addEventListener("click", (e) => {
  if (e.target && e.target.nodeName === "LI") {
    console.log("List item clicked:", e.target.innerText);
  }
});
```

### ৯. Common mistakes beginners make
* ডালপালা বা নেস্টেড ডম স্ট্রাকচারে কাজ করার সময় বাবলিং স্টপ না করার কারণে প্যারেন্ট ডমের অবাঞ্ছিত ক্লিক লজিক ফায়ার হয়ে যাওয়া (যেমন পপআপ ওপেন থাকা অবস্থায় পপআপের ভেতরে ক্লিক করলে পপআপ ক্লোজ হয়ে যাওয়া)।
* `event.preventDefault()` এবং `event.stopPropagation()` গুলিয়ে ফেলা। (১মটি ব্রাউজারের ডিফল্ট অ্যাকশন থামায় যেমন লিংক ক্লিক বা ফরম সাবমিট, আর ২য়টি ইভেন্টের ওপরে ওড়া থামায়)।

### ১০. Interview questions related to this topic
* **Q:** Event Bubbling এবং Event Capturing (Trickling) এর মধ্যে পার্থক্য কী?
  * **Ans:** Event Capturing হলো ইভেন্ট ফ্লো যা উপর থেকে শুরু হয়ে নিচের টার্গেট এলিমেন্টের দিকে নামে (টপ-ডাউন)। আর Event Bubbling হলো নিচের টার্গেট এলিমেন্ট থেকে শুরু হয়ে বুদবুদের মতো ওপরে ছড়ায় (বটম-আপ)।
* **Q:** কীভাবে Event Bubbling সম্পূর্ণ বন্ধ করা যায়?
  * **Ans:** ইভেন্ট অবজেক্টের `event.stopPropagation()` মেথড কল করার মাধ্যমে।

### ১১. Best practices
* ডমের পারফরম্যান্স ঠিক রাখতে শত শত এলিমেন্টে লিসেনার না বসিয়ে ইভেন্ট বাবলিংয়ের সুবিধা নিয়ে প্যারেন্ট এলিমেন্টে Event Delegation ব্যবহার করুন।
* যেখানে বাবলিংয়ের প্রয়োজন নেই, সেখানে সিকিউরড কোডিংয়ের জন্য `stopPropagation()` ব্যবহার করুন।

### ১২. Performance considerations
* বাবলিং বন্ধ না করলে অপ্রয়োজনীয় ডম ট্রাভার্সিংয়ের কারণে ব্রাউজারের মেইন থ্রেডে ক্ষুদ্র ওভারহেড হতে পারে, বিশেষ করে খুব নেস্টেড ডম থাকলে।

### १३. When NOT to use it
* যদি এমন কোনো ইভেন্ট হ্যান্ডলার হয় যা অন্য কোনো প্যারেন্ট লজিকের সাথে কনফ্লিক্ট করতে পারে (যেমন মডেল ডিরেক্টরি ক্লিক), তবে বাবলিং বন্ধ রাখাই শ্রেয়।

### ১৪. Comparison with similar concepts
| মেকানিজম | প্রপাগেশন ডিরেকশন | ডিফল্ট আচরণ |
| :--- | :--- | :--- |
| **Event Bubbling** | টার্গেট থেকে রুট (Bottom-Up) | হ্যাঁ, ব্রাউজারের ডিফল্ট |
| **Event Capturing** | রুট থেকে টার্গেট (Top-Down) | না, ম্যানুয়ালি অন করতে হয় |

### ১৫. Summary in simple Bangla
ইভেন্ট বাবলিং হলো পুকুরের নিচে ঢিল মারলে যেমন বুদবুদ ওপরে ওঠে, তেমনি ডমের ভেতরে বাটনে ক্লিক করলে সেই ক্লিকের ঢেউ ওপরে প্যারেন্ট এলিমেন্টগুলোতে ছড়িয়ে পড়া।

### ১৬. 5 MCQ questions
১. ইভেন্ট বাবলিংয়ের ক্ষেত্রে ইভেন্ট প্রপাগেশনের দিক কোনটি?
   (ক) Root -> Target (খ) Target -> Root (গ) Left -> Right (ঘ) Random
   **উত্তরঃ খ**
২. ডম ইভেন্ট বাবলিং বন্ধ করার জন্য নিচের কোন মেথডটি ব্যবহৃত হয়?
   (ক) event.preventDefault() (খ) event.stopPropagation() (গ) event.stopImmediatePropagation() (ঘ) খ এবং গ উভয়ই
   **উত্তরঃ ঘ (stopImmediatePropagation একই এলিমেন্টের অন্যান্য লিসেনারও ব্লক করে)**
৩. ব্রাউজারের ডিফল্ট ক্লিক ক্যাপচারিং অন করতে `addEventListener`-এর ৩য় আর্গুমেন্টে কী পাস করতে হয়?
   (ক) `{ capture: false }` (খ) `{ capture: true }` (গ) `{ bubble: true }` (ঘ) null
   **উত্তরঃ খ**
৪. React-এর নিজস্ব ইভেন্ট সিস্টেমকে কী বলা হয় যা ব্রাউজারের ইভেন্ট ডেলিগেশনকে র‍্যাপ করে?
   (ক) Virtual Events (খ) Synthetic Events (গ) Fiber Events (ঘ) Native Actions
   **উত্তরঃ খ**
৫. নিচের কোডের `button` ক্লিক করলে কনসোলে কী আউটপুট আসবে?
```javascript
document.body.addEventListener("click", () => console.log("Body"));
document.getElementById("btn").addEventListener("click", (e) => {
  e.stopPropagation();
  console.log("Btn");
});
```
   (ক) Btn -> Body (খ) Body -> Btn (গ) Btn (ঘ) Error
   **উত্তরঃ গ (stopPropagation বাবলিং আটকে দেওয়ায় Body পর্যন্ত পৌঁছাবে না)**

### ১৭. 5 Coding exercises
১. একটি ৩ স্তরের ডম তৈরি করে (Grandparent, Parent, Child) বাবলিং প্রুফ করুন।
২. ১ নং কোডে `event.stopPropagation()` ব্যবহার করে বাবলিং থামান।
৩. একটি `ul` লিস্টের ভেতরে ৫টি `li` নিন এবং শুধুমাত্র `ul` এ সিঙ্গেল ক্লিক লিসেনার বসিয়ে ক্লিকড আইটেমের টেক্সট প্রিন্ট করার ডেলিগেশন কোড লিখুন।
৪. কাস্টম ড্রপডাউন মেনু তৈরি করুন যা ড্রপডাউনের বাইরে ক্লিক করলে মেনুটি বন্ধ হয়ে যাবে (উইন্ডো ক্লিক ট্র্যাকিং এবং বাবলিং কন্ট্রোল)।
৫. নিচের কোডটিতে এরর খুঁজে বের করে ঠিক করুন:
```javascript
// link.addEventListener("click", (e) => { e.stopPropagation(); });
```

---

## ২৪. Throttling

### ১. Simple definition (বাংলায়)
**Throttling** হলো এমন একটি পারফরম্যান্স অপ্টিমাইজেশন টেকনিক, যা কোনো একটি উচ্চ-ফ্রিকোয়েন্সি ইভেন্টকে (যেমন উইন্ডো স্ক্রল বা মাউস মুভমেন্ট) বারবার ফায়ার হওয়া থেকে বিরত রাখে এবং নিশ্চিত করে যে একটি নির্দিষ্ট সময় পরপর (যেমন প্রতি ৫০০ মিলি সেকেন্ডে একবার) ফাংশনটি সর্বোচ্চ একবারই এক্সিকিউট হবে।

### ২. Why this concept exists
কিছু ব্রাউজার ইভেন্ট (যেমন: `scroll`, `resize`, `mousemove`) খুব দ্রুত ফায়ার হয়—এক সেকেন্ডে ১০০ বারেরও বেশি। এই প্রতিটি ইভেন্টে যদি কোনো এপিআই কল বা ডম ক্যালকুলেশন থাকে, তবে ব্রাউজার স্লো হয়ে ঝুলে যাবে। এই অপ্রয়োজনীয় এক্সিকিউশন রেট লিমিট করতেই থ্রটলিং ব্যবহার করা হয়।

### ৩. What problem it solves
এটি সিপিইউ (CPU) কনজাম্পশন কমায়, ব্রাউজারের পেইন্ট ও রেন্ডার ফ্রেম রেট (FPS) ভালো রাখে এবং সার্ভারের ওপর অতিরিক্ত এপিআই রিকোয়েস্ট লোড পড়তে দেয় না।

### ৪. Real-life analogy
মনে করুন আপনার বাড়িতে একটি পানির কল আছে যা দিয়ে সেকেন্ডে ১০ ফোঁটা পানি পড়ে। আপনি ফোঁটা ফোঁটা পানি ট্র্যাক না করে একটি বালতি বসিয়ে দিলেন এবং ঠিক করলেন প্রতি ১০ মিনিট পর পর এসে বালতি খালি করবেন (Rate Limit)। এই নির্দিষ্ট সময় পর পর চেক ও অ্যাকশন নেওয়ার প্যাটার্নই হলো Throttling।

### ৫. How React works internally regarding this concept
React অ্যাপে রিয়েল-টাইম স্ক্রল পজিশন ট্র্যাকিং (যেমন: ইনফিনিট স্ক্রল বা স্টিকি হেডার) অথবা উইন্ডো রিসাইজ করে কোনো ডাইনামিক উইডথ ক্যালকুলেট করার সময় আমরা থ্রটলিং করি। থ্রটলিং না করলে প্রতি পিক্সেল স্ক্রলে React রি-রেন্ডার ট্রিগার করবে, যা পুরো অ্যাপ্লিকেশনকে চরম ল্যাগি বানিয়ে ফেলবে।
```jsx
// Throttled Scroll Listener in React
useEffect(() => {
  const handleScroll = throttle(() => {
    console.log("Current Scroll:", window.scrollY);
  }, 200);

  window.addEventListener("scroll", handleScroll);
  return () => window.removeEventListener("scroll", handleScroll);
}, []);
```

### ৬. Basic example
```javascript
function throttle(func, limit) {
  let inThrottle;
  return function() {
    const args = arguments;
    const context = this;
    if (!inThrottle) {
      func.apply(context, args);
      inThrottle = true;
      setTimeout(() => inThrottle = false, limit);
    }
  };
}

// Target function
const logScroll = () => console.log("Scrolled at:", new Date().toLocaleTimeString());

// Throttled version
const throttledScroll = throttle(logScroll, 1000);

// Simulating multiple rapid calls
throttledScroll(); // Runs immediately
throttledScroll(); // Ignored
setTimeout(throttledScroll, 1100); // Runs (after 1000ms limit passed)
```

### ७. Step-by-step explanation of the code
১. `throttle` ফাংশনটি ইনপুট হিসেবে একটি টার্গেট ফাংশন `func` এবং সময়সীমা `limit` নেয়।
২. এটি একটি ফ্ল্যাগ ভ্যারিয়েবল `inThrottle` মেইনটেইন করে যা ট্র্যাকিং করে লক রিলিজ হয়েছে কি না।
৩. প্রথম কলের সময় `inThrottle` মিথ্যা থাকায় ফাংশনটি সাথে সাথে রান হয় এবং `inThrottle` ট্রু (লকড) হয়ে যায়।
৪. একটি টাইমার সেট করা হয় যা নির্দিষ্ট `limit` সময় পার হওয়ার পর লক খুলে দেবে (`inThrottle = false`)।
৫. এই লক থাকা অবস্থায় যতবারই থ্রটলিং ফাংশন কল করা হোক না কেন, তা ইগনোরড বা বা ব্লকড হবে।

### ৮. Another real-world example (API Rate Limit protection)
```javascript
// Throttle database save calls in editor to save every 2 seconds
const saveContent = (content) => saveToDB(content);
const throttledSave = throttle(saveContent, 2000);

editor.addEventListener("keyup", (e) => {
  throttledSave(e.target.value);
});
```

### ৯. Common mistakes beginners make
* থ্রটলিং এবং ডিবান্সিং মেকানিজম গুলিয়ে ফেলা (থ্রটলিং নির্দিষ্ট ইন্টারভালে রান করায়, আর ডিবান্সিং টাইপিং থামার পর রান করায়)।
* React-এর ভেতরে হুকের ডিপেন্ডেন্সি ভুল হওয়ার কারণে প্রতি রেন্ডারে নতুন করে থ্রটলিং ফাংশন রি-ক্রিয়া করা (এর ফলে রেট লিমিট কাজ করে না; এটি এড়াতে `useRef` বা `useCallback` ব্যবহার করতে হয়)।

### ১০. Interview questions related to this topic
* **Q:** Throttling এবং Debouncing এর মূল পার্থক্য কী?
  * **Ans:** Throttling নিশ্চিত করে যে একটি ইভেন্ট চলাকালীন প্রতি X মিলি সেকেন্ড অন্তর ফাংশনটি অন্তত একবার ফায়ার হবেই। আর Debouncing নিশ্চিত করে যে ইভেন্টটি সম্পূর্ণ থামা বা পজ হওয়ার X মিলি সেকেন্ড পর ফাংশনটি কেবল একবার ফায়ার হবে।
* **Q:** React-এ থ্রটলিং করার সময় ফাংশন বারবার তৈরি হওয়া কীভাবে প্রতিরোধ করবেন?
  * **Ans:** `useCallback` বা `useRef` ব্যবহার করে থ্রটলিং ফাংশনটির মেমোরি রেফারেন্স রেন্ডারের মধ্যবর্তী সময়ে সেভ রেখে এটি সমাধান করা যায়।

### ১১. Best practices
* স্ক্রল, রিসাইজ, এবং ড্র্যাগ ইভেন্টগুলোতে অবশ্যই থ্রটলিং বা ডিবান্সিং প্র্যাকটিস করুন।
* প্রোডাকশন লেভেলে নিজে কাস্টম কোড না লিখে Lodash-এর `_.throttle` ব্যবহার করা বেশি নিরাপদ ও এজ-কেস প্রুফ।

### ১২. Performance considerations
* সঠিক লিমিট টাইম (যেমন ১০০-২০০ মিলি সেকেন্ড) সিলেক্ট করুন, খুব কম লিমিট দিলে পারফরম্যান্স ইমপ্রুভমেন্ট ভালো পাওয়া যায় না, আবার খুব বেশি দিলে ইউজার এক্সপেরিয়েন্স ল্যাগি মনে হতে পারে।

### १३. When NOT to use it
* যদি সার্চ ইনপুট বক্স হয় যেখানে ইউজার টাইপিং শেষ করার পর ইনস্ট্যান্ট রেজাল্ট দেখতে চায়, সেখানে থ্রটলিং না করে ডিবান্সিং ব্যবহার করুন।

### ১৪. Comparison with similar concepts
| টেকনিক | ট্রিগার মেথড | সাধারণ ব্যবহার |
| :--- | :--- | :--- |
| **Throttling** | নির্দিষ্ট সময় পরপর রেগুলার ইন্টারভালে | Scroll, Window Resize, Game Loop |
| **Debouncing** | অ্যাকশন শেষ হওয়ার নির্দিষ্ট সময় পর | Search Input, Auto-save |

### ১৫. Summary in simple Bangla
থ্রটলিং হলো ট্রাফিক সিগন্যালের মতো, যেখানে প্রতি ২ মিনিট পর পর ট্রাফিক লাইট একবার করে সবুজ হবে এবং জ্যামের গাড়ি ছাড়বে, মাঝখানে যতই জটলা লাগুক না কেন।

### ১৬. 5 MCQ questions
১. নিচের কোন ইভেন্টের জন্য থ্রটলিং করা সবচেয়ে উপযোগী?
   (ক) Button Click (খ) Search Input Typing (গ) Window Scroll (ঘ) Page Load
   **উত্তরঃ গ**
২. থ্রটলিংয়ের ইন্টারনাল মেকানিজমে স্টেট ট্র্যাকিংয়ের জন্য সাধারণত কী ব্যবহৃত হয়?
   (ক) Array (খ) Closure lock/flag variable (গ) Event loop queue (ঘ) Web worker
   **উত্তরঃ খ**
৩. Lodash লাইব্রেরিতে থ্রটলিং মেথডটির নাম কী?
   (ক) `_.debounce` (খ) `_.throttle` (গ) `_.rateLimit` (ঘ) `_.delay`
   **উত্তরঃ খ**
৪. থ্রটলিং না করে প্রতি মিলি সেকেন্ডে ডম আপডেট করলে ব্রাউজার স্লো হয়ে যাওয়ার প্রধান কারণ কোনটি?
   (ক) Memory leakage (খ) CPU/Render Reflow Choke (গ) JavaScript engine crash (ঘ) HTTP waterfall
   **উত্তরঃ খ**
৫. নিচের কোডটির জন্য কোনটি সঠিক?
```javascript
window.addEventListener("resize", throttle(myFn, 1000));
```
   (ক) myFn প্রতি পিক্সেল চেঞ্জে রান হবে (খ) myFn প্রতি সেকেন্ডে সর্বোচ্চ একবার রান হবে (গ) resizing শেষ হওয়ার ১ সেকেন্ড পর রান হবে (ঘ) এরর দেবে
   **উত্তরঃ খ**

### ১৭. 5 Coding exercises
১. একটি মাউস মুভমেন্ট ট্র্যাকার পেইজ বানিয়ে থ্রটলিং মেথড ছাড়া এবং থ্রটলিং মেথডসহ ইভেন্ট কাউন্ট তুলনা করে স্ক্রিনে দেখান।
২. Lodash ছাড়া পিউর জাভাস্ক্রিপ্ট দিয়ে একটি কাস্টম `throttle` ফাংশন লিখুন যা আর্গুমেন্ট পাসিং সাপোর্ট করবে।
৩. React-এ `useCallback` ব্যবহার করে একটি থ্রটলেড উইন্ডো রিসাইজ লিসেনার ইমপ্লিমেন্ট করুন।
৪. এমন একটি কোড লিখুন যা প্রতি কলে লক ভ্যালু কনসোলে প্রিন্ট করবে এবং থ্রটলিং লাইফ সাইকেল ভিজ্যুয়ালাইজ করবে।
৫. নিচের কোডটির বাগ সনাক্ত করে ঠিক করুন:
```javascript
// window.addEventListener("resize", throttle(handleResize, 500)); // inside render loop
```

---

## ২৫. Debouncing

### ১. Simple definition (বাংলায়)
**Debouncing** হলো এমন একটি পারফরম্যান্স অপ্টিমাইজেশন টেকনিক, যা নিশ্চিত করে যে কোনো একটি দ্রুত বারবার ঘটা ইভেন্টের ফাংশনটি তখনই এক্সিকিউট হবে, যখন ওই ইভেন্টটি ঘটা সম্পূর্ণ বন্ধ হবে এবং একটি নির্দিষ্ট নিষ্ক্রিয় বা পজ সময় (যেমন ৫০০ মিলি সেকেন্ড) পার হবে।

### ২. Why this concept exists
সার্চ ইনপুট ফিল্ডে ইউজার টাইপ করার সময় প্রতি স্ট্রোকে (যেমন `a`, `b`, `c`) যদি আমরা এপিআই রিকোয়েস্ট পাঠাতে থাকি, তবে ৫ অক্ষরের একটি শব্দ টাইপ করতেই ৫ বার সার্ভারে রিকোয়েস্ট যাবে যা ডাটাবেজের ওপর চরম প্রেসার তৈরি করবে। এই অতিরিক্ত রিকোয়েস্ট কমানোর জন্যই ডিবান্সিং অত্যন্ত জরুরি।

### ৩. What problem it solves
এটি অনাবশ্যক নেটওয়ার্ক রিকোয়েস্ট (API Calls) কমায়, সার্ভার কস্ট সাশ্রয় করে এবং ডম রেন্ডারিং অপ্টিমাইজ করে।

### ৪. Real-life analogy
মনে করুন আপনি লিফটে উঠছেন। আপনি লিফটে ঢোকার পর লিফট দরজা বন্ধ করতে যাবে, এমন সময় আরেকজন এসে বোতাম চাপল। লিফট দরজা বন্ধ করা থামিয়ে তার জন্য দরজা খুলে দিল এবং আবার ৫ সেকেন্ডের টাইমার রিসেট করল। এভাবে যতক্ষণ নতুন মানুষ ঢুকতেই থাকবে, লিফটের দরজা বন্ধ হওয়া পেছাতে থাকবে। মানুষ আসা বন্ধ হওয়ার ৫ সেকেন্ড পরই কেবল লিফট চলতে শুরু করবে। এই টাইমার রিসেট করার প্রসেসটিই হলো Debouncing।

### ৫. How React works internally regarding this concept
React-এর লাইভ সার্চ উইজেট বা ফিল্টারিং ফিল্ডে ডিবান্সিং বহুল ব্যবহৃত। ইউজার টাইপ করা শুরু করলে আমরা টাইমার সেট করি, টাইপিং শেষ হলে তবেই আমরা স্টেট চেঞ্জ বা এপিআই রিকোয়েস্ট ফায়ার করি।
```jsx
// React Debounced Search Input
useEffect(() => {
  const delayDebounceFn = setTimeout(() => {
    if (searchTerm) {
      fetchSearchResults(searchTerm);
    }
  }, 500);

  return () => clearTimeout(delayDebounceFn); // Clean up: resets timer on next keystroke
}, [searchTerm]);
```
এখানে React-এর `useEffect` ক্লিনআপ মেথডটি লিফটের মতো প্রতিবার টাইপ করার সাথে সাথে আগের পেন্ডিং টাইমারটি ডিলিট করে দেয়।

### ৬. Basic example
```javascript
function debounce(func, delay) {
  let timeoutId;
  return function() {
    const context = this;
    const args = arguments;
    clearTimeout(timeoutId); // Reset previous timer
    timeoutId = setTimeout(() => {
      func.apply(context, args);
    }, delay);
  };
}

// Target search action
const searchAPI = (query) => console.log("API Fetch for:", query);

// Debounced version
const debouncedSearch = debounce(searchAPI, 500);

// Keystroke simulations
debouncedSearch("a"); // Timer started
debouncedSearch("ap"); // Previous timer cleared, new timer started
debouncedSearch("app"); // Keystrokes stop here... 
// After 500ms -> Prints: API Fetch for: app (Only once!)
```

### ৭. Step-by-step explanation of the code
১. `debounce` ফাংশনটি টাইমার আইডি ট্র্যাক করার জন্য একটি `timeoutId` ভ্যারিয়েবল ক্লোজার মেমরিতে রাখে।
২. যখনই ডিবান্সড ফাংশনটি কল হয়, সে প্রথমে `clearTimeout(timeoutId)` করে আগের পেন্ডিং টাইমারটি পুরোপুরি বাতিল করে দেয়।
৩. এরপর সে নতুন করে আরেকটি `setTimeout` চালু করে যা `delay` সময় পর মূল ফাংশনটি ফায়ার করবে।
৪. ইউজার যত দ্রুত টাইপ করতে থাকবেন, আগের টাইমারগুলো ডিলিট হতে থাকবে এবং কোনো অ্যাকশন ফায়ার হবে না।
৫. টাইপিং বন্ধ হওয়ার পর কেবল সর্বশেষ টাইমারটি সফলভাবে সম্পূর্ণ হয়ে অ্যাকশনটি ট্রিগার করবে।

### ৮. Another real-world example (Auto-Save system)
```javascript
// Auto-save form data when user stops editing for 1.5 seconds
const saveDraft = (data) => console.log("Saving draft...", data);
const debouncedSave = debounce(saveDraft, 1500);

document.getElementById("form-text").addEventListener("input", (e) => {
  debouncedSave(e.target.value);
});
```

### ৯. Common mistakes beginners make
* ডিবান্সিং টাইম বেশি দিয়ে ফেলা (যেমন ২ সেকেন্ড), যার ফলে ইউজার ভাবেন অ্যাপটি স্লো বা কাজ করছে না।
* React কম্পোনেন্ট রিবিল্ড হওয়ার সময় ডিবান্স মেথড রিসেট করা (যেমন `useCallback` বা `useMemo` ছাড়া সরাসরি ডিবান্স ফাংশন ডিক্লেয়ার করা, যা প্রতি রেন্ডারে নতুন টাইমার স্কোপ তৈরি করে ডিবান্সিং নষ্ট করে)।

### ১০. Interview questions related to this topic
* **Q:** Throttling এবং Debouncing এর মধ্যে কোনটি সার্চ ইনপুটের জন্য বেস্ট এবং কেন?
  * **Ans:** Debouncing বেস্ট। কারণ ইউজার একটি বড় শব্দ টাইপ করার মাঝখানে আমরা অর্ধেক ডাবল রেসপন্স চাই না, ইউজার টাইপিং শেষ করার পর একবারই সঠিক ডেটা লোড করতে চাই।
* **Q:** `clearTimeout` কীভাবে ডিবান্সিংয়ে সাহায্য করে?
  * **Ans:** এটি নতুন ইভেন্ট ট্রিগার হওয়ার সাথে সাথে পূর্ববর্তী শিডিউলড টাইমারটিকে মেমোরি থেকে ডিলিট করে দেয়, ফলে আগের পেন্ডিং কাজটি আর এক্সিকিউট হতে পারে না।

### ১১. Best practices
* সার্চ ইনপুট বক্সে ৩০০-৫০০ মিলি সেকেন্ড ডিবান্স টাইম ব্যবহার করা স্ট্যান্ডার্ড প্র্যাকটিস।
* React এ কাস্টম `useDebounce` হুক বানিয়ে প্রজেক্ট জুড়ে ব্যবহার করুন।

### ১২. Performance considerations
* ডিবান্সিং মেমোরির ওপর প্রেশার কমায় কারণ এটি ব্যাকএন্ড এপিআই কল এবং ডম পেন্টিং এর হার অনেক কমিয়ে দেয়।

### १३. When NOT to use it
* যদি গেম লুপ বা ড্র্যাগ-অ্যান্ড-ড্রপ গেমের মতো ইনস্ট্যান্ট কোঅর্ডিনেট ক্যালকুলেশন দরকার হয়, সেখানে ডিবান্সিং ব্যবহার করবেন না, কারণ এটি ল্যাগ বা ডিলে তৈরি করবে।

### ১৪. Comparison with similar concepts
| মেথড | কাজ করার নিয়ম | বেস্ট কেস |
| :--- | :--- | :--- |
| **Debouncing** | ইভেন্ট পজ হওয়ার পর কেবল একবার রান হবে | Search Input, Window Resize End |
| **Throttling** | ইভেন্ট চলাকালে নিয়মিত বিরতিতে রান হবে | Scrolling Tracker, Game loop |

### ১৫. Summary in simple Bangla
ডিবান্সিং হলো ডন বা লিডারের মতো, যে সবার কোলাহল শেষ হওয়া পর্যন্ত অপেক্ষা করে এবং শান্ত পরিবেশ তৈরি হলে তবেই তার রাজকীয় ঘোষণা (Action) দেয়।

### ১৬. 5 MCQ questions
১. সার্চ ডাইনামিক ফিল্টারিংয়ের জন্য নিচের কোন অপ্টিমাইজেশন পদ্ধতিটি সবচেয়ে উপযোগী?
   (ক) Throttling (খ) Debouncing (গ) Currying (ঘ) Memoization
   **উত্তরঃ খ**
২. ডিবান্সিংয়ের ইন্টারনাল মেকানিজমে আগের পেন্ডিং কাজ ডিলিট করার মেথড কোনটি?
   (ক) deleteTimeout (খ) clearTimeout (গ) resetTimeout (ঘ) killTimeout
   **উত্তরঃ খ**
৩. নিচের কোডটিতে কী ঘটবে?
```javascript
const search = debounce(() => console.log("Run"), 500);
search();
setTimeout(search, 300);
```
   (ক) "Run" ১ বার প্রিন্ট হবে (খ) "Run" ২ বার প্রিন্ট হবে (গ) "Run" প্রিন্টই হবে না (ঘ) Error
   **উত্তরঃ ক (কারণ ৩০০ms পরের কলটি প্রথম ৫০০ms টাইমারকে ডিলিট করে নতুন ৫০০ms টাইমার চালু করবে)**
৪. ডিবান্সিংয়ের ক্ষেত্রে "নিষ্ক্রিয় সময়" (Inactivity Period) বলতে কী বোঝায়?
   (ক) ব্রাউজার হ্যাং থাকা সময় (খ) দুটি ইভেন্ট ট্র্রিগারের মধ্যবর্তী যে সময়টায় নতুন কোনো ইভেন্ট ঘটেনি (গ) নেটওয়ার্ক লোডিং টাইম (ঘ) রেন্ডারিং ব্লক সময়
   **উত্তরঃ খ**
৫. React-এ কাস্টম ডিবান্সিং করার জন্য নিচের কোন হুকটি তৈরি করে রি-ইউজ করা হয়?
   (ক) useThrottle (খ) useDebounce (গ) useMemo (ঘ) usePromise
   **উত্তরঃ খ**

### ১৭. 5 Coding exercises
১. একটি ইনপুট সার্চ বক্স তৈরি করে ডিবান্সিং ইমপ্লিমেন্ট করুন যা টাইপিং শেষ হওয়ার ৫০০ মিলি সেকেন্ড পর কনসোলে সার্চ কোয়েরি প্রিন্ট করবে।
২. ১ নং অনুশীলনের কোডটি React কম্পোনেন্টে `useCallback` বা কাস্টম `useDebounce` হুক ব্যবহার করে রি-রাইট করুন।
৩. Lodash-এর মতো একটি কাস্টম `debounce` ফাংশন লিখুন যা immediate invocation (প্রথম কলেই রান করবে) ফিচার সাপোর্ট করে।
৪. অটো-সেভ ড্রাফট মেকানিজমের একটি প্রোটোটাইপ কোড লিখুন ডিবান্সিং দিয়ে।
৫. নিচের কোডটির ফ্লো ডায়াগ্রাম এঁকে বুঝান কীভাবে টাইমার ডিলিট ও রিলোড হচ্ছে:
```javascript
const fire = debounce(() => console.log("Bang!"), 1000);
fire();
setTimeout(fire, 500);
setTimeout(fire, 1200);
```
