# React Mastery: Part 3 - React Internals (Virtual DOM, Reconciliation, & Strict Mode)

স্বাগতম! এই গাইডে আমরা React-এর ইঞ্জিনের ভেতরে কী ঘটে তা গভীরভাবে বোঝার চেষ্টা করব। আমরা শিখব কীভাবে **Virtual DOM** এবং **Reconciliation (Diffing Algorithm)** কাজ করে এবং কেন **Strict Mode** ডেভেলপমেন্টের সময় অত্যন্ত গুরুত্বপূর্ণ।

---

## ১. Virtual DOM and Reconciliation

### ১. Simple definition (বাংলায়)
* **Virtual DOM:** ভার্চুয়াল DOM হলো ব্রাউজারের আসল DOM-এর একটি হালকা এবং মেমরিতে থাকা রিপ্রেজেন্টেশন (blueprint/copy)। এটি সাধারণ JavaScript অবজেক্ট দিয়ে তৈরি।
* **Reconciliation:** রিকনসিলিয়েশন হলো একটি প্রক্রিয়া যার মাধ্যমে React তার ভার্চুয়াল DOM-কে আসল DOM-এর সাথে সিঙ্ক (sync) করে। এটি করার জন্য React একটি বিশেষ **Diffing Algorithm** ব্যবহার করে আগের ভার্চুয়াল DOM এবং নতুন ভার্চুয়াল DOM-এর মধ্যে তুলনা করে শুধু পরিবর্তিত অংশটুকু আসল DOM-এ আপডেট করে।

### ২. Why this concept exists
ব্রাউজারের রিয়েল DOM (Document Object Model) অত্যন্ত ধীরগতির। যখনই DOM-এ কোনো ছোট পরিবর্তন হয়, ব্রাউজারকে পুরো পেজের লেআউট রি-ক্যালকুলেট করতে হয় এবং পুনরায় রেন্ডার করতে হয় (যাকে Reflow ও Repaint বলে)। অ্যাপের সাইজ বড় হলে এটি পারফরম্যান্স মারাত্মকভাবে কমিয়ে দেয়। React এই সমস্যা সমাধানের জন্য মেমরিতে ভার্চুয়াল DOM-এর ধারণা নিয়ে এসেছে।

### ৩. What problem it solves
ভার্চুয়াল DOM আমাদের সরাসরি আসল DOM ম্যানিপুলেশন (যেমন `document.getElementById().appendChild()`) করার ঝামেলা থেকে মুক্তি দেয়। আমরা শুধু স্টেট আপডেট করি এবং React নিজে ব্যাকগ্রাউন্ডে অত্যন্ত দক্ষ উপায়ে খুঁজে বের করে যে আসল DOM-এর ঠিক কোন জায়গাটিতে পরিবর্তন করা প্রয়োজন। এটি ব্রাউজারের রিফ্লো এবং রিপেইন্ট মিনিমাইজ করে অ্যাপকে সুপার ফাস্ট করে তোলে।

### ৪. Real-life analogy
মনে করুন আপনি একটি বড় দোতলা বাড়ির মালিক এবং আপনি সেখানে কিছু পরিবর্তন করতে চান (যেমন লিভিং রুমের জানালার পর্দা নীল থেকে সবুজ করা)। 
* **ভার্চুয়াল DOM ছাড়া (Real DOM):** আপনি পুরো বাড়িটি ভেঙে নতুন করে আবার তৈরি করলেন শুধু জানালার পর্দা সবুজ করার জন্য! (এটি অত্যন্ত ব্যয়বহুল ও ধীরগতির)।
* **ভার্চুয়াল DOM সহ:** আপনার কাছে বাড়ির একটি নিখুঁত ত্রিমাত্রিক ব্লুপ্রিন্ট বা নকশা (Virtual DOM) আছে। নকশায় আপনি পর্দাটি সবুজ করলেন এবং নকশার সাথে আগের নকশার তুলনা (Diffing) করে দেখলেন কেবল জানালার পর্দাটি বদলানো দরকার। এরপর আপনি রাজমিস্ত্রি পাঠিয়ে শুধু জানালার পর্দাটি বদলে দিলেন (Reconciliation)। এতে সময় এবং খরচ দুই-ই বাঁচল।

### ৫. How React works internally regarding this concept
React-এর রেন্ডারিং প্রসেস দুটি ফেজে ঘটে:
1. **Render Phase:** এই ধাপে React নতুন স্টেট অনুযায়ী সম্পূর্ণ কম্পোনেন্ট ট্রি-টি রান করে এবং একটি নতুন ভার্চুয়াল DOM ট্রি (JavaScript Objects) তৈরি করে।
2. **Commit Phase:** এই ধাপে React তার **Diffing Algorithm (O(n) complexity)** ব্যবহার করে পূর্ববর্তী ভার্চুয়াল DOM ট্রির সাথে নতুন ভার্চুয়াল DOM ট্রির তুলনা করে। এরপর শুধুমাত্র যেখানে পার্থক্য পাওয়া গেছে, সেই অংশটুকু আসল DOM-এ অ্যাপ্লাই করা হয়।

**React-এর Diffing Algorithm-এর দুটি মূল নিয়ম:**
* **ভিন্ন টাইপের এলিমেন্ট:** যদি কোনো এলিমেন্টের টাইপ বদলে যায় (যেমন `<div>` থেকে `<span>` হয়ে গেল), React পুরো পুরনো ট্রিটি ভেঙে ফেলে এবং সম্পূর্ণ নতুন ট্রি তৈরি করে।
* **লিস্ট এবং Keys:** যখন কোনো লিস্ট রেন্ডার করা হয়, তখন React `key` প্রপ ব্যবহার করে ম্যাচিং করে। যদি কি (key) একই থাকে, তবে React কেবল ডেটা আপডেট করে, নতুন করে DOM নোড তৈরি করে না।

### ৬. Basic example (Understanding Virtual DOM Object)
যখন আমরা JSX লিখি:
```jsx
const element = <h1 className="header">Hello World</h1>;
```
React ব্যাকগ্রাউন্ডে একে এভাবে একটি সাধারণ JavaScript অবজেক্টে রূপান্তর করে মেমরিতে রাখে:
```javascript
// Virtual DOM Representation (Simplified)
{
  type: 'h1',
  props: {
    className: 'header',
    children: 'Hello World'
  }
}
```

### ৭. Step-by-step explanation of the code
* `type`: নির্দেশ করে এটি কোন ধরনের HTML ট্যাগ (যেমন: `h1`, `div`, `span`) অথবা এটি কোনো কাস্টম React কম্পোনেন্ট কিনা।
* `props`: এই অবজেক্টের ভেতর ট্যাগের সমস্ত অ্যাট্রিবিউট (যেমন: `className`) এবং তার চিলড্রেন বা ভেতরের টেক্সট থাকে।
* React-এর মেমরিতে এই অবজেক্টের একটি বিশাল ট্রি তৈরি হয়। স্টেট আপডেট হলে নতুন একটি অবজেক্ট ট্রি তৈরি হয় এবং পূর্বের ট্রির সাথে প্রোপার্টি বাই প্রোপার্টি তুলনা করা হয়।

### ৮. Another real-world example (Dynamic List with Keys)
```jsx
import React, { useState } from 'react';

function TodoList() {
  const [todos, setTodos] = useState([
    { id: 'a', text: 'Learn React' },
    { id: 'b', text: 'Build a project' }
  ]);

  const addTop = () => {
    // Adding a new item at the top of the list
    setTodos([{ id: 'c', text: 'Master Virtual DOM' }, ...todos]);
  };

  return (
    <div>
      <button onClick={addTop}>Add to Top</button>
      <ul>
        {todos.map(todo => (
          // key helps React identify which item has changed/added
          <li key={todo.id}>{todo.text}</li>
        ))}
      </ul>
    </div>
  );
}
```
এখানে যদি আমরা `key` না দিতাম, তবে React-এর কাছে নতুন এলিমেন্টটি প্রথমে যুক্ত হলে সে পুরো লিস্টের প্রতিটা ইলিমেন্টকে নতুন ভেবে রেন্ডার করত। কিন্তু `key="c"` থাকায় React বুঝতে পারে যে কেবল প্রথম নোডটি নতুন যুক্ত হয়েছে এবং বাকি নোডগুলো আগের জায়গাতেই আছে।

### ৯. Common mistakes beginners make
* **লিস্টের Key হিসেবে Array Index ব্যবহার করা:** `key={index}` ব্যবহার করা একটি বড় ভুল। যদি লিস্টের অর্ডার সর্টিং বা ফিল্টারিং বা কোনো আইটেম ডিলিট করার মাধ্যমে পরিবর্তিত হয়, তবে ভুল ইডেক্সের কারণে React ভুল ডেটা রেন্ডার করতে পারে বা ইনপুট বক্সে ভুল টেক্সট দেখায়। সবসময় ইউনিক আইডি (যেমন `todo.id`) ব্যবহার করা উচিত।
* **ভার্চুয়াল DOM-কে Shadow DOM মনে করা:** ভার্চুয়াল DOM এবং Shadow DOM সম্পূর্ণ আলাদা জিনিস। Shadow DOM হলো ব্রাউজারের নিজস্ব ফিচার যা দিয়ে কম্পোনেন্টের CSS এবং মার্কআপকে আইসোলেট করা হয় (যেমন `<video>` ট্যাগের ইন্টারনাল বাটনসমূহ)। ভার্চুয়াল DOM হলো React-এর একটি ডিক্লেয়ারেটিভ অবজেক্ট ট্রি।

### ১০. Interview questions related to this topic
1. **React-এর Diffing Algorithm-এর টাইম কমপ্লেক্সিটি কত এবং কেন?**
   * উত্তর: এটি \(O(n)\) যেখানে \(n\) হলো নোডের সংখ্যা। সাধারণত হিউরিস্টিক ছাড়া দুটি ট্রির ডিফ করার টাইম কমপ্লেক্সিটি \(O(n^3)\) যা অত্যন্ত ধীরগতির। React দুটি অনুমানের ওপর ভিত্তি করে একে \(O(n)\) এ নামিয়ে এনেছে: (১) দুটি ভিন্ন টাইপের এলিমেন্ট ভিন্ন ট্রি তৈরি করবে, (২) ডেভেলপাররা `key` প্রপ দিয়ে চাইল্ড এলিমেন্টগুলোকে ইউনিক আইডি দেবে।
2. **লিস্টের মধ্যে `key` প্রপের ভূমিকা কী?**
   * উত্তর: Key হলো একটি ইউনিক স্ট্রিং যা React-কে বুঝতে সাহায্য করে যে লিস্টের কোনো আইটেমটি আপডেট, এড বা ডিলিট হয়েছে। এটি না থাকলে React সম্পূর্ণ লিস্ট রি-রেন্ডার করতে বাধ্য হয়।
3. **রিয়েল DOM এবং ভার্চুয়াল DOM-এর মধ্যে প্রধান পার্থক্য কী?**
   * উত্তর: রিয়েল DOM সরাসরি ব্রাউজারের ভিউপোর্টকে রিপ্রেজেন্ট করে এবং এতে পরিবর্তন করা ধীরগতির ও ব্যয়বহুল। ভার্চুয়াল DOM মেমরিতে থাকা একটি হালকা অবজেক্ট কপি, যা ব্রাউজার পেইন্টিং মেকানিজম এড়িয়ে দ্রুত ক্যালকুলেশন করতে পারে।

### ১১. Best practices
* লিস্টের কি (key) হিসেবে সবসময় ডেটাবেসের ইউনিক আইডি বা `uuid` ব্যবহার করুন। কি কখনো বিল্ড টাইমে ডাইনামিক্যালি জেনারেট করবেন না (যেমন `Math.random()`), কারণ প্রতি রেন্ডারে তা বদলে যাবে।
* কম্পোনেন্টের হায়ারার্কি বা স্ট্রাকচার স্ট্যাটিক রাখার চেষ্টা করুন যাতে রিকনসিলিয়েশন অ্যালগরিদম সহজে নোড রিইউজ করতে পারে।

### ১২. Performance considerations
রেন্ডারিং অপ্টিমাইজ করতে `React.memo` ব্যবহার করুন যাতে প্যারেন্ট কম্পোনেন্ট আপডেট হলেও যে চাইল্ডগুলোর প্রপস পরিবর্তন হয়নি, তারা যেন রেন্ডার ফেজ স্কিপ করতে পারে।

### ১৩. When NOT to use it
যদি আপনার প্রজেক্টের সাইজ অত্যন্ত ছোট হয় এবং কোনো ডাইনামিক ইন্টারঅ্যাকশন বা স্টেট চেঞ্জ না থাকে, তবে React-এর ভার্চুয়াল DOM-এর মতো জটিল আর্কিটেকচার ব্যবহারের প্রয়োজন নেই।

### ১৪. Comparison with similar concepts
* **Virtual DOM vs Shadow DOM:** Virtual DOM হলো মেমরি অপ্টিমাইজেশন ও Reconciliation টুল। Shadow DOM হলো ব্রাউজারের ক্যাপসুলেশন (encapsulation) মেকানিজম যা স্টাইল আইসোলেট করতে ব্যবহৃত হয়।
* **Virtual DOM vs Svelte (No DOM):** Svelte বিল্ড টাইমে কোড কম্পাইল করে সরাসরি রিয়েল DOM আপডেটের জাভাস্ক্রিপ্ট কোড লেখে। তাই Svelte-এ মেমরিতে কোনো ভার্চুয়াল DOM থাকে না।

### ১৫. Summary in simple Bangla
ভার্চুয়াল DOM হলো রিয়েল DOM-এর একটি মেমরি কপি। যখন আমাদের স্টেট পরিবর্তন হয়, তখন React নতুন ও পুরোনো ভার্চুয়াল DOM-এর মধ্যে তুলনা করে (Diffing) এবং অত্যন্ত দক্ষতার সাথে কেবল পরিবর্তিত অংশটুকু আসল DOM-এ পেস্ট করে (Reconciliation)।

### ১৬. 5 MCQ questions
1. React-এর Diffing Algorithm-এর টাইম কমপ্লেক্সিটি কত?
   * A) \(O(n^2)\)
   * B) \(O(n^3)\)
   * C) \(O(n)\)
   * D) \(O(\log n)\)
   * *উত্তর: C*
2. নিচের কোনটি ভার্চুয়াল DOM সম্পর্কে সত্য নয়?
   * A) এটি ব্রাউজারের আসল DOM-এর কপি
   * B) এটি সাধারণ JavaScript অবজেক্ট
   * C) এটি ব্রাউজারে সরাসরি রেন্ডার হয়
   * D) এটি মেমরিতে থাকে
   * *উত্তর: C*
3. রিকনসিলিয়েশনের সময় লিস্ট এলিমেন্টগুলোর অবস্থান ট্র্যাক করতে কোনটি বাধ্যতামূলক?
   * A) id
   * B) index
   * C) key
   * D) class
   * *উত্তর: C*
4. React-এর রেন্ডারিং প্রসেসের দুটি মূল ধাপ কী কী?
   * A) Compilation & Execution
   * B) Render & Commit
   * C) Mount & Unmount
   * D) Diffing & Patching
   * *উত্তর: B*
5. Shadow DOM মূলত কোন কাজের জন্য ব্যবহৃত হয়?
   * A) React-এর পারফরম্যান্স বাড়ানোর জন্য
   * B) স্টাইল ও মার্কআপ ক্যাপসুলেশন করার জন্য
   * C) রিকনসিলিয়েশনের জন্য
   * D) কোনোটিই নয়
   * *উত্তর: B*

### ১৭. 5 Coding exercises
1. একটি React কম্পোনেন্ট লিখুন যা প্রতি সেকেন্ডে একটি ঘড়ির সময় আপডেট করে এবং প্রুফ করুন যে পেজের অন্যান্য অংশে কোনো রি-রেন্ডার হচ্ছে না (আসল DOM এ শুধু সময় নোডটি ফ্ল্যাশ করছে)।
2. একটি লিস্ট অবজেক্ট তৈরি করুন এবং `key` প্রপ ছাড়া ও `key` প্রপ সহ রেন্ডার করে ব্রাউজার কনসোলের ওয়ার্নিংটি অবজার্ভ করুন।
3. একটি বাটন তৈরি করুন যা ক্লিক করলে লিস্টের প্রথম আইটেম এবং শেষ আইটেমের পজিশন অদলবদল (swap) করবে। দেখুন `key` ব্যবহারে এটি কীভাবে দক্ষ হয়।
4. একটি সাধারণ টেক্সট ইনপুট বক্স ও বাটন তৈরি করুন। বাটনে ক্লিক করলে ইনপুট বক্সে থাকা টেক্সটটি একটি নতুন `li` নোড হিসেবে লিস্টের উপরে যোগ করবে। এর জন্য উপযুক্ত ইউনিক কি প্রদান করুন।
5. একটি নেস্টেড ট্রি স্ট্রাকচার (যেমন ফাইল অর্গানাইজার: ফোল্ডারের ভেতর ফাইল) তৈরি করুন এবং দেখান কীভাবে একটি নোড রিলনসিল করা হচ্ছে।

---

## ২. Strict Mode

### ১. Simple definition (বাংলায়)
React-এর Strict Mode হলো একটি বিশেষ হেল্পার বা টুল যা ডেভেলপমেন্টের সময় অ্যাপ্লিকেশনের সম্ভাব্য ভুলত্রুটি, মেমরি লিক এবং অপ্রচলিত (deprecated) কোড শনাক্ত করার জন্য অতিরিক্ত ওয়ার্নিং ও চেক প্রদান করে। এটি প্রোডাকশন বিল্ডে কোনো প্রভাব ফেলে না।

### ২. Why this concept exists
React-এর ফিউচার ভার্সনগুলোতে (যেমন Concurrent features) পারফরম্যান্স বাড়ানোর জন্য রেন্ডারিং প্রসেস মাঝপথে থামানো বা বাতিল করা হতে পারে। এই কনকারেন্ট ফিচারগুলো সঠিকভাবে কাজ করার জন্য কম্পোনেন্টগুলোর রেন্ডার মেথড এবং সাইড-ইফেক্টগুলো পিউর (pure) হতে হবে। Strict Mode এই পিউরিটি নিশ্চিত করতে সাহায্য করে।

### ৩. What problem it solves
ডেভেলপমেন্টের সময় এটি মেমরি লিক (যেমন আনসাবস্ক্রাইব না করা ইভেন্ট লিসেনার), আনসেফ লাইফসাইকেল মেথড এবং সাইড-ইফেক্ট আইডেন্টিফাই করতে সাহায্য করে।

### ৪. Real-life analogy
ভাবুন আপনি ড্রাইভিং লাইসেন্স পাওয়ার জন্য একটি ডেমো ট্র্যাকে গাড়ি চালাচ্ছেন। আপনার পাশে একজন অত্যন্ত কড়া ইন্সপেক্টর (Strict Mode) বসে আছেন। আপনি ডবল লাইনে লেন পরিবর্তন করলে বা ইন্ডিকেটর দিতে ভুলে গেলে তিনি সাথে সাথে লাল পেন দিয়ে নোট নিচ্ছেন এবং আপনাকে সতর্ক করছেন। কিন্তু আপনি যখন ফাইনাল রাস্তায় গাড়ি চালাবেন, তখন আর সেই ইন্সপেক্টর আপনার পাশে থাকবেন না। তিনি শুধু ডেমো ট্র্যাকে বা টেস্টের সময় আপনাকে পারফেক্ট করার জন্য ছিলেন।

### ৫. How React works internally regarding this concept
Strict Mode অন থাকলে, React ডেভেলপমেন্ট মোডে প্রতিটি কম্পোনেন্টকে **দুইবার রেন্ডার করে (Double Rendering)** এবং ইফেক্টগুলো (`useEffect`) দুইবার রান ও ক্লিনআপ করে। এর মূল উদ্দেশ্য হলো:
1. **Side-effect checks:** কোনো স্টেট আপডেট বা রেন্ডার লজিকে যদি কোনো ইম্পিউরিটি (Impurity) থাকে (যেমন রেন্ডারের ভেতর সরাসরি গ্লোবাল ভ্যারিয়েবল পরিবর্তন), তবে ডাবল রেন্ডারের কারণে ভুল ফলাফল আসবে এবং ডেভেলপার তা সহজে ধরতে পারবেন।
2. **Cleanup checks:** `useEffect`-এ কোনো সাবস্ক্রিপশন বা টাইমার থাকলে তা রেন্ডার বাতিল হলে মেমরি লিক তৈরি করে কিনা, তা চেক করতে React প্রথমে ইফেক্ট রান করে, তারপর ক্লিনআপ রান করে এবং পুনরায় ইফেক্ট রান করে।

### ৬. Basic example
```jsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
```

### ৭. Step-by-step explanation of the code
* `<React.StrictMode>` কম্পোনেন্টটি দিয়ে পুরো `<App />`-কে র্যাপ (Wrap) করা হয়েছে।
* এর ফলে অ্যাপের ভেতরের সমস্ত চাইল্ড কম্পোনেন্ট Strict Mode-এর রুলস অনুযায়ী চেক হবে।
* এটি একটি ডেভেলপমেন্ট-অনলি কম্পোনেন্ট। প্রোডাকশন বিল্ড তৈরির সময় এটি কোনো অতিরিক্ত কোড বা লজিক DOM-এ যোগ করে না।

### ৮. Another real-world example (Detecting memory leaks in useEffect)
```jsx
import React, { useState, useEffect } from 'react';

function MouseTracker() {
  const [coords, setCoords] = useState({ x: 0, y: 0 });

  useEffect(() => {
    const handleMouseMove = (e) => {
      setCoords({ x: e.clientX, y: e.clientY });
      console.log('Mouse moving...');
    };

    window.addEventListener('mousemove', handleMouseMove);

    // strict mode will fire cleanup immediately to test if listener is removed
    return () => {
      window.removeEventListener('mousemove', handleMouseMove);
    };
  }, []);

  return <p>Mouse at: {coords.x}, {coords.y}</p>;
}
```
যদি এই কোডে ক্লিনআপ ফাংশন (`return () => window.removeEventListener...`) না থাকত, তবে Strict Mode-এর কারণে মাউস নাড়ালে কনসোলে দ্বিগুণ মেসেজ প্রিন্ট হতে থাকত, যা সহজেই ডেভেলপারকে মেমরি লিক সম্পর্কে সজাগ করে দেয়।

### ৯. Common mistakes beginners make
* **ডাবল রেন্ডার দেখে কনফিউজ হওয়া:** শিক্ষানবিসরা প্রায়ই ভয় পেয়ে যান যে ডেভেলপমেন্ট মোডে বাটনে ক্লিক না করতেই কনসোল ডাবল প্রিন্ট কেন হচ্ছে বা এপিআই রিকোয়েস্ট দুইবার কেন যাচ্ছে। তারা ভাবেন এটি হয়তো React-এর বাগ বা প্রোডাকশনেও এমন হবে। কিন্তু এটি কেবল ডেভেলপমেন্টে Strict Mode-এর স্বাভাবিক আচরণ।
* **রেন্ডার ফেজে সাইড ইফেক্ট রাখা:** কম্পোনেন্টের বডির ভেতরে সরাসরি এপিআই কল করা বা লোকালস্টোরেজ আপডেট করা।

### ১০. Interview questions related to this topic
1. **React Strict Mode কেন ডেভেলপমেন্ট মোডে কম্পোনেন্ট দুইবার রেন্ডার করে?**
   * উত্তর: কম্পোনেন্টের রেন্ডারিং লজিকে কোনো অশুদ্ধতা (side-effects/impurities) আছে কিনা এবং লাইফসাইকেলে কোনো মেমরি লিক আছে কিনা তা যাচাই করার জন্য।
2. **Strict Mode কি প্রোডাকশনেও দুইবার রেন্ডার করে?**
   * উত্তর: না, Strict Mode শুধুমাত্র ডেভেলপমেন্ট ফেজে কাজ করে। প্রোডাকশন বিল্ডে এটি নিষ্ক্রিয় থাকে এবং কোনো পারফরম্যান্স ক্ষতি করে না।
3. **Strict Mode মূলত কোন কোন জিনিস ডিটেক্ট করতে পারে?**
   * উত্তর: আনসেফ লাইফসাইকেল মেথড, লিগ্যাসি স্ট্রিং রেফ এপিআই ব্যবহার, লিগ্যাসি ফাইন্ড-ডম-নোড ব্যবহার, আকস্মিক সাইড-ইফেক্ট এবং লিগ্যাসি কন্টেক্সট এপিআই।

### ১১. Best practices
* প্রোজেক্টের শুরু থেকেই `StrictMode` অন রাখুন যাতে কোনো ব্যাড প্র্যাকটিস বা মেমরি লিক কোডবেসে বড় রূপ নেওয়ার আগেই ধরা পড়ে।
* `useEffect`-এ যেকোনো এপিআই কল বা সাবক্রিপশন তৈরি করার পর অবশ্যই ক্লিনআপ ফাংশন লেখার অভ্যাস করুন।

### ১২. Performance considerations
ডেভেলপমেন্টে ডাবল রেন্ডারের কারণে অ্যাপ কিছুটা স্লো মনে হতে পারে, তবে এটি প্রোডাকশনে কোনো পারফরম্যান্স ওভারহেড তৈরি করে না।

### १३. When NOT to use it
এমন কোনো থার্ড-পার্টি লিগ্যাসি লাইব্রেরি যদি আপনার অ্যাপে থাকে যা React-এর আধুনিক আর্কিটেকচারের সাথে কম্প্যাটিবল নয় এবং প্রচুর স্ট্রিক্ট মোড এরর দেয়, তবে সাময়িকভাবে ওই নির্দিষ্ট পার্টের জন্য স্ট্রিক্ট মোড বন্ধ রাখা যেতে পারে।

### ১৪. Comparison with similar concepts
* **StrictMode vs Linter (ESLint):** ESLint আপনার কোডের সিনট্যাক্স ও স্ট্যাটিক অ্যানালাইসিস করে ভুল ধরেন। StrictMode রানটাইমে কম্পোনেন্টের বিহেভিয়ার চেক করে ভুল ধরে।

### ১৫. Summary in simple Bangla
Strict Mode হলো ডেভেলপমেন্টের সময় আপনার কোডকে আরও পারফেক্ট করার জন্য একটি সিকিউরিটি গার্ড। এটি ডেভেলপারকে মেমরি লিক ও ভুল কোড এভয়েড করতে সাহায্য করতে রানটাইমে অতিরিক্ত চেক করে এবং ডাবল রেন্ডার করায়।

### ১৬. 5 MCQ questions
1. Strict Mode নিচের কোন মোডে কাজ করে?
   * A) Production Mode only
   * B) Development Mode only
   * C) Testing Mode only
   * D) Both Development & Production
   * *উত্তর: B*
2. Strict Mode অন থাকলে ডেভেলপমেন্ট মোডে কম্পোনেন্ট কতবার রেন্ডার হয়?
   * A) ১ বার
   * B) ২ বার
   * C) ৩ বার
   * D) রেন্ডার হয় না
   * *উত্তর: B*
3. ডাবল রেন্ডারিংয়ের মূল উদ্দেশ্য কী?
   * A) অ্যানিমেশন টেস্ট করা
   * B) সাইড-ইফেক্ট ও মেমরি লিক শনাক্ত করা
   * C) ডেটাবেস স্পিড টেস্ট করা
   * D) ব্রাউজার ক্র্যাশ টেস্ট করা
   * *উত্তর: B*
4. নিচের কোনটি Strict Mode চেক করতে পারে না?
   * A) Deprecated API usage
   * B) Memory leaks in effects
   * C) CSS styling issues
   * D) Unsafe lifecycles
   * *উত্তর: C*
5. Strict Mode ব্যবহার করার সঠিক উপায় কোনটি?
   * A) `useStrictMode()`
   * B) `<StrictMode> ... </StrictMode>`
   * C) `strictMode: true` in config
   * D) `import strict from 'react'`
   * *উত্তর: B*

### ১৭. 5 Coding exercises
1. একটি `useEffect` হুক ব্যবহার করে উইন্ডো রিসাইজ ইভেন্ট লিসেনার সেট করুন এবং ক্লিনআপ ছাড়া রান করে স্ট্রিক্ট মোডে এর আউটপুট অবজার্ভ করুন। তারপর ক্লিনআপ যুক্ত করে প্রবলেমটি ফিক্স করুন।
2. কম্পোনেন্ট বডির ভেতর সরাসরি একটি গ্লোবাল কাউন্টার ভ্যারিয়েবল ইঙ্ক্রিমেন্ট করার কোড লিখুন। স্ট্রিক্ট মোডে ডাবল রেন্ডারের কারণে কাউন্ট কীভাবে অস্বাভাবিকভাবে লাফায় তা কনসোলে দেখুন।
3. একটি সিম্পল চ্যাট মেসেঞ্জার ইউআই বানান যা মাউন্ট হওয়ার সময় কনসোলে "Socket Connected" দেখাবে। ক্লিনআপ মেকানিজম ব্যবহার করে নিশ্চিত করুন যে ডাবল কানেকশন হচ্ছে না।
4. একটি এপিআই কল করার কম্পোনেন্ট তৈরি করুন এবং স্ট্রিক্ট মোডে এর নেটওয়ার্ক ট্যাব ট্র্যাক করে ক্লিনআপের গুরুত্ব বুঝুন।
5. একটি টাইমার বা স্টপওয়াচ তৈরি করুন যা প্রতি সেকেন্ডে আপডেট হবে। স্ট্রিক্ট মোডের ডাবল ইফেক্ট ফায়ারিংয়ের প্রভাবে টাইমার স্পিড দ্বিগুণ হচ্ছে কিনা এবং ক্লিনআপ দিয়ে তা ফিক্স করার রুলস প্র্যাকটিস করুন।
